# Guide plan

The working document for writing [the Ollin Guide](README.md). Readers should read [README.md](README.md) instead; this file is for whoever is writing the next chapter.

How to use it: pick the next chapter from the status table, read its brief below, then follow the session workflow in [AUTHORING.md](AUTHORING.md). When a chapter ships, update its status here, mark the coverage-matrix rows it satisfies, and check whether any parking-lot item it hosts has shipped in the framework meanwhile.

There is structural work in flight as well as chapters. *The shape ahead* below holds it, and while it has rows in it, read that section first: the chapter numbers everything else in this file uses are about to move.

## Status

| Chapter | File | Status |
|---|---|---|
| 1. Hello, Ollin | `01-HelloOllin.md` | done |
| 2. Color that works | `02-Color.md` | done |
| 3. Motion and time | `03-MotionAndTime.md` | done |
| 4. Randomness | `04-Randomness.md` | done |
| 5. Noise | `05-Noise.md` | done |
| 6. Grids and repetition | `06-GridsAndRepetition.md` | done |
| 7. Tiles that cover the plane | `07-Tiles.md` | done |
| 8. Words | `08-Words.md` | done |
| 9. Pictures and data | `09-Pictures.md` | done |
| 10. Vectors, gently | `10-Vectors.md` | done |
| 11. Forces and physics | `11-ForcesAndPhysics.md` | done |
| 12. Flocks and swarms | `12-FlocksAndSwarms.md` | done |
| 13. Growing things | `13-GrowingThings.md` | done |
| 14. Fields and flow | `14-FieldsAndFlow.md` | done |
| 15. Shapes as material | `15-ShapesAsMaterial.md` | done |
| 16. Layers and effects | `16-LayersAndEffects.md` | done |
| 17. Your first shader | `17-YourFirstShader.md` | done |
| 18. Iterated forms | `18-IteratedForms.md` | done |
| 19. Simulations on a grid | `19-GridSimulations.md` | done |
| 20. Simulations made of particles | `20-ParticleSimulations.md` | done |
| 21. 3D, gently | `21-3DGently.md` | done |
| 22. Worlds with weight | `22-WorldsWithWeight.md` | done |
| 23. Characters, vehicles, and cloth | `23-CharactersAndCloth.md` | done |
| 24. Sculpting with fields | `24-SculptingWithFields.md` | done |
| 25. Depth and the iPhone as a sensor | `25-DepthAndThePhone.md` | done |
| 26. Sound and control | `26-SoundAndControl.md` | done |
| 27. Seeing | `27-Seeing.md` | done |
| 28. Sharing and performing | `28-SharingAndPerforming.md` | done |
| A. Just enough Swift | `A-JustEnoughSwift.md` | done |
| B. Just enough math, visually | `B-JustEnoughMath.md` | done |
| C. Coming from p5.js and Processing | `C-ComingFromP5.md` | done |
| D. The complete toolbox | `D-CompleteToolbox.md` | done |

Statuses: `not started` → `figures` (figure sketches built and rendered) → `drafted` (prose written) → `done` (humanized, audited, committed). Appendices B and D keep growing with the chapters: a session that adds a chapter also adds its math ideas to B (an entry with a picture) and gives its new capabilities rows in D and in the coverage matrix.

Also tracked here so they aren't forgotten:

- The figure runner stays a local gate rather than a CI job, and the reason is the runner rather than the cost. Scarce CI minutes were the original reason and no longer decide it, since a full run went from twenty minutes to about three. What decides it now is that a CI runner cannot do this job honestly. Two figures (`27-Seeing/MotionBrush` and `27-Seeing/FlowArrows`) measure optical flow through the same Vision request `build.yml` already skips, because the virtualized runner fails it, and rendered pixels are GPU-specific anyway, so a CI render could only report that a figure compiled and ran, never that it still looks right. Reopen this if those figures are ever reworked, or if the runner grows a compile-only mode, which would catch the real rot (an API rename breaking a figure) without needing a GPU at all.
- The suite is 333 figures, and an unchanged run costs a few seconds, so run the whole thing rather than reaching for `--only`. A cold run is several minutes spread over four worker processes; the cache and the shards are described in *Figures* in [AUTHORING.md](AUTHORING.md). Two cautions on the number. It has grown with the figure count (the suite was 213 figures when three minutes was measured), and the runner's own "a full run costs about" line adds up per-figure times rather than reporting wall clock, so it reads about four times worse than the run actually is. Watch the trend anyway: a gate slow enough to skip protects nothing, which is the whole reason the cache exists.
- The ten figures that genuinely cannot reproduce carry `// figure: unstable`, so the runner verifies them without rewriting their images. Six are the GPU sims in `16-Simulations` (`ArtificialLife`, `Evolution`, `FluidAndBlobs`, `ParticleLenia`, `Swarm`, `SwarmChemistry`), whose neighbor sums are added in an order GPU atomics decide; `24-SculptingWithFields/CausticLight` accumulates photon parcels the same way; `21-3DGently/FieldWorld` has its surviving copies compacted by a GPU cull whose atomics decide their draw order; and two run on system ML models Ollin does not control, `27-Seeing/Trajectory` and `26-SoundAndControl/Listening`. There is no `git restore` step any more; a clean `git status` after a run is now the expected result, and churn under `Guide/Images` means something real changed.
- A website (Guide + Docs + gallery) is a later project; keep all markdown portable (plain relative links, standard tables, no HTML beyond the sanctioned `<img width>` figure embed in AUTHORING.md).
- Translations are out of scope for now.

## The shape ahead: the splits

A structure audit on 2026-08-18 found the Guide out of proportion, in one direction and for one reason. It was written once at a sane size, and then every feature the framework shipped was folded into whichever chapter had been assigned it, so the front half stayed fixed while the back half grew into catalogs. The 3D chapter reached 24,511 words across 48 flat sections, one sixth of the whole Guide. Simulations, Sound and control, and Sharing and performing each reached about 10,600, and each announces its own seam in its own prose. The cost lands on the anatomy's best rule. The simulations payoff composes 1 of that chapter's 22 teaching sections, and the sound payoff is called "a playable instrument" and plays no note.

Two tiers of fixes shipped on 2026-08-18 and 19: cross-reference linking, appendix wiring, missing figures, then in-chapter regrouping and a heading-findability pass. This third tier is the part that needs renumbering. It takes the Guide to **32 chapters in 5 parts**, no part more than 1.64 times another by words, with every chapter back inside the 2,000 to 8,000 word band the best-reading chapters already occupy.

Rows leave this section as they land, the way `ROADMAP.md` works. When a split ships, its chapters get ordinary rows in the status table above and ordinary briefs below, and the entry here goes. When the section is empty, the tier is done and the section goes too.

**This section names chapters by title, not by number, and that is deliberate.** Ten renumbers run before the tier is finished, and `Scripts/guide-renumber.sh` rewrites chapter numbers wherever it finds them. A target number written into the prose here would be moved by the very move it describes. The target table below is the one place numbers appear, in a form the tool leaves alone. Keep it that way when editing.

**Decisions taken, so they are not re-litigated.** Chapter files and both asset trees renumber together, so file order matches reading order everywhere (`Scripts/guide-renumber.sh` does it in one pass, `Scripts/guide-links.sh` proves it landed). The CSV and JSON section stays a Part I section inside "Pictures and data", because both halves are files you load; live data from the world is a separate future chapter, reserved in the parking lot below. The 3D physics block becomes two chapters rather than one of 12,000 words. The iterated-forms material becomes a new Part III chapter rather than the back half of "Fields and flow", so it gets a payoff of its own.

### The target

| Part | Chapters |
|---|---|
| I: Seeing something move | 1 Hello, Ollin · 2 Color that works · 3 Motion and time · 4 Randomness · 5 Noise · 6 Grids and repetition · 7 Tiles that cover the plane · **8 Words** · **9 Pictures and data** |
| II: Systems that come alive | 10 Vectors, gently · 11 Forces and physics · 12 Flocks and swarms · 13 Growing things · 14 Fields and flow · 15 Shapes as material |
| III: Pixels and light | 16 Layers and effects · 17 Your first shader · **18 Iterated forms** · **19 Simulations on a grid** · **20 Simulations made of particles** |
| IV: The third dimension | 21 3D, gently · **22 Meshes, maps, and materials** · **23 Landscapes and multitudes** · **24 Worlds with weight** · **25 Characters, vehicles, and cloth** · 26 Sculpting with fields |
| V: Out into the world | 27 Depth and the iPhone as a sensor · **28 Sound and control** · **29 Making sound** · 30 Seeing · **31 Sharing and performing** · **32 Installations** |

Bold marks a chapter this tier still has to create or rebuild. Two chapters change part: Depth and the phone moves to Part V, where it opens the world-as-input run ahead of the ears and the eyes, and the new iterated-forms chapter sits in Part III, because Buddhabrot, escape-time and attractor density are per-pixel plates and the chaos game opens the chapter as the CPU intuition.

### The splits, in order

Each lands complete: no gappy contents list, no chapter without its payoff, every gate green before the commit. Ascending order, because it is the cheaper direction and it settles the most-referenced early chapters first. The 3D chapter is cut from the back, so every intermediate state is still a whole Guide.

| Step | What moves | New payoff |
|---|---|---|
| 6 | The 3D chapter's terrain, instancing, culling and strands leave as Landscapes and multitudes. | Landscapes |
| 7 | The 3D chapter's mesh and texturing run leaves, and the mesh-only materials in Sculpting with fields join it (PBR, anisotropy, glass, clearcoat and sheen, subsurface). That also settles two principle-1 breaches, since `.dielectric` and `environment(.sky)` stop being used a chapter before they are taught. | Meshes |
| 8 | Sound and control splits three ways into two. Listening and control join under the existing title and keep the Resonator, which makes that chapter's hook and its README line accurate as written. Synthesis and composition become Making sound. | Making sound |
| 9 | Sharing and performing splits. Sharing keeps files, feeds, handoff, the stage and the five-evaluation set; Installations takes DMX and LED, the cost row and the whole "Leaving it running" run. | Installations |
| 10 | The parts are redrawn, Appendix D gains its new sections, README's reader's map replaces the "budget for the 3D chapter" line, and every matrix row is re-homed. | none |

### Briefs for the chapters this tier still creates

The existing briefs below were written before their chapters grew, so they describe a fraction of what shipped. Derive a new chapter's brief from the coverage-matrix rows and the section headings it inherits, never from the parent's brief.

**Words.**
Teaches: `drawText` and its box form; the three kinds of font; letters that move; text as geometry through `textToShapes`; scripts that do not work like English; vertical text and Mongolian; justification; hanging punctuation.
Payoff: new, and it must reach `textToShapes`, which the chapter calls its biggest idea and which currently reaches no piece.

**Pictures and data.**
Teaches: loading an image and asking it questions; a picture as marks, as one line, wound from thread, and sorted by pixel; then numbers from CSV and JSON, cross-linked forward to the live-data chapter reserved below.
Payoff: the type mosaic, inherited.

**Iterated forms.**
Teaches: the chaos game as the fern played as a game; four more chance games (flames, Buddhabrot, inversion, Kleinian); Schottky circle pairs; motion found in a formula (the 2D attractor maps as density plates); the bifurcation diagram; escape-time fractals.
Assumes: Randomness (chance), Your first shader (per-pixel thinking, for the GPU plates).
Payoff: new. The whole block currently feeds nothing, which is why it reads as an intrusion in two different chapters.
Draws from: `Docs/Drawing/Attractors.md`, `Docs/Generators/Fractals.md`, `Docs/Patterns/Schottky.md`.

**Simulations on a grid.** / **Simulations made of particles.**
Teaches, on a grid: state that lives on the GPU, Life and the wider automata, excitable media, sand, reaction-diffusion, multi-scale Turing, fluid, ripples, watercolor. In particles: a million grains, Physarum, ant colony optimization, Particle Lenia, the flock a thousand times bigger, SPH and soft bodies, evolution and its interactive twin, swarm chemistry.
Payoff: the organism stays with the grid half, which it genuinely composes. Particles needs a new one.

**Meshes, maps, and materials.**
Teaches: loading a mesh and taking a scene apart; normal, height, triplanar and detail maps; what the surface is per texel; smoothing from a cage and a surface that outgrows itself; decals; then the measured materials that only work on a mesh (PBR, anisotropy, glass, clearcoat and sheen, subsurface).
Payoff: new. Group the run under `###` families, the way Shapes as material already does.

**Landscapes and multitudes.**
Teaches: growing a landscape from a heightfield; a million riding the same field; ten thousand of the same thing; the world the camera trims; grass that was never built.
Payoff: new, and assemblable from figures already committed.

**Worlds with weight.** / **Characters, vehicles, and cloth.**
Teaches, in weight: bodies in the scene, asking what hit what and what is there, collision filtering, taking a direction away, machines out of joints, keeping what settled. In the animate chapter: someone to be in there, something to drive, turning without steering, letting a figure fall, cloth and a cape, a line that knows how it is turned, water and the raft.
Payoff: both new. Promote the `3D/Physics/Yard` example for one of them.

**Making sound.**
Teaches: a sketch that plays; building an instrument rather than choosing one; an instrument somebody recorded; the plucked string, modal synthesis, bowed and blown; music the sketch works out for itself; chords out of a key; tunings; tempo sync; sonification; spatial audio.
Payoff: new, and it has to actually play. `Examples/Audio/Generative` is the candidate. The harmony run and the FM and sampling run are figureless today, and the chapter has no way to hear anything; both are worth fixing here.

**Installations.**
Teaches: light instead of pixels (DMX and LED mapping); the cost row when it gets slow; then the whole unattended run, which is what actually breaks the clock, picking up where it left off, falling over, gallery hours, fitting the wall, several displays and several windows, and the log.
Payoff: new. `Examples/Installation/Unattended` is the candidate.

## Chapter briefs

Each brief lists what the chapter teaches, what it assumes, the piece it builds toward, and where to draw from. Figure lists are starting points, not contracts; the writing session decides the final set. Every chapter ends with two short sections: "Where this comes from" (technique credits, matching `ATTRIBUTION.md`) and "Go deeper" (links into `Docs/`).

### Part I: Seeing something move

**1. Hello, Ollin.**
Teaches: what a sketch is; `setup`/`draw`; the canvas and its top-left coordinate system; `background`, `fill`, `stroke`; `drawCircle`/`drawRect`/`drawLine`; motion by default (`time` in an expression moves); the working loop for the whole guide: `swift run OllinLive`, edit, save, watch; `@Param` knobs in the inspector; running any repo example.
Assumes: can program a little, in any language. First Swift callouts: a `class ... : Sketch` is a recipe; `var`/`let`; calling functions with labels.
Payoff: a small animated composition (drifting circles over a colored ground) the reader tunes live with knobs.
Figures: the coordinate-system diagram; a first-shapes contact sheet; the finished piece at a fixed frame.
Draws from: `Docs/Core/Sketch.md`, `Docs/Core/Canvas.md`, `Docs/Drawing/Drawing.md`, `Docs/Helpers/Input.md`, `Docs/Helpers/Parameters.md`; `Examples/Basic/`.

**2. Color that works.**
Teaches: `Color` and the named set; RGB vs HSB and when each is natural; opacity; palettes and `Ramp`s; mixing that looks right (OKLab by intuition: "mix in a space that matches your eye", one diagram, no math); `Colormap`s; gradient paint on shapes.
Assumes: Ch 1.
Payoff: a generative color-field poster from one palette, re-rolled live.
Figures: RGB-vs-HSB wheel diagram; a mixing comparison strip (naive vs perceptual); palette/ramp swatch sheets; the poster.
Draws from: `Docs/Drawing/Color.md`; `Examples/Color/`.

**3. Motion and time.**
Teaches: `time`, `deltaTime`, frame-rate independence; shaping functions as curves you can see: `map`, lerp, `step`, `smoothstep`, easing curves, each drawn as its curve next to the motion it produces (this is the chapter that must pass the smoothstep test; see AUTHORING.md); sine and cosine as "a point going around a circle", nothing more; phase; `Timeline` keyframes; `@Eased`/`@Smoothed`.
Assumes: Ch 1 (Ch 2 for nicer figures).
Payoff: a perfectly looping kinetic piece exported as a GIF (the guide's first export).
Figures: one curve-plus-motion pair per shaping function; the circle-to-sine diagram; the loop as GIF.
Draws from: `Docs/Helpers/Math.md`, `Docs/Helpers/Animation.md`; `Examples/Motion/`.

**4. Randomness.**
Teaches: `random` ranges and choices; seeds and why determinism matters (the same piece twice); `randomGaussian` vs uniform, shown as scatter; random walks; picking from palettes/arrays.
Assumes: Ch 1 to 3.
Payoff: a Molnár-inspired ordered-grid-with-disorder piece (connects to `Examples/Recreations/`).
Figures: uniform-vs-Gaussian scatter diagram; a seed contact sheet (same sketch, nine seeds); the finished piece.
Draws from: `Docs/Generators/Random.md`; `Examples/Randomness/`, `Examples/Recreations/`.

**5. Noise.**
Teaches: noise as "random that remembers"; 1D noise driving a value over time; 2D noise as terrain/texture; the third dimension as drift; `signedNoise`; layering two noise scales by hand for detail; noise vs random side by side.
Assumes: Ch 4.
Payoff: a drifting organic field piece.
Figures: random-vs-noise line comparison; a 2D noise field visualized; a zoom/scale diagram; the finished piece.
Draws from: `Docs/Generators/Noise.md`; `Examples/Randomness/NoiseField` and friends.

**6. Grids and repetition.**
Teaches: the `grid` helper looped once (cells and points, the `frame`/`center`/`column`/`row` a cell carries, nesting, `Insets` padding); transforms as moving the paper (`translate`/`rotate`/`scale`, `withState`); symmetry by hand and then the kaleidoscope fold (`symmetry`/`noSymmetry`, and why the mirrored form is the one worth having); `withClip` and nested clips; the divisions that aren't square (`hexGrid`, `triangleGrid`, `subdivide`, `maze`, `apollonianGasket`).
Assumes: Ch 2 (`Ramp`), Ch 4 (seeds and `random`).
Payoff: a wall of rosettes. One grid, one crooked arm, a fold count per block, and a clip that cuts every medallion to a disc.
Figures: grid anatomy; the four transform steps; the pinwheel quilt; the rosette; the kaleidoscope triptych; clip regions; the four other divisions; the finished wall.
Draws from: `Docs/Drawing/Geometry.md`, `Docs/Drawing/Drawing.md`, `Docs/Drawing/Tiling.md`; `Examples/Patterns/Grid`, `Kaleidoscope`, `Shapes/Clipping`.

**7. Tiles that cover the plane.**
Teaches: the edge-agreement idea itself (a tile promises only to meet its own doorways, and the order across the page is what emerges); Truchet's two tiles and the strand output; hitomezashi's one bit per grid line and its two faces; the never-repeating sets (`penroseTiling` with its arcs, `wangTiling`, `girihPattern` and the contact angle, `spectreTiling` and the single-shape result of 2023); `hyperbolicTiling` on the Poincare disk, with parity and depth coloring and the panning viewpoint.
Assumes: Ch 6 (the grid and the transforms), Ch 4 (seeds), Ch 5 (noise, for the finished piece's color).
Payoff: a meandering tangle. Truchet strands stroked twice, a rim pass under a color pass, with noise painting slow weather across it.
Figures: the two Truchet tiles; the doorway diagram; both hitomezashi faces; the four aperiodic panels; two Poincare disks; the finished tangle.
Draws from: `Docs/Drawing/Truchet.md`, `Hitomezashi.md`, `AperiodicTilings.md`, `HyperbolicTiling.md`; `Examples/Patterns/`.

**8. Words.**
Teaches: `drawText` and its box form; the three kinds of font (outline, stroke, bitmap) and why the pen font takes a stroke; per-glyph motion; `textToShapes` as the chapter's hinge, with warping and `resampled` respacing; scripts that do not work like English; vertical text and Mongolian; justification; hanging punctuation.
Assumes: Ch 5 (noise, for the warp), Ch 6 (`withState`).
Payoff: a type specimen. One word as glyph geometry, filled and then beaded along its own respaced outline, the three font kinds under it, and a passage set flush on both edges.
Figures: the specimen sheet; the three-kinds specimen; per-glyph wave; the warp triptych; five scripts; columns down the page; Mongolian; hanging stops.
Draws from: `Docs/Drawing/Text.md`; `Examples/Text/`.

**9. Pictures and data.**
Teaches: loading an image and sampling it; a picture as glyph marks, as stipple dots, as one unbroken line, as a spanning tree, as thread wound between pins; halftone screens; sorting the pixels a picture already has; then numbers from CSV and JSON through `loadTable` and `loadJSON`.
Assumes: Ch 8 (type, for the finished piece), Ch 6 (the grid).
Payoff: a picture painted with type. One image sampled over a grid, every sample answered by a letter sized and colored by the pixel under it.
Figures: pixel sampling; the glyph mosaic; the single line and the spanning tree; thread from pins; sorted pixels; data as material; the finished mosaic.
Draws from: `Docs/Drawing/Images.md`, `GlyphMosaic.md`, `Halftone.md`, `PixelSorting.md`, `Docs/Generators/Stippling.md`, `SingleLine.md`, `SpanningTree.md`, `StringArt.md`, `Docs/Helpers/Data.md`; `Examples/Images/`, `Examples/Data/`.

### Part II: Systems that come alive

**10. Vectors, gently.**
Teaches: `Vector2` as an arrow you can draw; add/subtract/scale shown geometrically; magnitude and normalize; position, velocity, acceleration as three arrows on one body; steering toward a target; wrapping/bouncing at edges.
Assumes: Ch 1 to 6. This is Appendix B's biggest client; every operation gets a picture.
Payoff: a swarm that chases the mouse.
Figures: arrow-arithmetic diagrams (add, scale, normalize); the position/velocity/acceleration trio; the swarm.
Draws from: `Docs/Drawing/Geometry.md`, `Docs/Helpers/Math.md`; `Examples/Motion/`.

**11. Forces and physics.**
Teaches: force → acceleration by intuition; hand-rolled gravity/drag on the Ch 10 mover; then the `World`: Verlet particles and springs (soft), bodies/colliders/joints (rigid); `grab` for mouse interaction; the ready-made systems (`IKChain`, `DoublePendulum`, `NBody`); forces as arrangement (`ForceLayout`: repulsion everywhere plus springs on edges, cooled to a frozen web you can grow, pin, and drag); when to hand-roll vs simulate.
Assumes: Ch 10.
Payoff: an interactive physics toy (a hanging chain or tumbling stack the mouse can grab).
Figures: force-accumulation diagram; spring-rest-length diagram; soft-vs-rigid contact sheet; the motion-systems three-panel; the graph-settling three-panel; the toy.
Draws from: `Docs/Simulation/Physics.md`, `Docs/Simulation/Motion.md`, `Docs/Generators/ForceLayout.md`; `Examples/Physics/`, `Examples/Patterns/ForceGraph`.

**12. Flocks and swarms.**
(Retitled from "Agents", 2026-07-06: the field's classic word for these rule-following creatures now reads as AI assistants, and this guide will outlive the collision. The chapter still teaches the term: open with a one-sentence disambiguation, crediting Craig Reynolds' 1986 *autonomous agents*, coined decades before "agent" meant software wielding a chatbot, then speak in flock/boid/creature language. Never use "agent" bare in headings or the TOC.)
Teaches: one creature that steers (the steering move: desired velocity minus velocity, capped; seek, arrive, wander, pursue via `Vehicle`); local rules → global behavior; the three boid rules, each visualized alone before combining; perception radius; `Boids`; differential growth as "a line that wants space"; stateful systems you step (holding a class across frames).
Assumes: Ch 10 (Ch 11 helps).
Payoff: a living flock piece, colored by heading.
Figures: steering-move diagram; wander/arrive diagrams; one diagram per boid rule; rule-combination sequence; a growth time-lapse strip; the finished piece.
Draws from: `Docs/Generators/Steering.md`, `Docs/Generators/Boids.md`, `Docs/Generators/DifferentialGrowth.md`; `Examples/Motion/Steering`, `Examples/Patterns/Flocking`, `Examples/Patterns/DifferentialGrowth`.

**13. Growing things.**
Teaches: recursion by drawing (a fractal tree written by hand first); rewriting rules (L-systems) and the turtle, grown iteration by iteration; branching with the stack; stochastic rules; parametric rules (symbols carrying numbers, so lengths can be fractions and a trunk can taper); space colonization (growth that claims space: veins and trees from attraction points, the pipe model for weight); diffusion-limited aggregation (growth by chance: frost from frozen walkers); crack growth (growth by collision: perpendicular cracks subdividing the plane, the one-sided wash); Wave Function Collapse as "every neighbor must agree", watched as it solves; the chance games (the chaos game deriving the same fern a second way, then fractal flames, circle-inversion limit sets, and Kleinian limit curves).
Assumes: Ch 4 (seeds), Ch 6 (grids, for WFC), Ch 12 (stateful steppers).
Payoff: a procedural garden.
Figures: branch-stack diagram; L-system expansion table + drawing per iteration; the three panels of what carrying a number buys; a space-colonization growth sequence; a DLA cluster; the cracked city; WFC solve sequence; the garden.
Draws from: `Docs/Generators/LSystem.md`, `Docs/Generators/SpaceColonization.md`, `Docs/Generators/DiffusionLimitedAggregation.md`, `Docs/Generators/DielectricBreakdown.md`, `Docs/Generators/CrackGrowth.md`, `Docs/Generators/Meander.md`, `Docs/Generators/WaveFunctionCollapse.md`; `Examples/Patterns/LSystem`, `Examples/Patterns/ParametricLSystem`, `Examples/Patterns/Venation`, `Examples/Patterns/Dendrite`, `Examples/Patterns/Cracks`, `Examples/Patterns/Meander`, `Examples/Patterns/WaveFunctionCollapse`.

**14. Fields and flow.**
Teaches: a field as "an answer at every point" (the mental model shaders and SDFs later reuse); visualizing a field with arrows; `FlowField` from noise; level curves of a scalar field (`isolines`, marching squares, the stacked-levels contour map, the image form); tracing streamlines; evenly-spaced streamlines; advecting particles; strange attractors as found motion; the logistic map and its bifurcation diagram (one dial's route to chaos).
Assumes: Ch 5 (noise), Ch 10 (vectors).
Payoff: a flow-field print (the Fidenza look, credited as such).
Figures: field-of-arrows diagram; streamline tracing step diagram; even-spacing comparison; attractor plates; the labeled bifurcation diagram; the print.
Draws from: `Docs/Generators/FlowField.md`, `Docs/Drawing/Attractors.md`, `Docs/Generators/Bifurcation.md`; `Examples/Patterns/Streamlines`, `Examples/Patterns/Bifurcation`, `Examples/3D/StrangeAttractor`.

**15. Shapes as material.**
Teaches: `Path` and `Shape` (curves, contours, holes); booleans (union/subtract/intersect) as vocabulary; offsetting; Voronoi and Delaunay from scattered points; blue-noise scatter; circle and shape packing; hatching fills for pen plotters; SVG export.
Assumes: Ch 4 to 6, Ch 10.
Payoff: a plotter-ready composition (exported SVG shown beside the raster render).
Figures: boolean-ops diagram; Voronoi/Delaunay duals; packing time-lapse; hatching close-up; the composition.
Draws from: `Docs/Drawing/Geometry.md`, `Docs/Drawing/Voronoi.md`, `Docs/Generators/BlueNoise.md`, `Docs/Generators/Packing.md`, `Docs/Generators/ShapePacking.md`, `Docs/Output/Export.md` (SVG); `Examples/Shapes/`, `Examples/Patterns/`.

### Part III: Pixels and light

**16. Layers and effects.**
Teaches: drawing into a layer (`renderTarget`/`withTarget`); filtering a layer (blur, bloom, the catalog at a glance); compositing with blend modes; `compose { }`; feedback (trails, tunnels); accumulation (`noClear`) and long-exposure looks; HDR and `toneMap` (why bright light needs rolling off).
Assumes: Part I; Ch 14's field mental model helps.
Payoff: an earlier piece reworked with layers, glow, and feedback.
Figures: layer-graph diagram; filter-family contact sheet; blend-mode grid; a feedback step sequence; before/after of the rework.
Draws from: `Docs/Drawing/Effects.md`, `Docs/Drawing/Accumulation.md`, `Docs/Drawing/HDR.md`; `Examples/Effects/`.

**17. Your first shader.**
Teaches: the per-pixel mental model (the Ch 14 field, evaluated by the GPU at every pixel); uv space; writing `shade(uv, info)`; color as a return value; params; the shaping functions from Ch 3 reappearing per-pixel (smoothstep as an edge, this time already familiar); the shader library at a glance; `Visual` chains for combining without writing code.
Assumes: Ch 3 (shaping), Ch 14 (fields), Ch 16 (layers).
Payoff: a live animated shader visual.
Figures: the pixel-grid mental-model diagram; uv-space diagram; smoothstep-as-edge pair; chain-graph diagram; the visual.
Draws from: `Docs/Shaders/Shaders.md`, `Docs/Shaders/Visuals.md`, `Docs/Shaders/ShaderLibrary.md`; `Examples/Shaders/`.

**18. Iterated forms.**
Teaches: one idea in six systems, that a picture can be the density of an orbit rather than a drawn shape. The chaos game and `ifsPoints`; fractal flames; the Buddhabrot and its three caps; circle-inversion limit sets and why tangency matters; Kleinian limit curves; Schottky paired circles with `lean` and `twist`; the 2D `ChaoticMap` family as density plates; `IteratedMap` and the bifurcation diagram; escape-time fractals and orbit traps.
Assumes: Ch 4 (chance), Ch 6 (the grid), Ch 17 (per-pixel thinking, for the escape-time plates).
Payoff: a plate of four orbits. Four rules handed to one plotting function, laid out in a two-by-two grid, with crowding rather than ink making the bright regions.
Figures: the chaos-game fern; the four-game family; the Buddhabrot plate; the Schottky circles and gaskets; the attractor plates; the bifurcation diagram; the escape-time pair and its trapped orbits; the finished plate.
Draws from: `Docs/Generators/Fractals.md`, `Docs/Drawing/Attractors.md`, `Docs/Generators/Bifurcation.md`; `Examples/Patterns/`.

**19. Simulations on a grid.**
Teaches: state that lives on the GPU as a ping-ponged field; the Game of Life and the wider automata (elementary rules, turmites, Lenia); excitable media; the sandpile; reaction-diffusion and the feed/kill map; multi-scale Turing; fluid you can stir; a ripple pool; watercolor that behaves.
Assumes: Ch 16 (layers and feedback), Ch 17 (per-pixel thinking).
Payoff: the organism, a reaction-diffusion labyrinth grown from a scatter of dots.
Figures: the Life panel; the automata sheet; excitable media; the sandpile; the Gray-Scott map; the Turing pyramid; fluid; ripples; watercolor; the finished organism.
Draws from: `Docs/Drawing/Effects.md#simfield`, `Docs/Generators/CellularAutomata.md`; `Examples/Simulation/`.

**20. Simulations made of particles.**
Teaches: a buffer of individuals updated by one small MSL program (`Particles`, the locals in scope, the `custom` knobs); Physarum agents that talk through the floor; ant colony optimization; Particle Life and the Primordial Particle System; Particle Lenia; the flock at GPU scale; SPH fluid and shape-matched jellies; evolution by score and by eye; swarm chemistry.
Assumes: Ch 19 (the GPU simulation loop), Ch 12 (flocking), Ch 16 (accumulation).
Payoff: the drift. A quarter of a million particles reading one curl field at three zooms, summed as light onto a canvas that keeps its past.
Figures: a million grains; Physarum; the ant search; Particle Lenia; the bigger swarm; SPH and jellies; evolution; pick-and-breed; swarm chemistry; the finished drift.
Draws from: `Docs/Shaders/Compute.md`, `Docs/Simulation/ArtificialLife.md`, `Swarm.md`, `Fluids.md`, `Evolution.md`, `Docs/Generators/AntColony.md`; `Examples/Simulation/`.

### Part IV: The third dimension

**21. 3D, gently.**
Teaches: the camera as an orbiting eye (`cameraShowcase`, drag to look); depth; solid primitives; the 3D transform stack; lights and materials by playing (presets, then individual lights); matcap as "shading from a picture"; loading a mesh; normal maps as relief without geometry; the surface-map set (metallic-roughness, occlusion, emissive) as the picture deciding what the surface is; height maps read two ways (parallax occlusion vs real displacement, the silhouette as the tell); triplanar projection as texture for surfaces nobody unwrapped; growing a landscape (`Heightfield`, diamond-square, hydraulic and thermal erosion, the three read-outs); rigid bodies inside the scene (`World3D`, `withBody`, the deterministic collapse); casting shadows; the scene-inspection views.
Assumes: Part I; Ch 10 (vectors; `Vector3` is introduced as "the same, plus z"); Ch 11 (the physics world's shape).
Payoff: a rotating sculptural scene the viewer can orbit.
Figures: camera-orbit diagram; primitive catalog sheet; lighting-preset contact sheet; material sweep; the crate collapse; the scene.
Draws from: `Docs/3D/3D.md`, `Docs/3D/Camera.md`, `Docs/3D/Combining.md`, `Docs/Simulation/Physics3D.md`; `Examples/3D/`.

**22. Worlds with weight.**
Teaches: `World3D` and rigid bodies in the 3D scene; asking what hit what and what is there (the query family); collision groups and layers; taking degrees of freedom away; machines out of joints (revolute, slider, gear, rack, path, pulley, and links between joints); snapshots and determinism.
Assumes: Ch 11 (the 2D physics world), Ch 21 (the 3D scene).
Payoff: the contraption. A motor on one hinge, a gear link to a second, and a bar that sweeps a shelf of crates, with nothing animated anywhere.
Figures: the crate fall; the windmill; the rockslide; the trigger and the sightlines; the sorted and flattened pairs; the machines; what settled.
Draws from: `Docs/Simulation/Physics3D.md`; `Examples/3D/Physics/`.

**23. Characters, vehicles, and cloth.**
Teaches: the three things a rigid body models badly. A character controller (a capsule you move, its feet as origin, and the floor-body gotcha); a raycast vehicle (chassis, sprung wheels, steering and drive); ragdolls; cloth as a simulated mesh with pinned vertices; capes; ropes as Cosserat rods; buoyancy and a raft.
Assumes: Ch 22 (the world and its bodies).
Payoff: the yard. A figure on its feet, a truck on four springs, and a banner pinned at two corners, none of them held up the same way.
Figures: the walker; the joyride; the crawler; the ragdolls; the cloth and the cape; the rope; the floating and the raft.
Draws from: `Docs/Simulation/Physics3D.md`; `Examples/3D/Physics/`.

**24. Sculpting with fields.**
Teaches: signed distance as "how far, and which side" (one diagram); the 2D combinators (`SDF`, smooth union as melting) built on Ch 14's field model; then the same idea raymarched in 3D (`SDF3D`, `drawSDF3D`); domain tricks (mirror, repeat) as space folding; a light touch of environments/PBR for the finish (pointers to Docs for depth).
Assumes: Ch 14, Ch 21 (Ch 17 helps).
Payoff: a melted-glass sculpture, orbitable.
Figures: signed-distance diagram; smooth-min melt strip; a march-the-ray diagram; domain-repetition diagram; the sculpture.
Draws from: `Docs/Drawing/Combinators.md`; `Examples/Shapes/Combinators*`, `Examples/3D/Raymarching/`.

**25. Depth and the iPhone as a sensor.**
Teaches: point clouds; what an RGBD frame is (color + depth + intrinsics, one diagram); playing a recorded `.r3d` clip as a cloud; the live USB stream; the Ollin Capture app streams (body, face, world depth, segmentation); world fusion (sweeping a room into one cloud); depth compositing (2D drawing inside a 3D scene).
Assumes: Ch 21. Requires hardware for the live half; the recorded-clip path works for every reader and leads the chapter.
Payoff: a room-scan artwork (or a recorded-clip artwork without the hardware).
Figures: RGBD anatomy diagram; unprojection diagram; a fusion sweep sequence; the artwork.
Draws from: `Docs/3D/RGBD.md`, `Docs/3D/Record3D.md`, `Docs/3D/Phone.md`, `Docs/3D/DepthCompositing.md`; `Examples/3D/` (Depth/Phone groups).

### Part V: Out into the world

**26. Sound and control.**
Teaches: hearing (`amplitude`, `spectrum`, bands, beats) and drawing what you hear; the microphone vs a file; MIDI knobs and pads; OSC from a phone/tablet; binding any of them to `@Param` so one sketch is playable from anywhere.
Assumes: Part I (Ch 16 makes richer visuals).
Payoff: an audio-reactive visual played with a MIDI controller (or the keyboard/mouse fallback).
Figures: spectrum-anatomy diagram; beat-detection timeline; a binding-flow diagram (controller → param → visual); the visual.
Draws from: `Docs/Helpers/Audio.md`, `Docs/Integration/MIDI.md`, `Docs/Integration/OSC.md`, `Docs/Integration/Controller.md`, `Docs/Helpers/Parameters.md`; `Examples/Audio/`, `Examples/Integration/`.

**27. Seeing.**
Teaches: the webcam as a live image; the tracker model (attach, read typed results in `draw`); a tour in three depths: hands/face/body (taught), contours and optical flow (shown), the wider catalog (pointed); coordinate mapping done right; video files as material, including analyzing them.
Assumes: Part I; Ch 8 (images), Ch 10 (vectors).
Payoff: an interactive mirror piece (the reader's motion paints).
Figures: tracker-flow diagram; landmark-map figures; a flow-field-from-motion figure; the mirror.
Draws from: `Docs/Vision/Vision.md`, `Docs/Video/Video.md`; `Examples/Vision/`, `Examples/Video/`.

**28. Sharing and performing.**
Teaches: exporting stills, sequences, video, GIF; SVG for plotters (closing Ch 15's loop); publishing to other apps (Syphon) and as a system camera; DMX light on a real rig (and a console driving the sketch back); the canvas itself onto LED strips and matrices (`LEDMap`); the live-coding host (`OllinLiveCoding`): evaluate-on-command, code over visuals, performing a set; reproducibility as a sharing feature (seeds, params).
Assumes: everything before it, lightly.
Payoff: a short performed piece, live-coded, recorded, and shared.
Figures: export-formats map; a Syphon-into-another-app screenshot; the lamps-and-bytes DMX diagram; the LED-wall mapping diagram; the performance host annotated; frames from the performance.
Draws from: `Docs/Output/Export.md`, `Docs/Integration/Syphon.md`, `Docs/Integration/VirtualCamera.md`, `Docs/Integration/DMX.md`, `Docs/Tools/LiveCoding.md`; `Examples/Export/`, `Examples/Live/`.

### Appendices

**A. Just enough Swift.** The guide's Swift, gathered: values and types, functions and labels, classes vs structs, closures, optionals as encountered, property wrappers as used by `@Param`/`@Eased`. Builds on `Docs/Swift.md` rather than duplicating it: the appendix is the pedagogical pass, the primer stays the quick reference. Written after several chapters exist so it matches what the guide actually uses. Decided 2026-07-08, the three Swift docs get one reader each: A is the narrative pass for the Guide's audience (an engineer from any language, no p5 assumed), `Docs/Swift.md` becomes the pure language quick-reference, and Appendix C owns the p5/Processing angle (see C's brief for the `Docs/Swift.md` refocus that goes with it).

**B. Just enough math, visually.** One page per idea, each with an Ollin-rendered picture: coordinates, angles and radians, sine/cosine as a circle, vectors, interpolation and the shaping functions, distance, randomness distributions, fields, matrices as "move/turn/scale" (no algebra). Every chapter that introduces a math idea adds its entry here in the same session, as a picture-plus-plain-words page in the matching theme group.

**C. Coming from p5.js and Processing.** The translation table (`circle()` → `drawCircle`, `createCanvas` → `canvasSize`, `push`/`pop` → `withState { }`, `random` seeds, `noise`, classes, the loop), what's the same, what's idiomatically different, and the habits worth dropping. This is also the roadmap's migration-guide item; writing this appendix completes it. Decided 2026-07-08: C owns the p5/Processing framing outright, and the session that writes it also refocuses `Docs/Swift.md` into the pure language quick-reference (today it's titled "Swift for p5.js newcomers", which is C's job): move the p5 comparisons here, keep the language mechanics there, cross-link both ways.

**D. The complete toolbox.** The 100%-coverage surface, generated from the matrix below: every capability, one plain-words line, where the Guide teaches it (if it does), and its Docs page. Doubles as the guide's index of the framework.

## Feature-coverage matrix

The guarantee that the Guide gives awareness of everything Ollin ships. One row per capability (keyed by Docs page, plus rows for capabilities without their own page). Depth, from most to least:

- **taught**: a chapter section explains it, with a figure. This is the goal for every row.
- **shown**: it appears in a worked example with a sentence or two, so a reader can find it and run it. This is the floor for anything that has shipped.
- **pointed**: named with a one-liner and a Docs link, at minimum in Appendix D. **Not a resting place.** A `pointed` row is a promise to teach, and it is only legal while the Guide debt ledger below names the chapter it is owed to.

`Scripts/guide-coverage.sh` enforces the rules: no row unassigned, no row `pointed` without a debt entry, every Docs page has a row, every Docs page reachable from a numbered chapter's "Go deeper" list, and every Docs page present in Appendix D. Run it before committing anything that ships a user-facing capability; it is step 5 of the ship checklist in `CLAUDE.md`.

Why the script exists rather than a habit: a row marked `pointed` used to satisfy the checklist completely, so a feature could ship, get its one-line table entry, and never be taught. Seven slices landed that way over two days at the end of July 2026 while every audit stayed green, because the matrix said each row was accounted for. Counting rows was never the check; the check is that the depth column is honest.

| Capability (Docs page) | Guide home | Depth |
|---|---|---|
| `Core/Sketch.md` (lifecycle, time, loop) | Ch 1 | taught (`setup()`/`draw()` and the clock; `noLoop()` and `@main` pointed) |
| `Core/Canvas.md` (canvasSize, windowMode) | Ch 1 | taught ("The canvas is not the window" with a diagram: the two are independent, `width` reports the canvas either way, exports ignore the window, the preset families including paper sizes and `.dpi()`, and the three `windowMode` cases) |
| `Swift.md` (language primer) | Ch 1 callouts, Appendix A | taught |
| `Drawing/Drawing.md` (shapes, state, transforms) | Ch 1, Ch 6 | taught |
| Variable-width strokes (`strokeProfile`/`noStrokeProfile`, `StrokeProfile`, in `Drawing/Drawing.md`) | Ch 15 | taught ("A mark, not a line: strokeProfile" with a three-panel figure: the profile as a multiplier on `strokeWeight`, taper/ramp/values along the path against the direction-driven nib, sampling density, why the analytic shapes ignore it, and the filled-outline vector export) |
| Stroke dynamics (`StrokeMark`/`drawMark`, `StrokeDynamics`/`StrokeResponse`/`StrokeInput`, `pressure`/`pressureIsAvailable`; `Drawing/Marks.md`) | Ch 15 | taught ("Painting as it happens: stroke dynamics" after the profile section, with a three-panel figure over one paced curve: why a fraction along the path cannot exist mid-stroke, the ten-line paint loop, width and opacity as separate named axes, the pressure fallback read in `mousePressed()`, and profiles composing with dynamics) |
| Brushes (`strokeBrush`/`noStrokeBrush`, `Brush`; `Drawing/Marks.md#brushes`) | Ch 15 | taught ("Stamps along the path: brushes" after the dynamics section, with a three-panel figure over one S-curve: separate stamps reading as a solid mark, spacing measured in stamp sizes rather than pixels, the jitter/angle/scatter/count knobs as fractions of the stamp, non-circular tips, composing with a profile, and stamps exporting as real shapes) |
| Kaleidoscope symmetry (`symmetry`/`noSymmetry`, in `Drawing/Drawing.md`) | Ch 6 | taught ("The fold, done for you" after the hand-rolled rotate loop, with a three-panel figure: symmetry as drawing state, why the mirrored form is the one worth having, and folding around the current origin) |
| Clipping (`withClip`, in `Drawing/Drawing.md`) | Ch 6 | taught ("Drawing inside a shape" with a three-panel figure: the same stripes confined three ways, all three region types, and nesting as intersection) |
| `Drawing/Color.md` (Color, OKLab, palettes, colormaps) | Ch 2 | taught |
| Gradient paint (`Drawing/Color.md`) | Ch 2 | taught |
| Palette file import (`loadPalette`/`loadPalettes`, hex/CSV/TSV/JSON/ASE; `Drawing/Color.md`) | Ch 2 | taught |
| Palette extraction from an image (`Palette(extractedFrom:)`; `Drawing/Color.md`) | Ch 2 | taught |
| Image dithering (`Image.dithered`: error diffusion, ordered Bayer, blue noise; `Drawing/Color.md`) | Ch 2 | taught (a three-panel figure over one gradient: why snapping bands, the two families and how each decides, `.none` as the teaching control, `levels:` posterizing, and the do-it-in-setup rule) |
| Retained batches (`Batch`, `makeBatch`/`drawBatch`; `Drawing/Batches.md`) | Ch 15 | taught (after the plate's setup/draw split: what is still costing you per frame, `makeBatch`/`drawBatch`, the measured 150k-circle numbers, transforms at replay, and the refuse-at-the-funnel list) |
| `Helpers/Math.md` (map, lerp, dist; the shaping scalars clamp/fract/step/smoothstep) | Ch 3, Appendix B | taught |
| `Helpers/Animation.md` (easing, @Eased, @Smoothed, Timeline; loopProgress/pingPong) | Ch 3 | taught |
| `Helpers/Input.md` (mouse, keyboard) | Ch 1 | taught |
| `Helpers/Parameters.md` (@Param) | Ch 1; bindings in Ch 26 | taught (typed family: sliders/stepper/color well taught in Ch 1's finished piece, the rest named + pointed; groups/icons pointed) |
| `Generators/Random.md` | Ch 4 | taught |
| `Generators/Noise.md` (noise, signedNoise, loop:, fbm, curlNoise) | Ch 5; curl in Ch 14/16 | taught |
| Noise toolkit (simplexNoise, worley, ridgedFbm/turbulence, warpedFbm; the `.cellular` generator + `.noise` warp knob; `Generators/Noise.md`) | Ch 5 (shader-lib mirror named in Ch 17) | taught (the family toured with two figures in Ch 5; feature:/jitter: and warp: explained; GPU legs pointed to the Cellular/DomainWarp examples) |
| `Drawing/Geometry.md`: `Grid`, `Insets` | Ch 6 | taught |
| `Drawing/Truchet.md` | Ch 7 | taught |
| `Drawing/Hitomezashi.md` | Ch 7 | taught ("One coin per line: hitomezashi" with the HitomezashiFaces figure: the one-bit-per-line rule, both faces with the fill-then-stitch snippet, biased flips, explicit bits and the word-encoding trick; lineage in "Where this comes from") |
| `Drawing/Tiling.md` (HexGrid/TriangleGrid, subdivide, Maze, apollonianGasket) | Ch 6 | taught ("Grids that aren't square" with a four-panel figure: the other two regular tilings and why hexes keep their proportions, hex ring distance, recursion versus tabulation for layout, what makes a maze perfect and the algorithm as a texture knob, plus the gasket) |
| `Drawing/HyperbolicTiling.md` (hyperbolicTiling, the {p,q} tessellations of the Poincaré disk) | Ch 7 | shown ("More room than the page has: hyperbolic tiling": why only three regular tilings fit on flat paper and how the disk holds the rest, the parity checkerboard snippet, depth as the odd-`meeting` fallback, the panning viewpoint, and the Coxeter-to-Escher lineage in "Where this comes from") |
| `Drawing/AperiodicTilings.md` (penroseTiling, wangTiling, girihPattern, spectreTiling) | Ch 7 | taught ("Tiles that never repeat: aperiodic tilings" with a four-panel figure: what aperiodicity is, the Penrose pair and its arc decoration, the spectre and the einstein problem, Wang edge matching as the historical root, and girih as the Truchet doorway idea generalized, with the angle dial; snippets for the rhombs and the hex-cell strapwork, lineage in "Where this comes from") |
| `Drawing/Text.md` (three font kinds, textToShapes, atlas, every script) | Ch 8 | taught (drawText/textSize/textAlign, the three kinds, perGlyph, textToShapes warp, atlas mode, complex scripts with the EveryScript figure, textDirection, emoji as pictures, script-aware box wrapping, vertical writing and textJustify with the WritingInColumns figure, Mongolian columns with the ColumnsTheOtherWay figure, hanging punctuation with the HangingStops figure, fontsUsed/textMissingCharacters; on-path/metrics/variable axes named + pointed) |
| `Drawing/Images.md` (Image, pixels, tint) | Ch 9 | taught (loadImage/drawImage/tint, blank-image authoring, the pixel subscript both ways; resource loading named) |
| `Helpers/Data.md` (loadTable/Table, loadJSON/JSON) | Ch 9 | taught ("Numbers you didn't type: CSV and JSON" with the DataAsMaterial figure showing a file beside what it draws: typed reads by column name and nil as the honest answer for a gap, the scale taken from `numbers(_:)` so the file decides it, the quoted-cell and header-guess rules read off the figure, and JSON's reach-then-ask with a missing key answering null so a path needs no check; the format/header overrides and the Codable alternative pointed to the docs) |
| `Drawing/Geometry.md`: `Vector2`/`Vector3`, `Rectangle`, `Circle` | Ch 10, Ch 21 | taught (Vector2 arrows, +/−/scale, length/normalized/limited/distance/angle, with(x:y:) taught in Ch 10; dot/cross/lerp/rotated/projected pointed; Vector3 waits for Ch 21) |
| `Simulation/Physics.md` (Verlet + rigid World) | Ch 11 | taught (forces by hand + accumulate/divide-by-mass; World/step, particle collisions, bounds/bounce/drag, springs/rest length/stiffness, pin/place, Body/colliders/friction/density/restitution, .static, the revolute joint, grab; push/strain/soft blobs/other joints named + pointed) |
| `Simulation/Physics3D.md` (World3D, Body3D, Collider3D, compound bodies, terrain/scene colliders, joints, motors/limits, contacts, sensors, characters, vehicles, ragdolls, soft bodies, ropes, water/buoyancy, world queries, collision groups, camera grab) | Ch 21, Ch 22, Ch 23 | taught ("Things with weight" with the CrateFall figure: World3D/ground/addBody/step, withBody as the one new move vs Ch 11's translate+rotate, determinism of the replayed collapse; collider catalog with the tapered forms, `.ball` joints, and grabBody/dragGrab named; `.compound` taught with the blade-cross listing and per-part density; joint motors/limits/springs with the Windmill figure: limits-from-connect-pose, drive(at:)/drive(to:), stopMotor/friction, softenLimits, weak strength as a character knob; the `.heightfield` collider taught with the Rockslide figure closing the loop with "A landscape you grow", addStaticColliders(from:) named; contacts and sensors taught in "Asking what hit what" with the Trigger figure: world.contacts as a polled per-step list, contact.speed as the impact size, per-pair granularity, isSensor regions with entered/touching, the solid-rim-plus-sensor-hole trick, and why a sensor never sleeps where a settled pile does; queries taught in "Asking what is there" with the Sightlines figure: raycast read as line of sight (visible when the target is what the ray found), Hit3D's point/normal/distance with the same call aimed down as a ground probe, sweep as the fits-question a ray cannot answer, bodiesOverlapping as a blast radius with a shape that never existed and when a sensor is the better tool, and the three habits (sensors transparent while cloth is not, ignoring: for casting from inside yourself, asking never steps the world); characters taught in "Someone to be in there" with the Walker figure: why a character is not a body with a capsule, addCharacter + the move/jump draw() poll, withCharacter putting the origin at the feet, stepHeight/maxSlope/pushStrength met by breaking each one, velocity vs actualVelocity driving a walk cycle, and walker.body as the stand-in that makes the sensor from the previous section work on people; vehicles taught in "Something to drive" with the Joyride figure: addVehicle read as the whole machine from its wheel list, the throttle/steering/handBrake draw() poll beside the character's, withWheel as withBody's twin knowing the suspension and the spin, wheel.slip colored so a sliding tire lights up, suspensionFrequency and topSpeed as the two knobs worth breaking, and balances plus casterAngle for a two-wheeler; ragdolls taught in "Letting a figure fall" with the Ragdolls figure: addRagdoll(from:) reading a skinned Scene's skeleton, shapes fitted to the mesh rather than the bones, scene.apply(ragdoll) as apply(_:at:) run backwards, drive(toward:) turning an animation into a request with strength as the character knob, the two-scene target/drawn split, the unpowered root and the kinematic-hips puppet, and per-figure collision groups; soft bodies taught in "Cloth that finds its own shape" with the Cloth figure: addSoftBody(from:) turning any mesh into simulated particles, drawSoftBody as the one new draw call, stiffness and bend as separate axes and why a bedsheet is 1/0, the pinned: closure as the whole hanging story plus pin/unpin/move, pressure in gravities on a closed surface and inert on a sheet, applyForce as the only push that reaches one, and the no-self-collision envelope stated; a soft body as a member of the world taught in "A raft made of cloth" with the Raft figure: SoftBody3D.density as the one line that makes a sheet a raft (and a closed surface working its own out), drag biting harder on cloth than on a crate, a cloth turning up in world.contacts, why a contact names `any Colliding3D` and the cast that acts on what it found, and the settled-cloth-keeps-its-touches asymmetry; cloth a skeleton carries taught in "A cape on someone's back" with the Cape figure: skinnedTo:/carriedBy: naming which joint carries which part of a cloth, the bind pose being whatever the figure is standing in when you build it (so nothing is painted anywhere), pinned: read as held-by-whatever-holds-it, follow(_:) once a frame before step and snap(to:) for a teleport, sway/backStop/maxStretch as plain distances with maxStretch worth having on any hung cloth, and swayScale/followsSkin as the live pair; water taught in "Water, and what it holds up" with the Floating figure: world.water as one property like ground with nothing opting in, density deciding what floats and the waterline being an answer rather than a setting, Water.density as brine, linearDrag as the difference between water and a trampoline, Water.Waves plus waterMesh as one surface for drawing and riding (and why to keep roughness in it), and the ocean-not-a-pool envelope stated; collision groups taught in "Things that pass through each other" with the Sorted figure: a group as a word where a body is built plus one sentence about a pair, the rule reading both ways, naming a group not being a rule (and a typo therefore doing nothing, with collisionGroups as the check), a group still colliding with itself unless said twice, every addable thing taking one, the rule holding everywhere the pair could meet (contacts, sensors, characters, wheels), `as:` narrowing a query the way the shot itself would see it, and body.group changing while it runs; the per-body motion knobs taught in "Taking a direction away" with the Flattened figure: freedom as six things a body can do and any of them removable, `.plane()` as the way to have a flat world without giving up lights and solids, a locked direction being one the body no longer has rather than a rule it obeys (so no gravity, contact, joint, or set velocity reaches it), the named sets with `.upright` as the second one worth wanting, no tilted plane because whole world axes are what go, gravityScale as a balloon and a feather in one world, and checksPath read as what a bullet needs plus why it is off by default and nearly free; the joints beyond hinge and slider taught in "Machines out of joints" with the Machines figure: `.path` as a track threaded through points with alignment and progress, `.pulley` read the way a finger traces the rope plus the block-and-tackle ratio, `.allowing` as the joint that is just a list of freedoms (and weld/ball/hinge written out as choices from it), and the shift that a gear connects two *hinges* rather than two wheels, with `.rackAndPinion` turning a turn into a distance and the meshed-cylinders-jam gotcha; tracks taught in "Turning without steering" with the Crawler figure: `tracked: true` as the same call again, bands worked out from which side of the hull a wheel sits on, steering read as running the inside band slower (half lock stopping it, full lock reversing it into a spin on the spot) and therefore needing throttle at all, trackSpeed per band painted warm and cool so a still picture of a turn reads, wheels(on:) for laying a drawn band, and the two knobs that change meaning (`driven` as the sprocket, `grip` as a flat coefficient rather than a slip curve, which is why a track climbs what a tire spins on); saving and loading taught in "Keeping what settled" with the Kept figure: an arrangement you find rather than design and therefore cannot write down, `snapshot()`/`restore(_:)` as the in-place undo and `save(to:)`/`load(contentsOf:)` as the file that survives quitting (with the load-or-build `setup()` idiom), restoring being exact rather than approximate (pose, motion, sleep, and a joint's zero), the three-heap figure showing why re-simulating is not the same thing (a ten-millionth of a unit compounding into a different heap), the rigid-tier envelope with the higher tiers named as skipped, and the restore-replaces-your-references gotcha with order and `collider` as what a drawing loop leans on; ropes taught in "A line that knows how it is turned" with the Rope figure: a rope built from any polyline with its points as the particles, `bend` as the knob that decides whether it is rope, cable, or stem (shown as three lines differing in one number), `rope.segments` and `withSegment` as `withBody`'s twin, the interlocking-chain roll as the thing only a segment that knows its own orientation can be asked for, `maxStretch` and `iterations` for anything long, and the no-self-collision, invisible-to-queries, one-strand envelope; the swing-twist motors (`drive(toward:)`, `twist`) and the mouse-grab wiring pointed to the docs and the 3D/Physics examples) |
| `Generators/Boids.md` | Ch 12 | taught |
| `Generators/Steering.md` | Ch 12 | taught |
| `Generators/DifferentialGrowth.md` | Ch 12 | taught |
| `Generators/SpaceColonization.md` | Ch 13 | taught |
| `Generators/DiffusionLimitedAggregation.md` | Ch 13 | taught |
| `Generators/DielectricBreakdown.md` | Ch 13 | taught ("Growth by voltage: dielectric breakdown" with the two-panel VoltageChooses figure: the solved field washed behind a young discharge with the frontier dotted by the actual growth weights, the eta dial's regimes, the stepper snippet, and the three extra reads: thicknesses, branches, potential(at:)) |
| `Generators/CrackGrowth.md` | Ch 13 | taught ("Growth by collision: crack growth" with the two-panel cracked-city figure: the stop-and-recruit rule, the accumulating canvas, the marks and the one-sided wash) |
| `Generators/Meander.md` | Ch 13 | taught ("Growth by wandering: meander" with the two-panel wandering-river figure: the outside-bank rule, the downstream slide, the scars and the oxbow lakes) |
| `Generators/LSystem.md` (symbolic and parametric) | Ch 13 | taught ("When the rules need arithmetic" motivates parametric rules from what one step cannot say, walks a guarded rule left to right, explains that an unmatched symbol is how growth stops, and shows the tapered form through marks, with a three-panel figure) |
| `Generators/WaveFunctionCollapse.md` | Ch 13 | taught |
| `Generators/FlowField.md` (streamlines, advection) | Ch 14 | taught |
| `Drawing/Attractors.md` (strange attractors, chaotic maps, `AttractorFlow`) | Ch 18, Ch 21 | taught (the 2D maps as density plates in Ch 14, the gumowskiMira/ikeda/hopalong trio named there with their temperaments; the GPU tier in Ch 21's "A million riding the same field" with a one-orbit-versus-a-million figure: a velocity field read as which-way-is-this-point-moving, the four-line sketch, why it names no per-system numbers because `center`/`extent`/pace are measured from a CPU orbit at build, color-is-speed-brightness-is-crowd and why the ramp holds its brightness level, and the hair of jitter that turns one trajectory into a million) |
| `Generators/Bifurcation.md` (`IteratedMap`: the logistic/sine/tent/gauss families, bifurcation diagrams, cobwebs, Lyapunov exponents) | Ch 18 | taught ("One dial away from chaos" with the labeled diagram figure, the diagram read as a story, and the cobweb/exponent companions pointed at the example) |
| `Drawing/Geometry.md`: `Path`, `Shape`, booleans, offset | Ch 15 | taught |
| `Drawing/Voronoi.md` (+ Delaunay, Lloyd) | Ch 15 | taught |
| `Generators/BlueNoise.md` | Ch 15 | taught |
| `Generators/LowDiscrepancy.md` (`haltonPoints`, `sobolPoints`) | Ch 15 | taught (beside blue noise, with a growth figure: not random at all, the incremental property blue noise lacks, why it never touches the sketch rng, and the scalar `halton`) |
| `Generators/Stippling.md` (`stipple`, weighted-Voronoi) | Ch 9 | taught ("A picture as one line" explains the settling method in plain words before the Voronoi structure is named in Ch 15, plus `count` and the `cutoff` rounding) |
| `Generators/SingleLine.md` (`singleLine`, the TSP tour) | Ch 9 | taught (the tour beside the tree in a three-panel figure, both the `through:` and `of:points:` forms) |
| `Generators/SpanningTree.md` (`spanningTree`, the MST rendering) | Ch 9 | taught (same figure and section; the minimal-chain decomposition explained as pen lifts) |
| `Generators/StringArt.md` (`StringArt`, the greedy thread winding) | Ch 9 | taught ("A picture wound from thread" with a three-panel figure: the greedy chord choice and its ink pay-down, the accumulating stepper, the bold-masses guidance, and the sequence as a hand-winding recipe) |
| `Generators/Percolation.md` (`Percolation`, site-percolation clusters) | Ch 4 | taught ("When chance acts as a crowd" with the three-panel threshold figure: the phase transition near 0.5927, largest-first clusters, the spanning question, and the outline loops; `criticalProbability` as the no-magic-number knob) |
| `Generators/Isolines.md` (`isolines`, marching-squares level curves) | Ch 14 | taught ("Where the field equals something" with a three-panel figure: scalar versus direction fields, marching squares explained by the sixteen corner patterns, `resolution`, why the stacked-levels form costs almost nothing extra, open versus closed contours, and the image form) |
| `Generators/Hulls.md` (`concaveHull` / `alphaShape`) | Ch 15 | taught ("What shape are these points?" with a three-panel figure over one scatter: convex vs concave vs alpha, the `concavity` and `alpha` ranges that read well, and which to reach for by what happens next) |
| `Generators/MedialAxis.md` (`medialAxis`, skeletons with radii) | Ch 15 | taught ("The skeleton inside: the medial axis" with a two-panel figure: branches and the inscribed disks, `spacing`/`prune` explained, `isClosed` rings, and what the radii are good for) |
| `Generators/StraightSkeleton.md` (`straightSkeleton`, mitered insets) | Ch 15 | taught ("The straight skeleton" with a two-panel figure: corner arcs and ridges, then the inset ladder splitting at the waist; `inset(by:)` vs `offset(by:)`, `maxInset`, faces as panels, the simplify-first habit) |
| `Generators/Marbling.md` (`Marbling`, closed-form paper marbling) | Ch 15 | taught ("Ink on water" with a four-panel figure: the bull's-eye of drops, the area-preserving push, the shared falloff law, all four raking tools, `add`, and the concentric-swirl no-op) |
| `Generators/Watercolor.md` (`Watercolor` / `drawWatercolor`, layered deformation) | Ch 15 | taught ("Pigment from a polygon" with a three-panel figure: the starting polygon, one 4% layer, forty stacked; layers-vs-opacity, `variance`, the paint-once rule, and interleaving two pigments) |
| `Generators/Chladni.md` (`chladni` field + `.chladni` generator, mode-by-pitch audio join) | Ch 17 | taught ("Standing waves: Chladni figures" with a six-mode figure: the closed form and plate coordinates, sand settling at the zero crossing, the m == n degeneracy, fractional modes for morphing, both generator styles, and the Ch 26 audio join pointed) |
| `Generators/Isosurface.md` (`isosurface`, `Metaballs`, marching cubes) | Ch 24 | taught ("The other way out: field to mesh" after the sphere-tracing section, with a two-panel figure of one coarse metaball chain shaded and wireframed: a field becoming an ordinary `Mesh` rather than pixels, `radius` as the size a ball reads at alone, why summed fields make blobs fuse and neck, the `level` and negative-`strength` knobs, why a coarse mesh still reads smooth (normals from the field, not the facets), the above-the-level solidity convention that makes a distance function need a minus sign, and the pay-by-the-pixel versus pay-by-the-volume choice) |
| `Generators/Terrain.md` (`Heightfield`, diamond-square, hydraulic + thermal erosion, terrain meshes) | Ch 21 | taught ("A landscape you grow" with a three-stage erosion figure and a lit mesh: the closure and diamond-square builders, `roughness`, why rain is what makes noise read as land, thermal talus settling, the three read-outs, and the erode-in-setup rule) |
| `Generators/SubdivisionSurfaces.md` (`mesh.subdivided`, Catmull-Clark + Loop control cages) | Ch 21 | taught ("Smooth from a cage" with a cage/level-1/level-2 star figure: the control-cage idea, easing toward the averages so the surface sits inside the cage and points round fastest, the chunkier-cage fix, the no-preparation welding and face recovery, open rims kept, the `.loop` scheme pointed at the reference, and the refine-in-setup rule) |
| `Generators/MeshGrowth.md` (`MeshGrowth`, `MeshReactionDiffusion`, differential growth over a surface) | Ch 21 | taught ("A surface that outgrows itself" with a three-panel figure of a seed sphere, even growth, and growth restricted to the equator: the one-sentence rule, why nothing can inflate so every driver folds, the driver as a choice of *where* the folds go, a plain sphere becoming a brain, the leaf-margin case, `edgeLength` and `stiffness` as the fold-size knobs, the `.chemical` coral pointed at the reference, and the step-and-watch rhythm with `maxVertices` as the ceiling) |
| `Generators/SurfaceReconstruction.md` (`reconstructSurface`, `particleSurface`, points back to a mesh) | Ch 25 | taught ("From the cloud to a surface" with the staged corner sweep rebuilt solid: the plane fit, the `.robust` fitting and its frayed-edge reading, the camera path as orientation, the torn-rim honesty rule and its sweep-coverage reading, the color carry and its multiply-fill contract, the ordinary-`Mesh` payoff, and the `particleSurface` sibling pointed at the reference page and the side-by-side example) |
| `.ripples` wave-equation Sim (`Drawing/Effects.md`) | Ch 19 | taught ("A pool you can drop things into: ripples" with a figure: marks add height rather than set it, the soft-dab and brief-drop rules and why, `damping`, and the raw state as a debug view to shade with `.relight`) |
| Modulated reaction-diffusion regimes (`Drawing/Effects.md`: `.reactionDiffusion(feed:kill:toFeed:toKill:)` + `SimField.modulation`) | Ch 19 | shown (a paragraph in "Two chemicals: reaction-diffusion" after the feed/kill map: the layer's brightness picking the map spot per texel, one simulation so the patterns grow into each other rather than meeting at a mask edge, and the `Vision/TuringMirror` camera-matte example as the worked case) |
| `Simulation/Watercolor.md` (`Sim.watercolor`, `WatercolorField`, `WatercolorPigment`, the wet-paint wash) | Ch 19 | taught ("Paint that behaves: watercolor" with the scripted-painting figure: the pigment-channels-plus-water-alpha mapping and the noStroke rule, edge darkening as physics, dry() glazing vs blot() and the held-water backrun, dry-brush/grain/paperSeed, and inventing pigments from two swatches) |
| `Drawing/GlyphMosaic.md` (`drawGlyphMosaic`, measured glyph ramps) | Ch 9 | taught ("A picture as marks" with a two-panel figure: measured ink per font, the curated sets, `glyphScale` gutters, the data form, and the inverted-polarity trap it shares with halftone) |
| `Drawing/PixelSorting.md` (`Image.pixelSorted`) | Ch 9 | taught ("Sorting the pixels" with a before/after figure: the threshold as the whole technique, the keys, and the needs-texture and reversed-on-a-gradient caveats) |
| `Drawing/Halftone.md` (`drawHalftone`, area-exact dot screens) | Ch 9 | taught (same section and figure: `pitch`/`angle`, area-exact coverage, real circles for the plotter, and continuous tone versus the mosaic's steps) |
| `.melt` design filter (`Drawing/Effects.md`, the luminance melt) | Ch 16 | taught (a before/after figure beside the design filters, the shared displacement vector that makes it read as dyed rather than smeared, the brightness mix-back, the dial-back knobs, and the never-perfectly-loops caveat) |
| `Video/SlitScan.md` (`SlitScan` frame history) | Ch 27 | taught ("Every pixel its own moment: SlitScan" with a synthetic-clip figure: the rolling history, the delay closure and what 0 and 1 mean, alternative delay maps and the image form, the memory cost, and the `Ollin.SlitScan` shadowing note) |
| `Generators/Walks.md` (`randomWalk`, `levyFlight`, `selfAvoidingWalk`) | Ch 4 | taught ("Three walks, three rules" after the hand-rolled walk, with a same-seed three-panel figure: why an even-stepped walk pools, the power-law step lengths and the foraging connection, the `minStep` divergence, and getting stuck as the point of the self-avoiding one) |
| `Generators/Packing.md` (circles) | Ch 15 | taught |
| `Generators/ShapePacking.md` | Ch 15 | taught ("Packing shapes, not circles" with a two-panel figure of one packing: why a spiky outline is the interesting case, the bounding circles drawn overlapping as proof the fit used outlines, what `padding`/`rotation`/`scale` change in character, and `ContinuousPacking` held open against `noClear()`) |
| Hatching (`Output/Export.md`) | Ch 15 | taught |
| Perfect-loop export (`--export-loop`, `loopDuration`; `Output/Export.md`) | Ch 3 | taught |
| `Drawing/Epicycles.md` (Fourier epicycles) | Ch 15 | taught ("Circles all the way down" with a rising-term figure: the largest-first prefix, exactness at full terms, under-terming as a smooth simplifier, and the construction versus the path) |
| `Drawing/Curves.md` (classic curves: phyllotaxis, lissajous, rose, superellipse, supershape, hypotrochoid/epitrochoid, `guilloche`+`Rosette`, `Harmonograph`, Chaikin `smoothed`) | Ch 15 | taught ("Curves you can write down" with a nine-panel figure; the superellipse/supershape pair and the guilloche added with their own paragraphs, snippet lines, and the integer-m closure rule: the golden angle and why it can't line up, the frequency ratio, petal parity, whole-number gears, damped pendulums, corner cutting, and the fit-by-points rule) |
| `Drawing/Morphing.md` (shape morphing: `ShapeMorph`, `Tweenable` geometry) | Ch 15 | taught ("One shape becoming another" with a five-step figure: build-once-read-cheap, exact originals at the ends, unmatched contours growing from their own center, and timing living outside the read) |
| `Drawing/SVG.md` (SVG import: `loadSVG`, `drawSVG`) | Ch 15 | taught |
| `Drawing/Effects.md` (targets, filters, compose, combine) | Ch 16 | taught (targets/filters/generators/compose/feedback taught; combine + aside pointed) |
| Blend modes (`Drawing/Drawing.md`) | Ch 16 | taught |
| Feedback (`Drawing/Effects.md`) | Ch 16 | taught |
| `Drawing/Accumulation.md` (noClear) | Ch 16; used in Ch 19 | taught |
| `Drawing/HDR.md` (toneMap) | Ch 16 | taught |
| `Drawing/ColorOutput.md` (colorOutput, `Color(displayP3:)`, HDR10 export, gain-map HEIC stills) | Ch 16 | shown ("Genuinely brighter: HDR output" plus the `Rendering/ColorOutput` example, and the `--export lamp.heic` line that keeps the highlights in a gain map; no figure by nature, since neither the wider gamut nor the brightness above white survives an SDR page, and the section says so) |
| Design patterns + design filters (`Drawing/Effects.md`) | Ch 16 | taught ("The design family" with a six-tile two-row figure: the generator set and the phase-is-a-number rule that keeps exports reproducible, then the filters split into the alpha-shape readers (draw a shape, then filter it) and the picture transformers, plus the tuned-for-full-canvas caveat and the `edges` knob) |
| Relight, two-tone dither, warp centers (`Drawing/Effects.md`) | Ch 16 | taught ("Filters that read the layer as something else" with a three-panel figure: brightness as height and the five finishes, brightness as tone with `pixelSize` grain, and `center:` in 0…1 layer coordinates as the knob that makes a warp a composition) |
| Pattern fields (`Drawing/Effects.md`) | Ch 17 | taught ("The pattern fields" with a six-field catalog plus a hand-rolled gyroid beside the built-in: closed form as the defining property and its three consequences, all six named, reading a 3D field at a moving slice, and the sRGB palette convention) |
| Escape-time fractals (`Drawing/Effects.md`) | Ch 18 | taught (escape time as the step count that becomes the color, the marked-c figure showing a Julia set as a portrait of one Mandelbrot point and why edge points are richest, smooth banding with `cycles`/`phase`, the zoom-needs-iterations trap, and orbit traps as the second question asked of the same loop, with the three-panel same-Julia figure and the turning cross) |
| `Generators/Fractals.md` (`IFS`, `FractalFlame`, `Buddhabrot`, inversion and Kleinian limit sets, Schottky circle orbits) | Ch 18 | taught ("The same fern, played as a game: the chaos game" derives the chaos game from the chapter's own L-system fern with a condensing figure and explains why contraction forces the attractor; "Four more games worth knowing" covers flames, the Buddhabrot (the escapers' density plate, with the gray-vs-false-color two-panel figure), inversion, and Kleinian with a three-panel figure, plus `fitted` and the Contour return; "Circles that pair off: Schottky" builds the Schottky lace from four paired circles with a three-panel nesting figure, explains why a touching pair keeps the picture full where a separated one empties it, and bridges to the trace recipe with a four-member family figure around the gasket) |
| Depth-of-field `.defocus`, SSAO, SSR (`Drawing/Effects.md`) | Ch 21 | taught ("What the depth buffer is for" with a three-panel figure: depth as an ordinary layer, occlusion as what stops objects floating, focus/range read against near/far, SSR's cannot-see-the-back limit, and quality tiers resolving on export) |
| `Shaders/Shaders.md` (user shaders) | Ch 17 | taught |
| `Shaders/Visuals.md` (Visual chains) | Ch 17 | taught |
| `Shaders/ShaderLibrary.md` (helper reference) | Ch 17 | taught (the named families, plus what splicing means: one source compiled into framework shaders, user shaders, and compute kernels, so a field behaves identically wherever you move it, and the `using:` opt-out) |
| `Shaders/Compute.md` (kernels, Particles, Simulation) | Ch 20 | taught |
| Sim fields: Game of Life, Gray-Scott, fluid (`Drawing/Effects.md`) | Ch 19 | taught |
| Multi-scale Turing: `.multiScaleTuring`, `TuringScale` (`Drawing/Effects.md`) | Ch 19 | taught ("The same rule at many sizes: multi-scale Turing" with a one-scale-versus-five figure: the activator/inhibitor comparison built up from the single-scale rule, least-disagreement selection as "the scale with the least to say here", the no-`withField` sketch and reading-keeps-it-stepping, `.relight` as the look the algorithm is known for, the equal-amounts and `variationRadius` traps and why each one collapses the picture, and `.rosette(n)`) |
| Artificial life: `ParticleLife`, `PPS`, `Physarum`, `SpatialHash` (`Simulation/ArtificialLife.md`) | Ch 20 | taught ("Crowds that organize themselves: Physarum" with a three-panel figure: the neighbor-search problem `SpatialHash` solves, all three systems with their rules and calls, and the no-frame-exact-reproducibility caveat; then "Matter that decides what shape to be: Particle Lenia" with a three-panel figure for `ParticleLenia`: an energy landscape instead of a force law, `muG` and `sigmaG` as the two numbers that decide what kind of thing it is, growth and repulsion as the terms that pull against each other on the same quantity, the membrane as where the population runs out rather than an authored feature, and the kernel weight as derived rather than named; and "A rule that spreads by winning arguments: swarm chemistry" with a three-ages figure for `SwarmChemistry`: a recipe per particle, transmission on contact with a competition function deciding direction, mutation as a chance per contact rather than per generation and what setting it too high looks like, and colour as the recipe itself) |
| `Generators/AntColony.md` | Ch 20 | taught ("A search you can watch: ant colony optimization" with a three-panel figure of one seeded search at iterations 1/8/60: the pheromone-with-a-task frame, the stepper-and-trails listing, evaporation and elitism as the search's temperament, the reproducible-and-monotone contrast with the GPU crowds) |
| Steering at scale: `Swarm` (`Simulation/Swarm.md`) | Ch 20 | taught ("The flock, a thousand times bigger" with a three-panel trails figure: the eight behaviors as weights over Chapter 12's rules, the three coupled numbers (neighbors seen, separation radius against spacing, turning circle against perception radius), `minSpeed` as the fix for a swarm that sets solid, and why a still frame needs trails rather than dots) |
| Evolution: `Evolution` + `Population` (`Simulation/Evolution.md`) | Ch 20 | taught ("Letting the sketch find it: evolution" with a three-generation figure of the same search: a genome read as a plan for a journey and the flight as what it turns out to be worth, the route as the one thing you leave out, why a tournament never adds anything up and so suits a GPU where the wheel of fortune does not, the pace derived from the distance between the two points, the opening generation as arcs rather than a random walk and why that decides whether selection has anything to tell apart, and mutation as what stops a run narrowing; then "Sixteen things and no opinion about them: interactive evolution" for the interactive half: a genome as numbers that know nothing about what they mean, the high mutation rate as arithmetic about how many candidates a person can judge, keeping the ones you picked, and why judging by eye reaches what a score cannot) |
| Fluids & soft bodies: `ParticleFluid`, `SoftBodies` (`Simulation/Fluids.md`) | Ch 20 | taught ("Liquids and jellies: SPH and soft bodies" with a two-panel figure: SPH density-to-pressure explained, `stiffness`/`nearStiffness`/`gravity`, shape matching and `squish`, and grabbing with `pull`) |
| Cellular automata: Lenia sim, `elementaryCA`/`totalisticCA`, `Turmite` (`Generators/CellularAutomata.md`) | Ch 19 | taught ("More ways to be an automaton": elementary rules as a numbered rule byte with rules 30/90/110, `totalisticCA` named, `Turmite`/Langton's ant held and stepped, and Lenia with its knobs and the dense-seed rule; two figures) |
| `.sandpile` Abelian sandpile Sim (`Drawing/Effects.md`) | Ch 19 | taught ("A pile of sand" with a relaxed-pile figure: the four-grain toppling rule, order-independence as what licenses the GPU's parallel topplings, the drop-a-mountain protocol behind the classic figure versus the molten held torrent, the quarters state encoding and the one-color-per-count ramp, self-organized criticality as the discovery beyond the picture, and `topplings` as the pacing dial) |
| State automata: `.cyclic`, `.excitable`, `.briansBrain`, `.hodgepodge` Sims (`Drawing/Effects.md`) | Ch 19 | taught ("The medium that has to rest: excitable media" with a four-panel figure: fire-then-rest as what makes a wave, draw-to-spark and the wipe recipe for a spiral pair, the cyclic loop with no rest state and its four acts, the exactly-two birth rule and why a solid blob dies, and the hodgepodge machine's `g` dial with the Belousov-Zhabotinsky aside) |
| Articulated & chaotic motion: `IKChain`, `DoublePendulum`, `NBody` (`Simulation/Motion.md`) | Ch 11 | taught (all three toured in "Systems that come assembled" with a three-panel figure: `reach`/`drag` and the solver/`maxBend` knobs, the pendulum's determinism-plus-sensitivity, and n-body's `theta`/`softening` and the seeded factories) |
| Force-directed layout: `ForceLayout`, `drawGraph` (`Generators/ForceLayout.md`) | Ch 11 | taught (the graph-layout section with a three-panel settling figure: repulsion between all nodes plus springs on edges, the temperature cap and linear cooling, the reheat/pin/drag interaction vocabulary, `idealDistance` as the size dial, and seeded starts for reproducible layouts; weights and gravity pointed) |
| Damped spring: `DampedSpring`, `@Sprung` (`Helpers/Animation.md`) | Ch 3 | taught (duration/bounce, the moving-target case that separates it from `@Eased`, and `kick`; `velocity` and `Vector2` springs pointed) |
| `3D/3D.md` (camera, primitives, meshes, lights, materials) | Ch 21 | taught |
| `3D/Scenes.md` (loadScene, drawScene, named nodes, authored cameras/lights incl. USD's UsdLux kinds, authored animations via `apply(_:at:)` from glTF and USD timeSamples, skins and morph targets posed by drawScene from both formats incl. UsdSkel, the USD structure read) | Ch 21 | shown ("A mesh from a file" teaches the loadScene shape: the file's camera and lights as ordinary values, `scene["name"]` driving one node, `apply(_:at:)` playing an authored animation on the sketch clock, the deforming tier (skins, morph `weights` as a hand-drivable node property), the USD leg in further reading; worked examples `3D/Geometry/LoadedScene` + `AnimatedScene` + `SkinnedScene` + `USDScene` + `USDAnimatedScene` + `USDSkinnedScene`) |
| `3D/Camera.md` (cameraControl, moves, showcase, views) | Ch 21 | taught |
| `3D/Strands.md` (`StrandField`, `drawStrands`) | Ch 21 | taught ("Grass that was never built" with the GrassMeadow figure: why a blade's bend defeats instancing, geometry born inside the draw from hashes, the camera skipping tiles and grading distant detail, blades on the shared lit path receiving the boulders' shadows, the zero-buffer zero-CPU cost story, the receive-but-never-cast and nothing-to-export fine print pointed at the reference) |
| `3D/Instancing.md` (`drawMesh(_:instances:)`, `MeshInstance`, GPU-resident placements, `MeshField`) | Ch 21 | taught ("Ten thousand of the same thing" with the InstancedField figure: the per-copy loop's cost and the one-call escape, MeshInstance as place/turn/size plus tint, rebuild-the-list-per-frame as the normal animation idiom, copies shade and shadow like solids, the Batch/particle-flow kinship, the A/B example knob, the compute-kernel placement buffer pointed at the reference; "A world the camera trims" with the FieldWorld figure: MeshField as the retained world drawn by one call, the GPU cull that can never change the picture, colors baked at place, uncculled shadow casters, the culling knob example) |
| `3D/Combining.md` (what stacks with what) | Ch 21/18 | taught (the concept in Ch 21's bearings section: several kinds of 3D thing reach the screen by different routes, so finishes apply unevenly, with the table pointed for lookup) |
| Area lights: `rectLight`/`diskLight`/`tubeLight` (`3D/3D.md`) | Ch 21 | taught ("A light with a body" with a breathing-softbox GIF: the three shapes, intensity as the surface's own glow, a flux-normalized size change moving highlight, shading wrap, and shadow softness together, the panel-extent cast shadows with `shadowSoftness` as a scale on the true size, why a tube never casts, and the prop-behind-the-emitting-plane occluder note) |
| Light shaping: IES `profile:` + `LightCookie` (`3D/3D.md`) | Ch 21 | taught ("The shape of the throw" with a two-light GIF: what an IES file is and why fixtures have signatures, the ring pool read from a dozen numbers, normalization and the axis/aim rule, the cookie as a projected gobo with edges at the outer cone, one `roll` for both, the measured-range-ends-dark habit, and the made-once-in-setup rule) |
| Atmosphere: `fog` + `aerialPerspective` + `volumetricLight` (`3D/Atmosphere.md`) | Ch 21 | taught ("Air you can see" with a gobo-beam still and a ridgeline-stack still: fog as the cheapest depth cue, height falloff as pooled mist, aerial perspective as fog's wavelength-split outdoor cousin following the sky's sun, the beam march making cones/cookies/profiles/shadow shafts visible in flight, anisotropy as the forward flare, beams-only versus beams-in-fog, and the quality dial's export lift) |
| Matcap (`3D/3D.md`) | Ch 21 | taught ("Shading from a picture: matcaps": sampling by facing direction, why baked lighting is both the appeal and the limit, when a material is the better choice, the 26 built-ins by family, `Matcap.shaded`, and the `fill` tint) |
| Anisotropic specular: `Material.anisotropy` + `.brushedMetal` (`3D/3D.md#anisotropy`) | Ch 24 | taught ("A streak instead of a dot: anisotropy" with the BrushedRing figure: the streak as stretched roughness, the frying-pan/laptop-lid frame, the rotation spinning it, the smeared environment reflection, the preset on a ring following the machining, and the mirror-has-nothing-to-stretch edge) |
| PBR materials + IBL environments + procedural sky + cloudscapes (`3D/3D.md`) | Ch 24 | taught ("The finish" in two parts with a roughness run, a two-image environment pair, and a cloud-deck still: metal versus dielectric and roughness, why a mirror with nothing to reflect goes dark (so PBR and environments are one topic), the bundled eight versus the downloading twelve, `.sky` as the zero-asset option, `.clouds` baking one weather into backdrop/light/reflections with the deterministic wind phase, and `lightingOnly`/`backgroundBlur`) |
| Live camera environment: `Environment.feed` (`3D/3D.md#live-environment`) | Ch 24 | taught ("The room as the light: Environment.feed" with the LiveRoom figure: the feed's frame as the surroundings, the mirror-fill of the unseen half as plausible-not-true, the frame drawn yourself so picture and light stay one world, why the feed never draws as its own backdrop, any `VideoFeed` working the same, and the neutral-sky stand-in before the first frame; the figure fakes the webcam with one authored picture so the guide reproduces) |
| Shadows: 2D map, PCSS, ray-traced point (`3D/3D.md`) | Ch 21 | taught ("Shadows, and what they tell you" with a rising-height figure: shadows as the only cue for height, contact hardening and `shadowSoftness`, the four practical notes (a receiver is needed, opt-in and per-frame, one caster chosen for you, nothing to aim), the three caster routes as an explanation of cost, and `shadowQuality` for graininess) |
| Contact shadows: `contactShadows()` (`3D/3D.md#contact-shadows`) | Ch 21 | taught (the closing passage of "Shadows, and what they tell you" with the Seated figure: where even a good map falls short (resolution + bias opening the last sliver of contact), the short screen-space march toward the caster, three resting solids seamed against one honestly hovering control, the world-units `length` dial with its scene-scale default, and the screen-space envelope stated plainly) |
| Ray-traced reflections (`3D/3D.md`) | Ch 24 | taught ("Mirrors that see off screen: ray-traced reflections": traced rays versus searching the finished picture, integration into the environment rather than post-process, the three conditions, no-op where unavailable so the call can stay in, and the settle-then-clean behavior) |
| Global illumination: `globalIllumination` probe field (`3D/3D.md#global-illumination`) | Ch 24 | taught ("Light that bounces: global illumination" with the BouncedLight Cornell figure: direct light only explains half a picture, everything outside the pool as bounce (the ceiling lit from below, the stained walls, the filled shadow side), the re-traced probe grid in one paragraph, composing with `castShadows()` (no second-hop leaks) and environments (sky with occlusion), diffuse-versus-`rayTracedReflections()` as the two halves, the physical-1 intensity dial, settle-live/converge-on-export, the no-op portability rule, bounce reaching mirror interiors and layers, and `globalIlluminationQuality` for graininess) |
| Caustics: `caustics` photon-traced focused light (`3D/Caustics.md`) | Ch 24 | shown ("The light the glass takes, given back: caustics": the shadow-versus-caustic line, the sunlit-table snippet with `castShadows()`, automatic casting from the materials, intensity/dispersion/quality knobs, the no-op portability rule, and the example pointer; a figure would lift it to taught) |
| Glass: `Material.glass` transmission + refraction (`3D/3D.md#glass`) | Ch 24 | taught ("Glass" with the three-sphere figure: transmission versus fill alpha, thickness as the lens-versus-window distinction, attenuation as Beer-Lambert under a friendlier name, roughness as frost and ior as bend, the environment requirement, the one `rayTracedReflections()` call upgrading mirrors and glass together, the soap-film mode (`iridescenceFlow`) composed onto thin glass for a bubble, and the two honest edges: solid shadows and opaque-in-mirrors) |
| Temporal anti-aliasing: `temporalAntialiasing` + `withMotion` (`3D/3D.md#temporal-antialiasing`) | Ch 24 | shown ("Edges that settle: temporal anti-aliasing": the render-different-estimates-and-average trick named as what reflections and probes already do, applied to every 3D edge; fixed-position MSAA's staircase versus the jitter average, camera-following history, `withMotion` handing a world-space mover's exact motion to the average, the untouched 2D path, the average-within-the-frame export rule, the any-GPU note, and the TemporalAA example's mid-motion toggles; a figure is hard to earn honestly since the win is temporal, a still shows only the staircase melting, so the example carries the demonstration) |
| Motion blur: `motionBlur(shutter:)` (`3D/3D.md#motion-blur`) | Ch 24 | taught ("The streak a shutter leaves: motion blur" with the MotionStreak figure: a frame as an instant versus a film frame's open shutter, three spheres at three speeds telling their own pace in one still while the columns stay sharp, camera motion read from depth with no declaration versus `withMotion` for a thing moving on its own (one wrapper serving TAA and the blur), the shutter as the photographic dial (0.5 film standard, low staccato, past 1 overdrive), the between-frames read that makes exports deterministic and the first frame honestly sharp, and the still-by-design backdrop) |
| Temporal upscaling: `temporalUpscaling(_:)` (`3D/3D.md#temporal-upscaling`) | Ch 24 | shown ("Rendering fewer pixels: temporal upscaling": per-pixel cost as the chapter's common bill, the render-small-reconstruct-full trade over the same jittered history as "Edges that settle: temporal anti-aliasing", the tier as the render fraction, one accumulation so it replaces temporal AA and reads the same `withMotion` declarations, the Apple-silicon gate, and the exports-never-upscale rule that makes it purely a live-window trade; the win is temporal and perf-shaped, so like TAA a still can't show it honestly and the Upscaling example's FPS readout carries the demonstration) |
| Clearcoat + sheen layered lobes (`3D/3D.md#clearcoat-sheen`) | Ch 24 | taught ("Paint and cloth: clearcoat and sheen" with the two-pair figure: finishes built by layering because that's how the real things are made, the coat as a second reflection no single roughness can make (car paint, `.lacquer`), the base dimming by what the film reflects so layering never invents brightness, sheen as fibers catching light edge-on (`.satin` versus `.felt`, `sheenColor` for two-tone velvet), sparkle composed on `.carPaint` for metal-flake, and the honest mirror edge shared with glass) |
| Subsurface scattering: `Material.scattering` + `.skin`/`.marble` (`3D/3D.md#subsurface-scattering`) | Ch 24 | taught ("Skin, wax, and stone: subsurface scattering" with the two-pair figure: light entering the body and re-emerging (the flashlight-through-fingers image), the warm carried light past the terminator versus the bare twin's cut to black, `scatteringColor` as per-channel reach (red farthest through flesh; near-equal = marble; green-dominant = jade), the world-units radius with the 1%-of-a-head rule and the too-big-slides-toward-wax dial, and the two honest edges: the solid-mesh main-canvas envelope, and how it differs from the stylized `subsurface` back-glow, which still layers on top; plus the transmittance half with the Translucency figure: `castShadows()` as the thickness gauge, the thin/deep/round backlit read, and the casting-light-only edge) |
| Wireframe, textured meshes, mesh loading (`3D/3D.md`) | Ch 21 | taught (loading with the `normalized(scale:)` habit, then a three-panel figure for the other two dressings: `wireframe()` as both an inspection tool and a look (and why it does not light), `textured(_:)` with the pole pinch a checker reveals, and all three as stack state in one frame) |
| Normal maps: `Mesh.normalMapped` + tangents (`3D/3D.md#normal-maps`) | Ch 21 | taught ("Relief from a picture: normal maps" with the SurfaceRelief figure: a texture changes color where a normal map changes how light lands, the perfect-circle silhouette as both the tell and the trade (relief costs a texture sample, not triangles), `normalMapped(_:scale:)` with the tangent basis set up quietly and named as the standard bakers target, maps authored from a height function's slopes (the NormalMaps example), and the green-up convention with the flipped-green fix for a map that lights upside down) |
| Surface maps: `Mesh.surfaceMapped` metallic-roughness/occlusion/emissive (`3D/3D.md#surface-maps`) | Ch 21 | taught ("What the surface is, per texel" with the SurfaceMaps figure: the map set as changing what the surface *is* rather than how light lands, the ORM packing and the multiply-the-finish rule (identity factors show a map as authored), occlusion as baked crevice shadow that dims only steady light, emissive as the surface's own radiance (works unlit, fogs with distance, lights neighbors only under GI), and the glTF/USD round trip; the SurfaceMaps example is the worked version) |
| Height maps: `Mesh.parallaxMapped` (parallax occlusion) + `Mesh.displaced(by:scale:)` (`3D/3D.md#height-maps`) | Ch 21 | taught ("Depth from a picture: height maps" with the HeightRelief figure: one crater map read two ways, the parallax march as shading-only depth (the perfect-circle silhouette as the honest envelope, shadows and mirrors keeping the geometric truth), displacement as the geometry read (setup-shaped, mesh resolution decides the detail), the shared white-at-the-surface datum that lets one map feed both calls, and the USD-yes/glTF-no file story; the Parallax example is the worked version with the depth on a knob) |
| Triplanar projection: `Mesh.triplanarTextured(_:normal:scale:)` (`3D/3D.md#triplanar`) | Ch 21 | taught ("A picture from three sides: triplanar" with the TriplanarSkin figure: the no-uv problem the grown/subdivided surfaces just posed, the three-projectors picture with the normal-blend handoff, the world anchor cutting both ways (the cairn's unbroken pattern vs a traveling body wanting uvs), a normal map riding the projection with no tangent basis, and the base-plus-normal envelope; the Triplanar example is the worked version with tile size and relief on knobs) |
| Detail maps: `Mesh.detailMapped(_:normal:scale:strength:)` (`3D/3D.md#detail-maps`) | Ch 21 | taught ("Texture that survives a close look" with the SurfaceGrain figure: every texture has a budget and the close look outspends it, the finer pair tiled across the base, the 128-gray neutral for the color multiply, the detail normal reoriented onto the base relief rather than replacing it, strength 0 as the honest off switch, and the no-mips shimmer caution; the Detail example is the worked version with tile count and strength on knobs under a swaying close camera) |
| Decals: `Decal` + `decal(_:at:...)` (`3D/3D.md#decals`) | Ch 21 | taught ("A picture stamped onto the scene: decals" with the Stamped figure: a sticker that does not care about meshes, the projection box placed per frame like a light so a moving stamp conforms across floor and crates, transparency and call-order compositing, the edge-on fade as the failure it prevents, and paint-takes-the-surface's-finish; the Decals example is the worked version with a sliding roundel, a rolled tag, and a see-through ring on knobs) |
| Point clouds (`3D/3D.md`) | Ch 25 | taught |
| `Drawing/Combinators.md` (SDF 2D + SDF3D raymarching) | Ch 24 | taught |
| `3D/DepthCompositing.md` | Ch 25 | taught ("Flat drawing that knows where it is" with a three-pillar figure: `depth(at:)` sets depth alone, `project` returns nil behind the camera, `withBillboard` does both and moves the origin, why a 2D mark keeps its canvas size, and `depth(_:)` for a feed) |
| `3D/RGBD.md` | Ch 25 | taught |
| `3D/Record3D.md` | Ch 25 | taught |
| `3D/Phone.md` (capture app streams, world fusion, drift correction, the room mesh, the flat surfaces, the room's light, the hands, the eyes and the gaze, the text in view) | Ch 25 | taught (streams + fusion via SweepFuse; `add(_:correcting:)`, the carried `correction`, the held-back frame and loop closure in "When the camera loses its place", DriftFixed figure; `ScanGraph`, keyframes, matches and their limits in "Coming back to where you started", LoopClosed figure; the room mesh in "A surface the phone already built" with the RoomAsSurface figure: blocks and the `sceneMeshVersion` rebuild gate, `PhoneSurface` per triangle across `mesh`/`mesh(colored:)`/`mesh(of:)`, `unclassified` and `foundSurfaces`, points vs surface; the flat surfaces and the light in "Somewhere to stand, and the light in the room" with the RoomAsPlanes figure: `planes.floor` vs `largest` by real area, the outline as `mesh`/`outline`, no LiDAR needed, `latestLight.ambient` into `ambientLight`, and why only Face mode knows a direction; the body's richer half in "A pose you can dress in solids" with the BodyAsFigure figure: joint orientations through `modelTransform(_:)` into `transform(_:)`, solid bones via `drawCapsule(from:to:)`, `isJointTracked` tinting, `scaleFactor`, and `worldTransform` standing the skeleton in the room; the hands in "A hand you can reach in with" with the HandsAsSkeletons figure: 21 joints as capsule bones, chirality picking the color, the LiDAR lift into world space, `pinchDistance` as the gesture staple, and the flat 2D fallback via `point(_:in:)`; the eyes and the gaze in "Where a look lands" with the GazeAsBeams figure: `worldLookAtPoint` as the convergence point, the per-eye poses, the head/gaze split, the blink pairing, the shared mask UVs; the text in view in "The words on the wall" with the WordsInPlace figure: `worldTransform` standing an underline on the sign, the all-four-corners lift, and the 2D fallback via `corners(in:)`) |
| `Helpers/Audio.md` (analysis, beats, sources) | Ch 26 | taught |
| `Helpers/Listening.md` (`SpeechListener`, `SoundClassifier`, `SpokenPhrase`, `SoundEvent`, the one-shot `transcribe`/`classify`, `waitFor`) | Ch 26 | taught ("Words, and what that noise was" with the Listening figure: the caption-versus-transcript rule taught off four real transcriptions of growing prefixes of one spoken sentence, where three quarters of the way through the recognizer was sure the fox jumped over the lace, so draw the guess and act on the commitment; two listeners on one microphone as the tap hub working; the level-versus-trigger split named on both sides, with a crossing from below as what keeps a continuing sound from firing per window; the classifier's willingness to answer whatever it hears taught off three sounds made of arithmetic; and live-only closed by the deterministic one-shot, which is the Chapter 4 seed promise again) |
| `Helpers/Synthesis.md` (`Synth`, `Pitch`, `Voice`, `VoiceSource`, `PluckedString`, `ModalBody`, `StruckShape`, `Envelope`, `Voice.Filter`, delay and reverb) | Ch 26 | taught ("A sketch that plays" with the Voices figure: the engine starting itself on the first note, pitches by name or number, `for:` versus `noteOn`/`noteOff`, polyphony and what a new note takes a voice from, `Voice` presets swapped without disturbing sounding notes, the envelope taught off four real curves with sustain read as a level rather than a time (and why `.percussive` ignores a held key), `level(at:heldFor:)` for drawing the shape you designed, the filter named as most of what "sounds like a synthesizer" means, and the loop closed by reading `synth.amplitude` back off the sketch's own playing; plus "Building an instrument instead of choosing one": the patch tier named as the same relationship the Drawer has to the bare draw calls, an operator's frequency taught as a ratio of the note rather than a pitch, the case for modulation put as the one thing a filter cannot do (a filter takes harmonics away and a sine has none to take), whole against fractional ratios as the difference between a note and metal, and the eight lane limit explained by what a patch has to survive travelling through; plus "An instrument somebody recorded": the sampler taught off what moving a recording costs (pitch and length move together, so five recordings against one stretched over everything is not a subtle difference), the recordings living on the Synth rather than in the Voice taught as the same copyable-a-word-at-a-time constraint wearing a different hat, and SFZ named with the licence tiers of where to find libraries; plus "And the rest of the instrument": the effect chain as the same idea on the way out, order taught off the pair that differ only in which way round they are (an echo of a distorted sound against a distorted echo), and the two old slots kept as views over the list; plus "A shape you can hit: modal synthesis" with the StruckShapes figure (five real outlines measured by the shipped solver: a circle giving a drumhead's Bessel ratios and a square a square membrane's, why a symmetric shape's tones come in pairs and an irregular one's do not, measure-once-strike-anywhere, and where you hit it taught as the pick position in another costume) and "A string, worked out rather than drawn: the plucked string" with the PluckedString figure: the loop as a block diagram, why a model gives the attack and the darkening for free, the pick position taught off the modes it silences with the shape and the bars drawn as the same fact, and the tuning explained as the thing you would never think to check and would certainly hear; plus "Spatial audio, and sound you can keep": placing taught as two facts that mean nothing apart, and sound in an export taught as the payoff of the clock-free renderer, closing back onto the seed promise from Ch 4; plus "A note you keep playing: bowed and blown": the driven models taught off the one thing they have that the struck ones do not, a middle, with `drive` read every sample against an envelope decided at the start, over-bowing taught as the friction curve doing its job rather than a bug, and the clarinet's missing even harmonics traced to the tube being stopped at one end) |
| `Helpers/Composition.md` (`Rhythm`, `Scale`, `Chord`, `Progression`, `Tuning`, `BeatFollower`, `Arpeggio`, `MarkovChain`, `StepCounter`, `Note`) | Ch 26 | taught ("Music the sketch works out for itself" with the Euclidean figure: every type answers a step number and owns no clock, so the same pattern runs on `time`, on a detected beat, or on a drum machine later in the chapter; `StepCounter` returning a range because a frame outlasts a step; the even-spread property read off the gaps column, and the traditional rhythms that fall out of it; degrees running past both ends so a wandering number cannot play a wrong note, and `snap` for a number that did not come from music; a chord built out of the scale so its quality follows the key; an arpeggio read at the step so the figure keeps its place in the bar; and a seeded chain that borrows nothing from the sketch's own randomness; plus "Chords that come out of a key": progressions written as degrees because that is what survives a change of key, the Markov wander taught as the chain reappearing on its own moves, and chord symbols as the other form and what it trades away; plus "Twelve is a choice: tunings": equal temperament named as a compromise and heard against just intonation on a held triad, and Bohlen-Pierce as a tuning with no octave that suits the odd-harmonic reed; plus "Playing along with the room: tempo sync": a detected beat turned into musical time a StepCounter can run on, with the fold-into-range and middle-not-average rules taught as the two things you would get wrong writing it yourself) |
| `Helpers/Sonification.md` (`Sonification`, its four sources, `Bounds`, `Polarity`, `reference(at:)`, `amplified(by:)`) | Ch 26 | taught ("Numbers you can hear: sonification" with the Sonification figure: the same sixteen values as bars, as notes spread evenly in semitones, as notes spread evenly in hertz where the low half crushes into the top two octaves, and snapped so every mark lands on a line of the key; the four sources as one call each; snapping named as what makes a reading music rather than a signal; the reference note taught as a chart's grid line in sound, and why a reading without one needs absolute pitch to mean anything; and the accessibility case closed by drawing and reading one series at once) |
| `Integration/MIDI.md` | Ch 26 | taught |
| Tempo sync (`TempoClock` over MIDI clock; `Integration/MIDI.md`) | Ch 26 | taught (a musical-time diagram: 24 ticks per beat is the whole protocol, why counting means the grid can't drift, every reader with a worked position, `progress(over:)`, and the arm-on-next-tick and free-running-master behaviors) |
| `Integration/OSC.md` | Ch 26 | taught |
| `Integration/Controller.md` (`Controller`, `ControllerButton`, `controllerMotion`, `controllerDeadzone`, several players) | Ch 26 | taught ("Something to hold: game controllers" with the ReadingAPad figure: one pose read five ways, so the level/moment/both split lands beside the same split the chapter already made for MIDI; why there is no drain, since a hand cannot press and release between two frames; the null read with nothing plugged in; the two rules the figure exists for, up being negative y and buttons named by position rather than by print; motion opt-in with Xbox having no sensors at all; and the live-only export note) |
| `Vision/Vision.md` (18 trackers, ModelTracker, ConceptTracker) | Ch 27 | taught (all eighteen: hands/face/2D body/contours/optical flow, the segmenters (with "A click cuts it loose: PointSegmenter" teaching the point-prompted one's pick/include/exclude loop, worked example included, kept figure-free like every downloaded-weights model here), the 3D body read in its three spaces, then four sections for the rest with three figures built from synthetic scenes, so no figure needs a camera, a person, or downloaded weights: the card read by the rectangle detector and the OCR, the fitted trajectory carried past its last sighting onto where the ball really went, and the saliency map beside the classifier's confidence floor; `ModelTracker`'s four output surfaces are taught, with its weights left to the reader since Ollin ships none; `ConceptTracker`'s phrase scoring has its own "Words as knobs" section, worked example included, kept figure-free for the same no-downloaded-weights reason) |
| `Video/Video.md` (playback as texture) | Ch 27 | taught |
| `Integration/Syphon.md` | Ch 28 | taught |
| `Integration/VirtualCamera.md` | Ch 28 | taught ("The sketch as a webcam": why the browser needs a camera and Syphon cannot cross that line, the system extension and its one-time approval, the test card, the fixed 1280x720 letterbox and 30 fps, and suspecting the viewer first when the picture looks mirrored or cropped) |
| `Integration/DMX.md` (`DMXUniverse`, `DMXFixture`, `DMXSender`, `DMXReceiver`, `LEDMap`) | Ch 28 | taught ("Light instead of pixels: DMX" with the LampsAndBytes figure: the universe/fixture/channel model in one sentence, sACN multicast as the zero-config default vs Art-Net unicast, send-every-frame with the wire cadence handled, and the reverse direction where a console fader binds onto a `@Param` like Ch 26's MIDI and OSC; the loopback example and the Local Network permission note; and the LEDWall figure: `LEDMap` sampling the canvas onto strips and matrices, the patch-per-LED and whole-LED universe packing rules, the LEDMapping example) |
| `Integration/ScreenCapture.md` | Ch 27 | taught ("Drawing with the screen: ScreenCapture": the third frame source beside the camera and a clip, so every tracker in the chapter reads it too; naming a display, app, or window as a written value and waiting for one not open yet; the backing-resolution `scale` knob; the feedback tunnel with a figure; and the permission attributed to the launching terminal rather than the sketch) |
| `Output/Export.md` (PNG, sequence, video, GIF, SVG, PDF) | Ch 3 (GIF), Ch 15 (SVG), Ch 28 (all) | taught |
| Path-traced export (`--path-traced`, `Camera3D.aperture` / `focusDistance`; `Output/PathTraced.md`) | Ch 28 | shown ("The slow render that pays for itself": what tracing adds over the raster export, the flag with its sample count, the real lens, the viewfinder-and-film-back workflow) |
| Live recording (`startRecording()`, `SessionRecorder`, ⌘⇧R in the hosts, `--record`; `Output/Recording.md`) | Ch 28 | taught ("Keeping the take": why an export cannot hold a performance and a recording can, the host's ⌘⇧R chip and the take riding through evaluations, the key-toggle sketch form with the worked `Export/Record` example, the sound found the way the offline soundtrack is with `.microphone` for outside music, `--record` for filming from the first frame, and the generous endings on quit and Control-C) |
| Reproducibility metadata in exports (`Output/Export.md`) | Ch 28 | taught (what the recipe holds, where it lives and why that slot exists, reading it back with `exiftool`, the recover-a-past-render scenario, and the GIF and missing-code limits) |
| Record & replay (`Take`, `--record-take`, `--replay` with the transport keys and export composition; `Core/Replay.md`) | Ch 28 | shown ("Playing the night again: replay": a take as the performance itself beside the movie of it, the record and replay commands, the keyboard transport with backward scrub as re-simulation, replay through the exporters incl. path-traced and the take-length default, `--seed` for the same gestures on a new variation, and the live-feed limit) |
| Print separations (`Image.separated` into spot inks, screening, `--export-separations`; `Output/PrintSeparations.md`) | Ch 28 | taught ("Printing one ink at a time: separations" with a masters-plus-overprint figure: what a master is, why translucent inks make more colors than drums, `printInks` and the in-code form, the perceptual nearest-mix search, and the minimum-dot and rosette notes) |
| Fabrication export (`Mesh.write(to:)` as STL/OBJ/3MF, `printCheck()`; `Output/Fabrication.md`) | Ch 28 | taught ("Something you can hold: a 3D print" with a closed-versus-open figure: `normalized(scale:)` as the sizing step, why a printer needs the surface to close when the screen does not care, `printCheck()` and its plain-words `problems`, which generators close by construction, and the welding and up-axis work the writers do quietly) |
| Spatial export (`Scene.write(to:)` as USDZ, `OllinApp.spatialScene`, `--export-usdz`; `Output/Spatial.md`) | Ch 28 | taught ("Something you can walk around: USDZ" with a drawn-versus-reopened figure: the artifact becoming the scene rather than a picture of it, a frame recorded as an ordinary `Scene`, the surfaces-only rule and what stays behind, `metersPerUnit` as a declaration, and why the environment deliberately does not travel) |
| Spatial video (`--export-spatial`, `Sketch.stereoGeometry`, `Camera3D.stereoPair`; in `Output/Spatial.md`) | Ch 28 | taught ("Something you can look into: spatial video" with a plan-view stereo-geometry figure: convergence as where the screen is rather than a setting, the crossed and spread marks that make near and far, interocular as the depth dial with the 1%-of-the-frame default and the divergence rule behind it, one draw rendered twice and why drawing twice would give the eyes different worlds, and what comes out flat) |
| Variations & seed exploration (`Core/Variations.md`): `variation`, the Variation card, `--export-grid`, `--seed` | Ch 4 | taught |
| Normalized coordinates `uv(u, v)` (`Core/Canvas.md`) | Ch 1 | taught ("Placing things without pixels" with a six-panel figure of one layout in three canvas shapes, placed by pixels and by fractions; `uv` corners and `scale` for sizes, plus honest guidance on when plain pixels are fine) |
| `Tools/LiveCoding.md` (OllinLiveCoding) | Ch 28; OllinLive workflow in Ch 1 | taught |
| Single-file sketches (the `ollin` command, hashbang files; `Tools/SingleFile.md`) | Ch 1 | taught ("A shorter way to run things": `install`/`new`/run, what it means that a sketch is one file, export flags on a loose file, and the hashbang form) |
| Project generator (`ollin new <Name>`, `ollin generate`, kinds/templates/capabilities; `Tools/ProjectGenerator.md`) | Ch 1 | taught ("When one file isn't enough": the moment a folder is wanted and why you should not build one by hand, `ollin new` writing sketch plus manifest plus material folders, the live-window loop preserved by opening the generated file with `ollin`, a template read as a small sketch that already works rather than an empty file, `ollin generate` doing the one thing the terminal cannot by running each template while you choose, and the reassurance that the `.swift` file is still the artifact; the gated kinds, `--canvas`/`--remote`, and adding a template pointed at the reference) |
| Bringing a GLSL shader over (`ollin new --from-shader`; `Tools/ShaderImport.md`) | Ch 17 | taught ("Somebody else's shader", with the ImportedShader figure) |
| Bringing a scene over (`ollin new --from-scene`; `Tools/SceneImport.md`) | Ch 21 | taught ("A scene you can take apart", with the SceneAsSource figure) |
| Writing an extension (`ollin new --kind extension`, the `ollinx-` convention, the four seams; `Tools/Extensions.md`) | Ch 28 | taught ("Giving it to somebody else", with the ExtensionShape figure) |
| Accessibility (`ColorVision`, `Filter.colorVision`, `Palette.confusions`/`colorblindSafe`, `prefersReducedMotion`, `describe(_:)`/`describe(_:as:in:)`/`noDescription()`; `Helpers/Accessibility.md`) | Ch 2, Ch 3, Ch 28 | taught ("Will everybody see it?" with the ColorVision figure, "When somebody would rather it stopped" in Ch 3, and "Saying what it shows: describable output" in Ch 28 with the SayingWhatItShows figure: one sentence for the canvas, named parts with a region each, the same-numbers habit that keeps words and picture in step, what an SVG and a PDF carry, and why Ollin will not write the description for you) |
| Examples gallery (`swift run OllinExamples`) | Ch 1 | taught ("The gallery" with a layout diagram: the three panes, the filter field, hiding the knobs, why arrow keys move the list rather than reaching the sketch, and that every entry is an ordinary sketch file to open and copy) |
| Extension seam (`Sketch.extend`, `SketchExtension`) | Ch 28 | taught ("Adding behavior without touching the sketch: SketchExtension": why cross-cutting behavior does not belong in `draw()`, the built-in stats extension as the example, and the opt-in frame readback) |
| Installation mode (`installation`, `Installation`, `--installation`; `Output/Installation.md`) | Ch 28 | taught ("Leaving it running" with the LongRunClock figure: the one-line declaration and what it turns on, the two flags, how a loose single file goes up with the same flag, why the sketch clock is the sum of its capped steps rather than the wall clock, why the 32-bit shader clock restarts on a whole lap, and the log) plus "Picking up where it left off" with the Resuming figure (`@Saved` state, the cadence, what a mismatch costs, and `--fresh`). "When it falls over" adds the BackUp figure: `restarts`, the watch process, the heartbeat, the backoff. "Gallery hours" adds the GalleryHours figure: `schedule`, opening hours, `scheduledPeriod`, `scheduledProgress`. "Fitting the wall" adds the FittingTheWall figure: `projection`, Command-K and the four handles, corners kept per display, and two machines sharing a band through `shows` + `blend`. "Several displays, one machine" adds the ManyDisplays figure: `Installation(displays:)`, `.spanning` reading the arrangement the displays are in, `.parts` for two projectors sharing a band, Command-K over the whole wall, and `--rehearse` laying that wall out on one desk. "Several windows, one world" adds the OneWorldManyWindows figure: `canvasOnScreen`, drawing in desk coordinates, and keeping separate programs in step by reading one clock rather than sending messages |
| Profiling (the inspector's cost row, `FrameProfile`, `captureGPUFrame()`; `Tools/Profiling.md`) | Ch 28 | taught ("When it gets slow: the cost row" with the CostRow figure: the two bars on one frame's scale and why they are not stacked, batches against shape count, what to do about each answer, and the Xcode capture) |
| Headless capture (`OllinApp.image(of:)`) | Ch 28; used by the Guide's own figure runner | taught ("Rendering from code": the flags are a wrapper around one call, what it does step by step, the batch cases it unlocks, a figure that renders another sketch four times to demonstrate itself, and the Guide's own figure runner as the worked example) |

## Guide debt

Capabilities that shipped in the framework without a Guide section yet. An entry here is the only legal way for a matrix row to sit at `pointed`, and it is a commitment, not a filing cabinet: it names the chapter that owes the section and the date the capability shipped, so the wait is visible and countable.

Add an entry only when a session genuinely cannot teach the feature it just shipped, and say so in the commit message. Then the *next* session touching that chapter clears it. `Scripts/guide-coverage.sh` prints every entry on every run, and fails the build for a `pointed` row that has no entry.

No entries. The last one, bringing a GLSL shader over owed to Chapter 17, was cleared on 2026-08-12. The one before it, Schottky circle orbits owed to Chapter 13, was cleared on 2026-08-01.

| Capability (Docs page) | Owed to | Since |
|---|---|---|

## Roadmap parking lot

Where each open `ROADMAP.md` item will live in the Guide once it ships in the framework. Guide prose is written only for shipped features; this table reserves the seat. When writing a chapter, check whether any of its parked items shipped since this table was made.

Homes are given as the chapter numbers this Guide has today. The splits above renumber most of them, so re-read a row against the target table before acting on it.

| Roadmap item | Future Guide home |
|---|---|
| More SDF shapes | Ch 24 / Appendix D |
| More model examples (ModelTracker) | Ch 27 |
| Generative-geometry refinements | Ch 13 |
| Technique catalog: GPU-scale steering agents | Ch 19, the particles half |
| Technique catalog: growth/morphogenesis (growth on meshes) | Ch 21, the meshes chapter |
| Technique catalog: tiling/layout families | Ch 6, the tiles chapter |
| Technique catalog: meshing (point-cloud reconstruction) | Ch 25 |
| Expressive brushes and strokes (Apple Pencil tilt and azimuth) | Ch 15, and it waits on the iOS leg |
| iPhone as a sensor array, the rest of the stream | Ch 25 |
| 3D mode: soft bodies that meet themselves | Ch 21, the animate physics chapter |
| Photorealistic 3D tier | Ch 21/18, likely a new chapter when substantial |
| Sound, synthesis, and spatial audio | Ch 26, the making-sound chapter |
| Tempo sync: Ableton Link | Ch 26, the listening and control chapter |
| Live rigs (serial/BLE, laser projection, NDI) | Ch 28, the installations chapter |
| Live data from the world (WeatherKit, Core Location, HealthKit, anything fetched) | a new Part V chapter beside Seeing. The file-loading half is taught in Ch 8's data section, which points forward to it |
| New input sources | Ch 26/21 |
| New output surfaces (haptics, screensaver) | Ch 28 |
| Rendering/color frontier (GPU-driven) | Ch 16/22 |
| Authoring/editor tooling | Ch 28 |
| Collaboration and multi-device | future chapter beside Ch 28 |
| Learning (user guide, migration guide, tutorials) | this Guide itself; migration guide = Appendix C |
| Extension ecosystem (ollinx-*) | a future "Extending Ollin" appendix |
| Profiling and GPU debugging | Appendix D note, future appendix |
| Swift Playgrounds / iOS, visionOS, AR | a future part or appendix, when the platforms land |
