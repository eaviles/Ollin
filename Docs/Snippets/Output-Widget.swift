// The names the widget page's prose establishes around its fragments.
// See Guide/AUTHORING.md, "The code in the prose".

// The sketch the page names all the way through: the one `ollin new Ripple
// --kind widget` writes, ordinary in every way, which is the page's point.
final class Ripple: Sketch {}
