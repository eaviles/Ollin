# Ollin — guidance for development

Ollin is a creative-coding framework for Swift + Metal (macOS). This file captures the design intent so work here stays coherent across sessions.

## Vision

**p5.js ergonomics on an OPENRNDR-grade core, with motion as the default.**

- The API people *type* should feel like p5: `setup()`/`draw()`, bare calls like `background(.white)`, `stroke(.black)`, `drawCircle(x, y, radius)`. That familiarity is the point — don't sacrifice it.
- The architecture they *grow into* should feel like OPENRNDR: typed value objects, an explicit `Drawer`, and composable geometry.
- `draw()` runs continuously at the display refresh rate. Animation is not opt-in — `radius: 120 + sin(time) * 40` just moves. `noLoop()` is the rare still-image escape hatch.

## Scope — a framework, not an editor (load-bearing)

Ollin is a *framework* — a library of creative-coding techniques you call from `draw()` — not an *application* you open instead of writing a sketch. The framing that keeps this honest: Ollin is a curated **collection of creative-coding recipes/techniques exposed as a coherent Swift API**, so depth is always welcome *as more recipes a sketch can call*, never as application surfaces that replace writing the sketch. The test for a feature: is it a *capability a sketch calls*, or an *application surface that owns the workflow*? The first grows without bound and Ollin stays a framework; the second is the drift to resist — a full 3D editor, a DAW, a video NLE, a node-graph studio.

**Richer experiences live in host apps built *on top of* the framework, not in the core.** `OllinLive` (dev hot-reload), `OllinExamples` (the gallery), and `OllinLiveCoding` (the live-coding / live-performance instrument) are apps that *consume* Ollin — the same tier, and the right home for performance- or editor-*shaped* features. **Live coding and live performance are first-class use cases** Ollin aims to serve, through that app layer. Three rules keep the apps from dragging the core out of lane: the framework stays fully usable on its own; the `.swift` **sketch is always the artifact and the source of truth**; and any tool is a *lens on that source* (round-trips to `.swift`), never a proprietary document only the GUI can open. A **code editor is the in-lane kind of editor** — `OllinLiveCoding` live-codes the Ollin **Swift sketch itself** (not just the shader part) and *shows* that code to the audience (the Hydra model, where the code is the performance), which keeps code canonical; a GUI document editor that makes the sketch incidental is the out-of-lane kind. (The live-coding loop builds on the shipped dylib hot-reload engine — `SketchLoader` / `SketchRunner.reload`, the `--keep-clock` phase-carry — driven from an in-app editor with evaluate-on-command, since Swift compiles rather than `eval`s.)

**Where Ollin meets the pro tools, it interoperates rather than replaces.** The integration tier — Syphon, OSC, MIDI, virtual camera, tempo sync (sync *to* Ableton, never *be* Ableton), USDZ export — is "play in someone's existing rig," not "be the rig." That interop posture is the framework lane expressed in code.

## Platform scope

Apple platforms only, by design. macOS is the focus today; iOS, tvOS, and visionOS are in scope as *future* targets (see the Swift Playgrounds / iOS follow-up and the 3D & visionOS follow-up). Linux and Windows are explicitly out of scope — not now, not later — so don't add cross-platform abstractions for them: lean on Metal, AppKit/UIKit, and SwiftUI freely. "No Linux/Windows" is not "macOS-only"; keep Apple-platform portability in mind (the `#if canImport(AppKit)`/`#if canImport(UIKit)` seams) so the iOS/visionOS work later isn't a retrofit.

**Web / WebAssembly was considered and declined, on purpose.** WASM (via SwiftWasm) is the one path to p5's "share a sketch by URL" web reach — the incumbents' biggest on-ramp advantage Ollin structurally lacks — so it's worth recording *why* it stays out rather than re-litigating it. The browser has no Metal: a web build needs a *second renderer* on WebGPU, exactly the cross-platform renderer abstraction this stance refuses, and the web version would be a reduced subset anyway (WebGPU has no hardware ray tracing or MetalFX, so the photorealism/RT tier wouldn't port, and the Apple-framework satellites — Vision, audio, MIDI, video, ARKit, the iPhone sensor link — have no browser equivalent). The Metal-native bet and a browser runtime are mutually exclusive, and the bet wins. Web *reach*, when wanted, is served by *exporting the artifact* (video / GIF / USDZ / SVG) and the OS-surface outputs, not a live web runtime; the only WASM use that wouldn't betray the bet is compiling the non-rendering geometry/SVG core for a web *tool* that emits vectors. The bar that would reopen it: WebGPU reaching feature parity (ray tracing included) *and* strong, specific demand for in-browser sketches — until both, it stays out.

## The architecture rule (load-bearing)

**The bare p5-style API is sugar over a public, typed core. Never make the facade the only path to a feature.**

- `Sketch`'s bare methods forward to an internal `Drawer` (see `Sources/Ollin/Core/Sketch.swift`). Keep that split. Anything `drawCircle(...)` can do, the `Drawer` should also do — with more control.
- The day a capability is reachable *only* through the bare API, the ceiling is capped. Build the feature on the core first, then add the sugar.

## Conventions

- **Typed value objects early.** Geometry and color are data you pass around, transform, and compose — not just immediate draw calls. Prefer adding `Vector2`, `Rectangle`, and a vector `Shape`/`Contour` type *before* the call sites multiply; retrofitting `x:y:` doubles into `Vector2` later is expensive.
- **Swift-idiomatic, not a literal p5 port.** Enums for modes (not `"CENTER"` strings), trailing-closure scoping (`withState { }`) over leaky global state, value types, overloads. Borrow p5's feel, not its warts.
- **Build on the extension seam.** OPENRNDR's `extend(...)` is shipped: register a `SketchExtension` (`Sketch.extend`) for `setup`/`beforeDraw`/`afterDraw` (in-frame, before the render — can draw into the canvas), `afterFrame` (post-render, with `FrameInfo` timing), and `frameRendered` (post-render, with the rendered `CGImage`) hooks. New cross-cutting features — overlays, guides, recorders, the FPS stats (already the seam's first customer) — bolt on as extensions instead of bloating the core. Extensions are per-instance, so the runner re-attaches built-ins like the stats observer across live-reload swaps. The **frame-grab** hook is the seam's latest customer: `frameRendered(_:_:)` hands an extension the rendered frame, *opt-in* via `wantsRenderedFrame` (read live each frame, so a recorder can arm/disarm) so only something recording pays the GPU→CPU readback; on the live path it's served by an off-screen re-render (pixel-identical to export, keeps it off the present path — an async drawable readback is a later optimization). The same capture without a window is the public headless `OllinApp.image(of:frame:)` (returns a `CGImage`; `OllinApp.export` is the PNG wrapper over it), which is what the render-correctness snapshot tests diff against committed references.
- Drawing methods live on `Sketch` (scoped to the instance), not true globals — keep it that way.
- **Draw verbs, not nouns.** Geometry-emitting calls are imperative verb phrases with a `draw` prefix — `drawCircle`, `drawRect`, `drawLine`, `drawPolygon`, `drawPolyline`, never bare nouns (`circle`). They read as the side-effecting immediate-mode commands they are (what the Swift API Design Guidelines want), and the prefix gives a clean rule: `draw*` emits geometry; state (`fill`/`stroke`/`background`) and transforms (`translate`/`rotate`/`scale`) keep their own names. "Feels like p5" is the *shape* of the API — bare top-level calls, terse positional args, motion by default — not p5's literal spelling, so the prefix keeps the feel and stays a good Swift citizen. New primitives take it too (`drawEllipse`, `drawTriangle`, …). (oF landed here too: `ofCircle` → `ofDrawCircle`.)
- **Consistent point arguments.** A primitive that takes a point offers a *positional* scalar form and a *role-labeled* `Vector2` form: `drawCircle(x, y, radius)` beside `drawCircle(center:radius:)`, `drawRect(x, y, width, height)` beside `drawRect(corner:…)`/`drawRect(center:…)`, `drawLine(x1, y1, x2, y2)` beside `drawLine(_:_:)`, `translate(x, y)` beside `translate(_ offset: Vector2)`. The rule: bare scalars go positional — everyone knows the `x, y, radius` order, and labels on them are pure stutter once call sites pass `x`/`y` variables (`drawCircle(x: x, y: y, …)`). The label is reserved for the value-object overload, where it names the *anchor* (`center:` vs `corner:`) and carries real information. New point-taking primitives (`drawEllipse`, `drawPoint`, `drawTriangle`, …) follow the same split. (Unlike Kotlin/OPENRNDR, Swift can't make one declaration callable both ways, so don't double the overloads to fake "both" — pick positional for the scalar form.)
- **State stack reads as Swift.** The drawing-state stack is `pushState()` / `popState()` — the combined transform+style snapshot, mirroring Core Graphics' `saveGState`/`restoreGState` — with `withState { }` as the scoped, exception-safe primary form (the `with…{ }` idiom of `withAnimation`/`withTaskGroup`). Reach for `withState { }`; the bare pair is the escape hatch. Combined (not split into transform vs style) by design; a split push/pop can come later as `Drawer`-level methods.
- **Deprecation path is a 1.0 gate.** Pre-1.0 the public API churns freely with no shims (the README disclaims stability) — no external sketches to protect yet, so renames like the noun→verb (`circle`→`drawCircle`) and `push`/`pop`/`isolated`→`pushState`/`popState`/`withState` ship unshimmed. *Before* 1.0, stand up the migration discipline that holds from 1.0 on: every public rename/removal ships an `@available(*, deprecated, renamed: "…")` shim that forwards to the new API, so Xcode offers a one-click fix-it. Gotchas: use the *full selector* in `renamed:` when labels or arity differ (`@available(*, deprecated, renamed: "drawCircle(_:_:_:)")`, not just `"drawCircle"`); keep deprecated members together (a `Deprecations.swift`) so they're easy to audit and drop after a grace cycle; have the shim *forward* to the new API, not duplicate it. A must-do gate, not a nice-to-have — the 1.0 stabilization pass is where the pre-1.0 renames get their shims.

## Sourcing & attribution (load-bearing — it's the public face)

This project is built with AI and says so openly (the README's "Built with AI" and "Influences & attribution" sections; the latter is a summary of the full credit record in the repo-root `ATTRIBUTION.md`). Getting attribution and license-compatibility right earns the creative-coding community's trust — treat it as first priority, not a side chore.

The framework is *inspired by* p5.js (LGPL-2.1), OPENRNDR (BSD-2-Clause), and openFrameworks (MIT): borrow their ideas and API vocabulary, write the implementation independently. **Never translate their source line-by-line** — a port of source is a derivative work that carries the original's license, and p5.js's LGPL is incompatible with Ollin shipping wholesale as MIT. "Inspired by" keeps Ollin MIT-clean; "ported from source" does not.

**One sanctioned exception: bundled third-party source (a distinct tier).** A library may be *vendored* — shipped as actual source — when it's a self-contained, permissively-licensed component wasteful to reimplement, wrapped behind Ollin's own API so it stays swappable and off the public surface. This is the opposite of inspired-by (it *is* their code), so it carries license obligations: keep the upstream LICENSE + per-file headers **intact** in the vendored dir (the one exception to "no framework names in code" — they're the library's own headers, and stripping them breaks the license); record provenance (upstream URL, commit, date); list it in the repo-root `THIRD-PARTY-NOTICES.md`; add a *Bundled third-party code* entry to `ATTRIBUTION.md` (the credits file the README links); keep the root `LICENSE` pure MIT so GitHub's root-only license detection holds the "MIT" badge. Vendor under `External/`, not `Sources/`. The first and only instance is **libtess2** (`External/CLibtess2`, SGI-B), the triangulator behind `Shape`/`Contour`. This tier is *not* a license to vendor copyleft (LGPL/AGPL/GPL) code, which stays inspired-by-only.

For ported *example sketches* (the iterate-by-porting workflow), provenance is per-sketch and must be honored:

- **Only port sketches whose license permits redistribution under MIT** — MIT, BSD, Apache-2.0, CC0/public domain, or CC-BY (with credit). For GPL/LGPL, CC-BY-NC, or CC-BY-SA sources, either get permission or rewrite the sketch as original work using the source only as inspiration (credit it as "inspired by").
- **Name the source in the file header** — the specific sketch, its author, a URL, and its license. Template lives in `Examples/README.md`.
- When unsure of a sketch's origin or license, **ask before committing it.** A trivial sketch (a plain dot grid) may fall below the copyright-originality bar, but crediting the inspiration is still the default.
- **Recreations** (`Examples/Recreations/`, organized by artist) credit the artist, framed as a homage *after* them (not a reproduction, not endorsed), plus the SFPC "Recreating the Past" class that inspired the section. Some are *ported* from a source sketch, so they also carry the code-lineage header (e.g. the Molnár ones, via the p5 repo); others are *original* Ollin interpretations built directly from the artwork, with no code lineage (e.g. the Riley one). Either way, name the artist and work and keep the homage framing. See `Examples/Recreations/README.md`.
- **Keep framework names — and product/lineage comparisons — out of code.** The influence story (p5.js / OPENRNDR / openFrameworks) lives in this file, the README, `ATTRIBUTION.md`, and commit messages — *not* in `.swift` comments. This is not limited to those three: it covers any *external product* reference (e.g. **Record3D** the capture app, SinkCam, Hydra) **and** *lineage/marketing comparison framing*. Don't write `// p5 default`, `/// like oF's ofNoise`, `// the own-app successor to borrowing Record3D's feed`, `// Ollin's own-app answer to X`, `// the way X does, but ours` — keep the information, drop the name *and* the comparison. (Ollin's *own* type/example names — `Record3DDevice`, `Camera`, `PhoneBodyPose` — are fine as plain internal cross-references, and Apple-framework API names actually in use — ARKit, Vision, usbmuxd — are fine; the prohibition is *external* product/framework names and *comparison framing*, not internal documentation.) Apply this *at authoring time*, not as a later cleanup, and when sweeping a file check *pre-existing* comments too — these have slipped into committed code before. The one *exception* is the per-example attribution header, which credits the actual source lineage. Source littered with running comparisons reads like a port and undercuts the "independent implementation, inspired-by not copied" stance.

There's a second tier of influence worth distinguishing from the conceptual one. Where p5.js / OPENRNDR / openFrameworks shaped the *API and ideas*, two peer Swift+Metal frameworks were read for *engineering approach* on the same platform: **swifty-creatives** (Apache-2.0, Processing-style immediate-mode) and **AsyncGraphics** (MIT, GPU image/video compositing). They were studied, not ported — specific lessons are noted inline through this file (shader build, pipeline cache, SDF circle, iOS seam, easing, snapshot testing). Same rules apply: credit them in `ATTRIBUTION.md` and commits, never in `.swift` comments, and write our own implementation. The public credit lives in `ATTRIBUTION.md` (linked from the README). (PixelKit is AsyncGraphics's older node-graph predecessor; cite AsyncGraphics instead.)

A third tier names influences for *capabilities still ahead* — shader-function collections, shader-composition / livecoding APIs, and AR. **These are inspirations, not dependencies:** *study how they work and reimplement our own*, never vendor or depend on them. It's especially clean for the shader-function collections — mostly implementations of *known, published techniques* (SDFs, noise, blends, color-space math, domain repetition): peek at the approach, then write Ollin's own, preferring the canonical source of a technique over any one collection's expression. Licenses still drive the no-line-by-line-copy rule:

- **LYGIA** (github.com/patriciogonzalezvivo/lygia, Patricio Gonzalez Vivo, who also created The Book of Shaders) — a large, multi-language catalog of shader functions (GLSL/HLSL/WGSL/CUDA plus an *experimental, partial* MSL/Metal port: `sdf`, `sampler`, `blend`, parts of `distort`/`generative`/`math` done; `lighting`/`geometry`/`palette` not yet). The best *map* of which techniques exist and how they're commonly approached. Licensed **Prosperity Public License 3.0.0** (noncommercial) — since we reimplement rather than ship its code, that's not a bundling problem; it's the reason to never translate a LYGIA file line-by-line and to implement from the underlying technique instead.
- **Hydra** (github.com/ojack/hydra, Olivia Jack) — a web livecoding environment with a chainable, analog-video-synth shader API (`osc().rotate().modulate(noise())…`). The thing to mine is *how it makes combining and mixing visuals so fluid* — borrow that ergonomic direction. Licensed **AGPL-3.0** (strong network copyleft) → **inspiration only**, never ported or vendored, exactly like p5.js's LGPL.
- **ShaderPark** (github.com/shader-park — `shader-park-core`, `shader-park-examples`, `shader-park-live-coding`, all **MIT**) — a JS→shader SDF "sculpting" library with composable combine/blend functions and its own livecoding environment; another reference for *how to compose and mix* shader-driven shapes. MIT, so it can be studied freely — still: write our own, credit it.
- **ofxFX** (github.com/patriciogonzalezvivo/ofxFX, Patricio Gonzalez Vivo again, **MIT**) — the openFrameworks effects addon behind his *Efecto Mariposa*: each effect is a ping-pong-FBO object chained/mixed with a `<<` operator, organized as composers (blend/mask/multi-texture), filters (blur, gaussian, bloom, glow, bokeh, **LUT color grading**, chroma aberration, old-TV), generative (noise, FBM, fire, Gray-Scott), and operations (ripples, flow, edge detect, normals). The reference for the [layered-effects](DESIGN-NOTES.md#layered-effects-and-compositing) *effect-object architecture and catalog* — blending fx shaders and LUTs — from the GL/oF generation, beside AsyncGraphics' Metal-native take. MIT, study freely — still: write our own, credit it.
- **Paper Shaders** (github.com/paper-design/shaders, paper.design, **Apache-2.0**, shaders by Ksenia Kondrashova) — production design-shader effects for the web: mesh gradients, liquid metal, fluted glass, god rays, water, heatmap, paper texture, dithering/halftone. The map of which *designer-facing* effects exist and their parameter feel — the design-pattern `Generator` set was cross-read against it (written from the canonical techniques, credited per-technique in `ATTRIBUTION.md`), and the design-filter set (fluted glass, water, paper texture, liquid metal, heatmap, gem smoke) was cross-read the same way. Permissive, so study freely — still: write our own, credit it.
- **Meta Spark** (Meta's discontinued AR studio, shut down Jan 2025 — no public source) — the *product* reference for an AR direction: its template-driven AR-effect authoring is the void @eaviles wants Ollin to fill on Apple platforms. Conceptual/product influence only; there's nothing to port. (Used in Zach Lieberman's AR class — keep that personal detail out of public files, see the AR follow-up.)

Same rules as every tier: credit in `ATTRIBUTION.md` and in commits, never in `.swift` comments, own implementation.

## Shaders & the Metal back end

The shader set is split across several `.metal` segments (`Renderer/Shader*.metal`), concatenated and compiled as one library at runtime (see *The shader set is multiple segments* below). Decisions that are cheap now and expensive to retrofit:

- **One source of truth for CPU↔GPU structs — defined once in a shared C header.** The three structs shared between Swift and MSL (`OllinVertex`, `Uniforms`, and `SDFInstance` — the last a stride-144 *tagged union* that keeps growing) live in `Sources/Ollin/Renderer/OllinShaderTypes.h`, so a field can't drift between the two sides and silently corrupt memory. Swift imports them through the thin `COllinShaders` C target (whose umbrella header re-includes the canonical file by relative path, since SwiftPM won't let a resource escape its target or two targets' dirs overlap); `ShaderCore.metal` (the first segment) `#include`s the same header. The one wrinkle the header note predicted: runtime `makeLibrary(source:)` has no include path, so `MetalRenderer.composeShaderSource` splices the header's text in for the `#include` directive before compiling — which is why the header ships *beside* the segments as a `.copy` resource. The fields are spelled in the `simd_*` names the Swift importer maps to `SIMD2<Float>`/etc.; a `__METAL_VERSION__` guard aliases those onto Metal's `float2`/`float3x3`. **A new shared struct (or a wider `SDFInstance`) goes in that header — never re-declared in two places.**
- **Runtime source compilation is a feature, keep it.** `loadLibrary` concatenates the `.metal` segments and compiles them from source at runtime (`makeLibrary(source:)`). For a creative-coding framework that's the seam for shader hot-reload and user-supplied shaders later — don't rip it out. Its costs are startup time and *no build-time error checking*.
- **Add a precompiled `default.metallib` when those costs bite, don't replace.** Build-time validation and zero startup cost need the built-in shaders compiled ahead of time. `loadLibrary` prefers a precompiled `Bundle.module` metallib *before* the source path **on a non-ray-tracing device** (an RT device source-compiles, to splice in the device-conditional `OLLIN_RT_SHADOWS` define), so a precompiled lib slots in with no API change; runtime compilation stays for dynamic shaders. Two facts settle *how* to produce it (both verified on this toolchain — Swift 6.3, macOS):
    - **Ship `.metal` as `.copy` (raw source) — `.process` now precompiles a metallib.** Ollin compiles its shaders from source at runtime, the seam the shader hot-reload, user shaders, *and the device-conditional `OLLIN_RT_SHADOWS` ray-traced-shadow path* all rely on — so the raw `.metal` must reach `Bundle.module`. **On current SwiftPM toolchains, `.process` on a `.metal` precompiles a single `default.metallib`** (and stops copying the raw source), so `makeDefaultLibrary(bundle: .module)` then succeeds — convenient for a fixed shader set, but a metallib holds only one compile, so it *can't* carry a device-conditional variant. That's why the `Shader*.metal` segments are **`.copy`** resources and `loadLibrary` source-compiles on a ray-tracing device (preferring the metallib only on non-RT GPUs). (Historically `swift run` did *not* auto-compile loose `.metal` — that was Xcode-build-system-only — but the toolchain caught up; don't assume `.process` leaves the source uncompiled.)
    - **A precompiled `default.metallib` shipped as a `.copy` resource loads fine.** `xcrun metal` + `xcrun metallib` produce a `default.metallib`; `.copy`-listed in the target, it's found by `makeDefaultLibrary(bundle: .module)`. So under `swift run` the path is a SwiftPM build-tool plugin that runs that compile and emits the metallib as a resource. (Playgrounds plugin support is unreliable — see the iOS follow-up — so keep runtime source compilation too; never go plugin-*only*.)
- **A user-supplied shader-library seam is cheap.** Resolve the active library as `customMetalLibrary ?? defaultMetalLibrary` so a user can drop in their own compiled library without touching the built-ins; pairs with the runtime-source loader for hot-reload. (Both peer Swift+Metal frameworks expose exactly this.)
- **Built-in shader functions are our own, written from the technique — not a vendored library.** As the shader set grows past drawing (noise, SDF primitives, blends, color-space conversions, domain repetition), implement each from the *published technique*, then credit that source in the `ATTRIBUTION.md` Techniques list (as the cosine `CosinePalette` and `curlNoise` already do). The canonical sources to reach for, baked in here so we go straight to them: **Inigo Quilez** for signed-distance functions (2D `iquilezles.org/articles/distfunctions2d`, 3D `/distfunctions`) and smooth-minimum, and the cosine palette (already used); **hg_sdf** (`mercury.sexy/hg_sdf`) for domain operators — axis rotation, mirror, and linear/polar repetition; **The Book of Shaders** for hashing and value noise. LYGIA is a broad *catalog/map* of which techniques exist and how they're approached — read it to discover, implement from the underlying technique, never translate its files (Prosperity; see [Sourcing & attribution](#sourcing--attribution-load-bearing--its-the-public-face)). (Distinct from the user-supplied seam above, which is for a *user's* own shaders, not for vendoring someone else's library into Ollin.)
- **Cache pipelines; don't grow `init`.** `MetalRenderer.init` builds one `MTLRenderPipelineState` inline. A new pipeline (textured quads, a new blend mode, compute) should be a cache lookup, not more code in the constructor. Key the cache on a `Hashable` descriptor struct (shader, blend mode, MSAA sample count, pixel format), not a flat enum: pipeline variants are *combinations* of those axes, and a struct key captures the product without an enum case per combination. Folding blend mode into that descriptor also keeps it a pipeline *parameter* — one pipeline factory — rather than a whole renderer subclass per blend mode (the trap a peer framework fell into).
- **The shader set is multiple `.metal` segments, concatenated at load.** It outgrew a single file at ~2,000 lines and split into `Renderer/Shader*.metal`: `OllinShaderLib` (the shared helper library — see *User-supplied shaders*), `ShaderCore` (preamble + shared color/dither/hash helpers), `ShaderShapes` (SDF primitives + the shared `ollin_sdf_distance`), `ShaderCombinator` (the SDF-combinator node VM — must follow `ShaderShapes`, it reuses `ollin_sdf_distance` + the coverage tail), `Shader3D` (particles/cloud/mesh/matcap/wireframe), `ShaderRaymarch` (the 3D SDF sphere-marcher — must follow `Shader3D`), `ShaderEffects` (present + the single-input filters + basic generators; `PresentOut` lives here for every later segment), `ShaderCombine` (two-input combines + the screen-space depth ops + the deferred RT-reflection passes), `ShaderSim` (the stateful ping-pong sims + fluid), `ShaderPatterns` (the design-pattern generators + design filters), `ShaderIBL` (the environment bake). `loadLibrary` reads them from `Bundle.module` in the fixed order `MetalRenderer.shaderSourceNames` and concatenates into one compile unit, because runtime `makeLibrary(source:)` has no include path (so an `#include` umbrella can't work). **Order is load-bearing:** `ShaderCore` is first because Metal needs a declaration before use and the later segments rely on its helpers; no segment is compilable alone, and the header `#include` lives once in `ShaderCore` for `composeShaderSource` to splice. Live shader reload (`SketchRunner.reloadShaderLibrary(fromDirectory:)`, used by OllinLive) concatenates the same segments from the repo filesystem via `MetalRenderer.concatenatedShaderSource(fromDirectory:)`. Adding a segment: create the file, add it to `shaderSourceNames` (in dependency order) and to the `Package.swift` `.copy` resources.

## Rendering performance

The drawing model is *immediate-mode GPU*: every frame `Drawer` records each primitive into call-ordered batches, the renderer uploads them through a **triple-buffered, semaphore-gated vertex-buffer ring**, and encodes one draw per batch run. (The ring is load-bearing — never collapse it back to one shared buffer: writing a buffer the GPU is still reading for an in-flight frame tears the geometry on screen, e.g. gaps in a stroked ring; a real bug, fixed by the `maxFramesInFlight` ring + `frameBoundary` semaphore in `MetalRenderer`.) This is the right shape — it's what NanoVG, Dear ImGui, and Processing's GL renderer do — so the "low-level" look is the cost of a Metal core, not accidental complexity. Don't trade it for Core Graphics / SwiftUI `Canvas` / SpriteKit: those are CPU rasterizers or retained-mode scene graphs, and adopting one as the substrate throws away the GPU + shader ceiling the whole vision rests on. **CPU tessellation is the first bottleneck, not the GPU** — and **instancing is the shipped answer** (the SDF quads, the point-cloud splats), with **retained batches** (`Batch`, the *Current state* bullet) the shipped answer for static heavy content; the tiers above them (indirect command buffers, mesh shaders) are roadmap (`ROADMAP.md` § *Rendering and color frontier*, `DESIGN-NOTES.md`).

Mechanism and history live in [`ARCHITECTURE.md`](ARCHITECTURE.md) § *The 2D drawing paths*; the regression-preventing rules stay here:

- **SDF analytic primitives — shipped.** The closed 2D primitives (~30 shapes: point markers / circle / ellipse / rect / oriented box / circular arc / triangle / ngon / star / ring + the novelty catalog) each render as one instanced quad (`SDFInstance`, the `.sdf` pipeline) — per-shape CPU cost is one struct write, and each instance carries its own CTM so AA stays ~1px under any transform. `drawLine`/`drawBezier` moved to the **fringe stroke path** (their shader cases retained-but-unemitted); arbitrary `drawPolygon`/`drawPolyline` and elliptical/full-turn arcs stay on the triangle path (`drawArc` runtime-branches; its *stroke* goes to the fringe path either way). **Coverage is split by whether a shape tiles, and it's load-bearing:** box/pie/chord/triangle/star/ngon + the non-round markers use the **inside-biased** ramp (`regionCoverage` — full to the geometric edge, AA halo only outside) so abutting fills leave no seam — never move them onto the centered ramp; disks and capsules/lines use **area-conserving** coverage (`diskCoverage` — a sub-pixel mark keeps a ~1px footprint while its alpha scales by true area/width, so sizes run 0…n without popping or flicker); stroke **bands** conserve ink the same way (`strokeBandCoverage`; a band ≥~1px reduces to the plain smoothstep, byte-identical). **Draw order is preserved:** call-ordered `GeometryBatch`es (`.triangles`/`.sdf` runs) composite as drawn — never collapse into unordered passes (breaks occlusion, e.g. EllipseField's black-fill ropes). One non-discoverable rule: vesica/moon `cornerRadius` must `opRound` off a *full-footprint* SDF (the inset form loses all float precision — a real glitch). **Hollow/band mode** (`hollow(_:)` / `solid()`) onions every region shape's SDF (`SDFShape.honorsHollow` opts in); **stroke alignment** (`strokeAlign(_:)`) shifts the stroke band by an exact SDF offset riding bits 8-9 of the `shape` tag; `.center` is byte-identical. New shapes drop in as four touch-points under a fixed scalar budget — **`SDFInstance` is full at 144 bytes** (the next field is a real widening) and `size` doubles as the covering-quad AABB half-extent, never a free geometry slot; the forward shape list lives in `ROADMAP.md`, and SDF *operators* live on the combinator path.
- **MSAA is 8× where the device supports it** (else 4×), and only for the *triangle* path (the SDF and fringe paths carry analytic coverage). The sample count is chosen once via `ollinPreferredSampleCount(_:)` (`SketchView.swift`) and shared by the live view and the export paths.
- **Rendering is gamma-correct (linear-light).** Geometry composites into a **linear `rgba16Float` intermediate**; the present pass tone-maps, dithers (TPDF, a pure function of pixel position — reproducible), and re-encodes to the sRGB drawable — the single 8-bit quantization point. Three rules keep tones from shifting: shaders **linearize** their sRGB `Color` inputs; the **clear color is linearized too** (`Color.srgbToLinear` in `mtlClearColor` — miss it and the background washes out lighter, since the clear bypasses the shader); image textures load `.SRGB: true`. **One deliberate carve-out: stroke and disk/dot AA *coverage* is remapped to perceptual alpha** (`perceptualCoverage`) — pure linear blending reads a thin dark mark faint/"beaded" (the Molnár *Interruptions* bug) — touching **only partial coverage**, never a shape's own fill/stroke alpha, and **not** the region *fill* ramp (abutting fills must keep tiling seamlessly). The capsule/bézier AA footprint is the **L2 gradient length**, not `fwidth` (the L1 norm overshoots √2× at 45°, fading diagonals). Verified near-optimal for the raster path (4× SSAA barely beats it).
- **Fills triangulate, strokes fringe.** Concave/holed fills go through **vendored libtess2** (`Shape.triangulatedFill()`, per-`Shape` `winding` — `.evenOdd` default, font glyphs `.nonZero`; the glyph clean/simplify gotchas live under *Text* in *Current state*) + MSAA; convex shapes keep the direct fan/strip math — don't route them through the triangulator. **Every stroke renders through the AGG/NanoVG-style fringe expander** (`appendFringeStroke`): CPU edge-expansion plus a ~1px screen-space AA fringe, honoring `strokeJoin`/`strokeCap` (`drawLine`'s default cap is now **butt**), the coverage riding an `aa` field in `OllinVertex`'s alignment padding (stride stays 32, the triangle path byte-identical), the fringe straddling the true edge so perceived width = `strokeWidth`. **The fringe is strokes-only — fills stay libtess2 + MSAA, byte-identical** (a fill fringe reintroduces abutting-fill seams or fattens; settled by a native-res A/B). A cross-section is **five points (outer/core/center/core/outer), and the centerline one is load-bearing**: every join fans from the path vertex, so without it that apex T-junctions into the middle of the core band's end edge and the sub-pixel-snapping hairline eats MSAA samples (isolated 6/8-coverage pixels inside solid ink; `FringeStrokeTests` pins it, no snapshot can — a few pixels in a million average away). Coverage is equal across the two core bands, so the split draws nothing new, and building each cross-section as a fixed `Cross` value rather than the old per-call `offsets(_:).map` arrays makes it a net **~10% faster** than before. **Consecutive segments end on their shared inner crossing** (the inner miter point, NanoVG's `chooseBevel` model, `innerMiter[v]` scaled so an offset stays the true distance to *both* centerlines): ending each on its own perpendicular makes both quads cover the wedge between them, and non-opaque ink composites it twice — a hard second-coat diamond at every corner (224 → 197 at alpha 0.25), a graded band down a curve's inner half, dark radial spokes combing a spiral. **Only the inner side moves** (the outer side keeps its perpendicular, so the join filler and the centerline seam are untouched), the covered region is *unchanged* for opaque strokes (the union already ended at that crossing → whole snapshot suite passed unrecorded), collinear vertices are skipped (a straight polyline is byte-identical), and a crossing outside either neighbouring segment falls back to square ends (the inner-bevel case — an overlap beats a crack when the corner folds anyway). Triangle count and speed are unchanged. `FringeStrokeTests` pins it (a snapshot's mean diff averages it away). **Measure coverage in linear light** before reasoning about gap widths (the same pixel reads 46% in sRGB, 75% linear — the sRGB figure made the seam look impossible). `appendStrokedPath` is retained for **outline-glyph stroking only** and shares the rule via `Drawer.innerCrossings`, but its segments are plain quads with no centerline point, so bending the inner end moves the end edge off the path vertex and a filler fanning from there T-junctions (a **pale** comb, the mirror of the dark one): its filler fans from **the crossing itself** instead (free — the gap is bounded by both end edges, which run to that point; pixel-identical to splitting each segment at the centerline, which costs two triangles). `GlyphStrokeTests` pins both symptoms with AA-immune predicates (darker-than-coat / lighter-than-coat-with-ink-all-around, `InkProbe` in the test support; note the present pass **dithers**, so one coat straddles two 8-bit levels). The **elliptical/full-turn `drawArc` stroke** joined the fringe path too (it used to lay bare `appendSegment` quads with no joins or caps at all, so it carried *both* defects: inner overlap **and** outer gaps, together a fine faceting) — it hands the expander the same shapes `svgRecord` writes (`.open` polyline / `.chord` closed / `.pie` closed through the center), so raster and vector trace one outline, and it now honors `strokeCap`/`strokeJoin` (default `.butt` keeps the old ends). Curved outlines flatten through the public `Path` builder (`drawShape { }`, `drawCurve(_:closed:)`) into the same fill + stroke paths — a curve never touches the SDF side.

Pipelines build through the `PipelineKey`-keyed cache (`makePipeline(_:)`; the enum→`Hashable`-struct key migration shipped with the 3D work), so a new pipeline is a new cache key, not more `init` code — blend mode, MSAA count, and pixel format ride the key as parameters (see *Cache pipelines* above).

## Build, run, verify

```sh
swift run Example-Basic-HelloCircle   # opens a window running an example
```

The canvas is `1080×1080` by default (`canvasSize`, the typed `CanvasSize` enum — `.square(n)` / `.size(w, h)` + named presets, integer pixels); the preview window is sized from the sketch's `windowMode` (`.auto` fits the screen, `.fixed`, or `.resizable`) — the window is a scaled view of the canvas, not the canvas itself.

- **Requires macOS 26+ and a Metal-capable GPU.**
- **`swift build --target Example-X` does not relink the executable.** It compiles the module and reports success, but `.build/debug/Example-X` stays the *old* binary — a tuning loop against it silently re-runs stale code (a real hour lost: three "tuning" rounds of VideoTrace all ran the first build). Rebuild runnable examples with `swift build --product Example-X` or just `swift run Example-X`; reserve `--target` for compile-checking libraries.
- **Check the environment before claiming you can't build.** Ollin needs the Swift toolchain and Metal, which exist on a Mac but *not* in the Linux web/iPhone container. These two run on different machines, so don't assume: if `swift --version` and `xcrun --find metal` succeed, you're on a Mac with the toolchain — **build, run, and verify directly** (`swift build`, `swift run <Example>`), and don't add caveats about being unable to compile. Only when that toolchain is genuinely absent (the Linux container) are changes unverifiable on this machine — say so then, and not before. Either way, the things that truly need a simulator or device (iOS/visionOS/AR) stay unverifiable here regardless.

## Current state & roadmap

Per-feature *user-facing* depth lives in the `Docs/` page named in each bullet; deeper *engineering internals* (the mechanism, the why, the measurements behind a shipped system) live in [`ARCHITECTURE.md`](ARCHITECTURE.md), so this section can keep a terse invariant + a pointer instead of swelling. This section is the capability inventory plus the cross-cutting gotchas. Architecture and policy that span features stay in their own sections above (*Shaders & the Metal back end*, *Rendering performance*, *Sourcing & attribution*, *Conventions*). When work ships it lands here — see the ship checklist under *Roadmap & design notes*. **Routing a new fact: a regression-preventing invariant or cross-cutting rule stays here (terse); its explanation/history goes to `ARCHITECTURE.md` (shipped internals) or the memory files (debugging narration); forward plans go to ROADMAP/DESIGN-NOTES; user-facing API goes to `Docs/`.**

A few load-bearing rules recur across the satellites, so they're stated once here instead of per-bullet:

- **Render-thread closures must be built non-isolated.** Any closure an audio / MIDI / Vision / capture render thread calls, if formed in a `@MainActor` context, inherits main-actor isolation and Swift injects an executor assertion that *SIGILLs* the instant that thread runs it — build it in a free function or a non-`@MainActor` type.
- **`resource:in:` can't default `in:` to `.module`** — a default arg resolves to *Ollin's* bundle, not the caller's; every font / image / mesh / model loader makes `in:` explicit.
- **Never replace a GPU texture in place** — an in-flight frame may still be reading the old one; allocate a fresh texture when the bytes change.
- **Satellites can't depend on each other** — a shared seam (`RGBDFrame`, `SegmentationImages`, `FrameSource`, `AudioTapSource`, the `OllinUSBMux` tunnel) lives in the `Ollin` core, often `package`-access.
- **Background-producer → main-reader handoff** is uniformly an `@unchecked Sendable` producer with an `OSAllocatedUnfairLock` handoff (audio analyzer, OSC/MIDI receivers, Vision analyzer, phone readers all share this shape).

**Today** (the shipped surface):

- **Lifecycle & input.** `Sketch` base class; temporal state (`frameCount` / `time` / `deltaTime` / `frameRate`); mouse (`mouseX` / `mouseY`, `mouseIsPressed`, `mousePressed()` / `mouseReleased()`) and keyboard (`key` / `keyCode` / `keyIsPressed`, `keyPressed()` / `keyReleased()`, `isKeyDown(_:)`, typed `KeyCode`). The held-flag + release-hook pair makes drag-to-paint a `draw()` poll. `Docs/Core/Sketch.md`, `Docs/Helpers/Input.md`.

- **Rendering.** `Drawer` + Metal renderer over several pipelines recorded into **call-ordered batches** (so SDF / triangle / fringe / image / glyph / 3D geometry composite front-to-back as drawn — never collapse into unordered passes): a tessellated-triangle path (libtess2 fills + 8×/4× MSAA), an instanced-SDF path, a fringe-stroke path (the high-quality stroke renderer), a textured-quad image path, and a glyph-atlas path. Gamma-correct **linear-light** compositing in a `rgba16Float` intermediate that a final present pass tone-maps + dithers to the sRGB drawable. **`blendMode(_:)`** (`.normal` / `.add` / `.subtract` / `.multiply` / `.screen` / `.lightest` / `.darkest`) is stack state carried as a `Pipeline` cache-key parameter, not a renderer-per-mode. **HDR / tone-map** — `toneMap(_:exposure:)` (`.clamp` default / `.reinhard` / `.aces`), a frame-wide mode over the float pipeline (`Docs/Drawing/HDR.md`). **Accumulation surface** — `noClear()` / `clearEachFrame()`, a persistent float canvas drawing piles onto; `background(_:)` is the in-mode wipe (`Docs/Drawing/Accumulation.md`). The **SDF path** covers a broad analytic-shape catalog (`drawPoint` / `drawCircle` / `drawEllipse` / `drawRect` / `drawOrientedBox` / circular `drawArc` / `drawTriangle` / `drawNgon` / `drawStar` / `drawRing` / `drawHeart` / `drawMoon` / … + `hollow(_:)` band mode, `strokeAlign(_:)`, area-conserving disk + outline coverage); the **fringe path** covers every stroke (`drawLine` / `drawBezier` / `drawPolyline` + polygon/shape/elliptical-arc outlines, honoring `strokeJoin` / `strokeCap` / `strokeProfile`, translucent + gradient paint); the **triangle path** covers convex/concave/holed fills (`drawPolygon` / `drawShape`, curved via the `Path` builder + `drawCurve`) and elliptical/full-arc fills. Collection sugar: `drawCircles` / `drawPoints` / `drawRects`. **Kaleidoscope symmetry** — `symmetry(_:mirrored:)` / `noSymmetry()`, stack state: every 2D draw call replicates through canvas-space fold matrices (each local rotation/reflection conjugated by the CTM at the `symmetry` call — fold 0 stays the exact identity, so the primary copy is byte-identical). **Load-bearing:** replication is per emission funnel — SDF instances and combinator groups left-multiply their per-instance `transform` (groups share one node program), the triangle/fringe/image/glyph paths range-copy canvas-space vertices via `Drawer.replicated` whose body must stay within **one batch kind** (wrap a fill section and its stroke separately; `isReplicating` passes a nested funnel through, and folds are rigid so screen-space AA/fringe widths stay exact), and `svgRecord` replicates commands so `--export-svg` folds too; 3D geometry, GPU particles, and depth scenes are untouched by design. Example `Patterns/Kaleidoscope` (declares `loopDuration`); snapshot `symmetry`; `SymmetryTests`. **Clipping** — `withClip(shape/rect/circle) { }`, the standard stencil clip (increment/decrement): the region triangulates through the fill path into a stencil-only `.clipPush` batch (CTM baked at the push), content batches carry a `clipLevel` and stencil-test `equal` to it, and the block's end appends a fullscreen-decrement `.clipPop`; nesting intersects. **Load-bearing:** a `stencil8` memoryless MSAA attachment joins a pass **only when that surface clips** (`Drawer.usesClipStencil` / `RenderTarget.needsStencil` — an unclipped frame is byte-identical), and in a stencil-carrying pass **every** pipeline key must declare `stencilFormat` (skybox and raymarch-upsample included) or Metal validation rejects the draw; never pass `setDepthStencilState(nil)` (validation rejects nil — use `noDepthState`); clipping is **per surface** (a layer opened inside a clip block starts unclipped); `background(_:)` inside a clip block re-emits the open pushes after the wipe (`reemitClipPushes`); clip edges AA at MSAA resolution (the clip shape rasterizes binary per sample); `--export-svg` emits native `<clipPath>` defs + nested `<g>` groups. Example `Shapes/Clipping`; snapshot `clip`; `ClipTests`. The load-bearing renderer rules (the vertex-buffer ring, the SDF tagged union, coverage models, the present/float pipeline) live in *Shaders & the Metal back end* + *Rendering performance* above. `Docs/Drawing/Drawing.md`.

- **Variable-width strokes** (`Sources/Ollin/Drawing/StrokeProfile.swift` + the expander in `Core/Drawer+Tessellation.swift`). **`strokeProfile(_:)`** / `noStrokeProfile()` is stack state shaping width along a path: the **`StrokeProfile`** value type (`.uniform` / `.taper(start:end:)` / `.ramp(from:to:)` / `.nib(angle:thinness:)` / `.values(_:)` + the closure forms) returns a **multiplier on `strokeWeight`**, read at path fraction 0…1 plus the local direction (what `.nib` uses). The fringe expander carries a **half-width per path vertex** (`Drawer.strokeHalfWidths`), so a segment quad becomes a trapezoid, exact for a linear ramp along a straight run. **Load-bearing:** width is sampled **per path vertex, not per segment**, which is *why* joins stay simple (both segments at a corner agree on the width there, so a corner is the constant-width corner problem at the local width) and why joins/caps just read their own vertex's width; a non-uniform profile **subdivides like a gradient does** (`maxLength: 12`) since the profile can only follow the points it's given; **`.uniform` is the untouched path** (`isUniform` gates the expander, the outline builder, and the vector recorder) and the coverage ramp is the shipped one either way, so a profiled and an unprofiled stroke of the same width lay down the same ink and every existing snapshot is unchanged; the **analytic SDF shapes carry one width by construction** and ignore a profile with a one-time `noteOnce`. **Vector export stays true:** `svgRecord` routes a profiled stroke to `variableStrokeOutline` (trapezoids + join wedges + caps, each wound the same way into **one nonzero-wound `Shape`**, so the union needs no boolean work), recorded as a filled path rather than a `stroke-width`; PDF replays the same commands. Example `Shapes/StrokeProfiles`; snapshot `stroke-profiles`; `StrokeProfileTests` (the family, arc-length sampling, outline winding + area, rendered taper falloff). `Docs/Drawing/Drawing.md#strokeProfile`.

- **Stroke dynamics** (`Sources/Ollin/Drawing/StrokeDynamics.swift` + `StrokeMark.swift`). The width-profile sibling driven by *how a mark is made* rather than where you are along it: a **`StrokeMark`** records the pointer (`record(_:dt:pressure:)`, sugar `Sketch.record(into:)`) measuring speed and press force, runs both through a **`StrokeDynamics`** (one **`StrokeResponse`** per axis over a **`StrokeInput`** of speed/pressure/direction/distance), and keeps the resulting width + opacity *at* each point; `drawMark(_:)` strokes it. **Load-bearing:** a profile can't express this because a mark in progress has no length to take a fraction of, and a recorded hand's points are **not evenly spaced** (dense where it slowed), so `.values(_:)` genuinely doesn't fit and `StrokeProfile.values(_:at:)` (values at *given* fractions) is the general form `drawMark` builds; speed is measured over **real elapsed time** with skipped sub-`minimumSpacing` frames **banking** their dt (frame-rate independence, pinned by `recordingIsFrameRateIndependent`), smoothed by a `OneEuroFilter` per axis fed a **scaled** signal (a px/s speed runs in the hundreds, and the filter's `beta` is in the signal's units); the **two axes are separate and each defaults to `.unchanged`** (a shared preset with per-axis defaults silently drove both, the bug the first probe caught); the first point is **held until a second gives it a heading**; the ambient `strokeProfile` **multiplies** with the mark's widths; the mark's opacity rides the existing **per-vertex color alpha** via an internal `strokeOpacityShape` (`.uniform` default → every other stroke byte-identical, and a `.uniform` mark is byte-identical to `drawPolyline`). **Pressure is plumbed** through the one platform-neutral input surface (`Sketch.pressure` / `pressureIsAvailable`, `OllinMTKView.pressureChange` + `NSPressureConfiguration(.primaryGeneric)` so no force-click fires mid-stroke; capability from `NSEvent.associatedEventsMask`, which is why `pressureIsAvailable` is only true after the first press), so it reaches standalone + gallery + OllinLive. **Vector export** keeps width exactly (the profiled filled-outline path) and **flattens opacity to its length-weighted average** with a `noteOnce` (one fill, one alpha per path; free for the one-pen plotter case). Example `Shapes/Brushwork` (class renamed off `StrokeDynamics` to dodge the `SlitScan` shadowing trap); `StrokeDynamicsTests` (mapping, measurement, arc-fraction placement, three rendered probes, no pixel snapshot since dynamics is interactive). `Docs/Drawing/Marks.md`.

- **Brushes** (`Sources/Ollin/Drawing/Brush.swift` + `Core/Drawer+Brush.swift`). The third mark axis beside the width profile and the recorded mark: **`strokeBrush(_:)`** / `noStrokeBrush()` is stack state that **replaces the ribbon with a row of stamps**. The **`Brush`** value type carries a `tip` (`.circle` / `.square` / `.shape(Shape)` / `.image(Image)`), `spacing`, `sizeJitter`, `angle` (`.followPath` / `.fixed` / `.random`), `angleJitter`, `scatter`, `count`, `opacityJitter`, `seed`, plus the `.round` / `.chisel()` / `.spray()` / `.scatter()` presets; size comes from **`strokeWeight`** and color from **`stroke`**, so a brush is texture only. **Load-bearing:** every distance is a **fraction of the stamp's own size**, which is what keeps a brush's texture constant across weights (pinned by `spacingScalesWithTheStampNotTheCanvas`); stamps are placed **by arc length, not per vertex** (a recorded mark's points bunch where the hand slowed, and a brush must not bunch with them); randomness is a **pure function of stamp index + `seed`** via a private `SplitMix`, **never the sketch's `rng`** (the halton/sobol rule), so adding a brush cannot shift a sketch's other rolls; the ambient `strokeProfile` / `strokeOpacityShape` are read per stamp and **multiply**, and the step is taken at the *local* size so a tapering mark crowds rather than going gappy; a stamp draws through the **ordinary draw methods** (`drawCircle` / `drawOrientedBox` / `drawShape` / `drawImage`) with fill/stroke/brush swapped and restored, so vector export, symmetry, clipping, and retained batches all work with no knowledge of brushes (and recursion is impossible); **`strokedAsBrush(_:closed:)` is called ahead of every stroke entry point's `svgRecorder` short-circuit** (`drawLine` / `drawBezier` / `drawPolyline` / `drawMark` / `drawArc`, and `drawShape` / `drawPolygon` which re-enter fill-only then stamp) or the vector path would silently export the bare path instead of the stamps — the `appendFringeStroke` hook is the catch-all for internal callers; a brushed `drawMark` **keeps** its varying opacity in vector export (each stamp is its own shape) where the ribbon must flatten it; opacity over a **gradient** stroke is the one thing a brush cannot vary (it would mean rewriting the ramp) and notes once, as do the analytic SDF outlines (the `strokeProfile` precedent). `Brush` is **not `Sendable`/`Equatable`** (an `Image` tip is neither). Example `Shapes/Brushes`; snapshot `brushes`; `BrushTests`. `Docs/Drawing/Marks.md#brushes`.

- **Retained batches** (`Sources/Ollin/Drawing/Batch.swift` + `encodeRetained` in `MetalRenderer+Effects.swift`). **`makeBatch { }`** records a drawing once into a **`Batch`** (persistent, made in `setup()` and held — the `Feedback` shape) and **`drawBatch(_:)`** replays it from the handle's own once-written per-device `MTLBuffer`s — the escape from per-frame re-recording for *static heavy* content (measured M2 1080² release: 150k circles ~13.5 ms/frame per-frame vs ~0.003 ms replayed; example knob flips the two live). **Load-bearing:** recording is an **array swap** on the `Drawer` (per-frame arrays + batch list + gradient-row table), so recorded runs index the handle's arrays and baked gradient rows are **handle-relative** — the replay binds the batch's **own strip texture**, never the frame's; the body runs style-scoped (`pushState`/`popState`) from an **identity CTM** with the target/clip stacks swapped out, and the `.retained` reference batch supplies the draw-time context (CTM + target + clip level + `depth(at:)`); the draw-time CTM rides **flag-gated `Uniforms` fields** every 2D vertex shader applies to its *output position only* (flag 0 outside a replay → ordinary paths byte-identical, snapshot suite unchanged; identity replay byte-identical to the per-frame drawing, pinned by `BatchRenderTests`; SDF analytic AA stays exact under any replay transform because shape-local interpolants never change); **unsupported content is gated at its funnel** with a one-time note (meshes + 3D fields would silently lose shadows/reflections; particles; layer blocks; clipping; `background`; nesting) — never dropped after recording, which would corrupt the next-run count derivation; point clouds record camera-less and replay through the frame's camera (the 2D replay CTM doesn't touch them); **draw-time symmetry does not fold a replay** (record folds inside the block); vector export flips **`OllinApp.isVectorExporting` for the whole `recordVectorFrame` drive** (setup + warmup included) so `makeBatch` captures `SVGCommand`s instead and `drawBatch` splices them under the composed CTM — SVG/PDF see every stamp. Example `Rendering/RetainedBatch`; snapshot `retained-batch`; `BatchTests`/`BatchRenderTests`. Mechanism: [`ARCHITECTURE.md`](ARCHITECTURE.md) § *Retained batches*. `Docs/Drawing/Batches.md`.

- **SDF combinators** (`Sources/Ollin/Drawing/SDF.swift` + `Renderer/ShaderCombinator.metal`; 3D in `Sources/Ollin/Drawing/SDF3D.swift` + `Renderer/ShaderRaymarch.metal`). Compose signed-distance *fields* so shapes **merge** instead of stack — the field-combining axis the per-instance SDF path can't express. The typed **`SDF`** value type (leaf region shapes + `.union`/`.smoothUnion(_:k:)`/`.subtract`/`.smoothSubtract`/`.intersect`/`.smoothIntersect`/`.morph`, the hg_sdf joint + detailing ops (the chamfer/stairs/columns trios + `engrave`/`groove`/`tongue`/`pipe` — crisp nearer-side/body color; the detailing ops are value-type-only (the morph precedent), columns gets block forms; they assume frankly-crossing surfaces — near-parallel faces within the radius echo the pattern, the documented envelope), and `.rounded`/`.onion`/`.at`/`.rotated`/`.scaled`/`.stretched`/`.colored`/`.mirrored`/`.repeated`/`.repeatedRadially`) draws via `drawSDF(_:)` honoring the current fill/stroke/CTM, the stroke tracing the *merged* outline; the scoped **block forms** (`smoothUnion(k:){ … }` + hard/mirror/repeat variants, and the stateful **`sculpt { }`** block whose `add()`/`carve()`/`blend(_:)` verbs mutate per-child mode/melt mid-block — built for the live-coding loop) capture bare region draws at `Drawer.appendSDF` as leaves, reaching **every** fillable region shape. Rendering is an **"SDF VM"**: the tree flattens (CPU) to a flat `SDFNode` program (own `SDFGroupInstance` + `.sdfGroup` pipeline *beside* the per-instance `SDFInstance` path, which is full at 144) walked per pixel with two fixed-depth stacks (cap 16); smooth ops **also lerp the operands' colors** (the melt look). **Load-bearing invariants:** **leaves carry no transform** (all positioning is XFORM nodes → method-chain order exact: `a.at(p).repeated()` tiles the moved shape, `a.repeated().at(p)` shifts the tiling); the leaf distance **`ollin_sdf_distance`** is **kept in sync by hand** with `ollin_sdf_fragment`'s switch (a new region shape goes in both; the per-shape switch stays untouched so single-shape snapshots are byte-identical); **polar/radial repeat is the shared sel-5 XFORM** (2D fold around origin, 3D around an `axis`), the **single-evaluation** kind (wedge-symmetric content exact, else a seam); **region shapes only** (lines/open-arcs/Béziers skipped with a one-time log); a field past the caps (depth 16, ~256 nodes) is logged + skipped, never mis-drawn; the buffers thread all 9 `encode` sites. **2D gradient paint** samples in **field space** (`in.field`; along-path falls back to solid; a solid-leaf field is byte-identical). **Per-axis sizing:** `stretched(x:y:[z:])` (iq `opElongate`, an **exact** SDF, the preferred tool) vs `scaled(x:y:[z:])` (true non-uniform, only a **conservative bound** — rescales by the **min** scale component for a safe Lipschitz march, so blends distort under strong anisotropy). The **raymarched 3D sibling** — **`SDF3D`** + **`drawSDF3D(_:)`**: the same ops over the 3D leaves (sphere/box/roundBox/torus/capsule/cylinder/cone/octahedron/ellipsoid/plane + line-between-points/hexPrism/pyramid/cappedTorus/link) plus the sculpting distortions `.twisted`/`.bent` and `.displaced`/`.roughened` (every distortion/displacement is a distance *bound*, so flatten attaches a conservative Lipschitz rescale that keeps the march hole-free), **sphere-traced** through the active camera and **depth-composited with the rasterized meshes** (a parallel path: own `SDFNode3D` + `.raymarch` pipeline). **Load-bearing:** the march runs in **world space** (the 4-tap gradient normal + written `[[depth(any)]]` come out world-space with no normal matrix); one **fullscreen triangle per field** bounded by its world AABB (a miss bails O(1)); shading **reuses `meshLitColor` unchanged**, **honors `material(_:)` per batch** (`ensureSDF3DBatch` records the finish — the generic `ensureBatch` records none, the bug that left field materials silently ignored) and **adds the same IBL ambient the mesh fragments add** (incl. the traced-reflection swap under `rtReflections`; gated on `iblEnabled` → byte-identical with no environment; `resolveFieldLighting` carries the same setup so the half-res tiers light identically). The infinite **plane** is the one *unbounded* leaf (flattener marks the field `unbounded`, marches the near..far span; value-type-only). The block form reaches 3D too — SDF-able mesh builders capture leaves via `Drawer.drawMeshPrimitive`, positioned by the model matrix decomposed to translate+rotate+uniform-scale (the only similarity an SDF respects); a non-primitive `Mesh` is ignored (one-time log). **Screen-space gradient paint** (3D, by each hit's projected screen position); **analytic silhouette AA** (the ray's closest approach vs the pixel cone). **Shadows** (gated on `castShadows()`, byte-identical off): self-shadows via iq's **standard** `min(k·h/t)` penumbra — *not* the previous-step refinement, which divides by zero on a receding ray and false-shadows lit faces — plus the full **field↔mesh cast/receive matrix across every light type**; the per-receiver-pixel point-light cast is trimmed by a conservative AABB early-out + a half-res field-shadow pass. **Quality dial** (`raymarchQuality`/`raymarchResolution`): a field's cost is **bound to pixel count, not step count**, so the dial scales internal **resolution** via a reduced-res pre-pass (point-sampled depth keeps the silhouette crisp) whose fraction is a **coverage-adaptive budget, not a fixed downscale** (`min(1, fraction/√coverage)` — a dollied-out field traces at full res, the zoom-out-blur fix); the automatic tier is **live-preview only** (export resolves `.detail`, full-res → default-tier snapshots byte-identical; an *explicit* fraction/tier is honored on export, adaptively); rides the shared **`RenderQuality`** model with the other three quality knobs. `ollin_sdf3d_eval`'s switch stays in sync with `SDF3DShape`. Examples `Shapes/Combinators*` (2D) + the `3D/Raymarched*` set; snapshots `sdf-combinators` + its `-gradient`/`-stretch`/`-joinery`/`-detailing`/`-sculpt` and `-3d*` variants. Full mechanism, rationale, and the field-shadow matrix: [`ARCHITECTURE.md`](ARCHITECTURE.md) § *SDF combinators*. `Docs/Drawing/Combinators.md`.

- **User-supplied shaders** (`Sources/Ollin/Effects/Shader.swift` + the wrapper/compile/error path in `MetalRenderer`). A sketch writes its own fragment shader and runs it through the effect graph. The contract is **Ollin-native, not raw Metal**: define `float4 shade(float2 uv, ShaderInfo info)` (uv 0…1 top-left, return straight sRGB — input layers are read back as straight sRGB too) and Ollin generates the surrounding fragment + vertex. The **`Shader`** value type (Sendable) carries inline-`String` or `resource:in:` source, `params` (up to 32 floats, read via `param(info, i)`), and a **`Modules`** opt-in set; routing **reuses the effect-graph enums** — a `.shader(Shader)` case on `Generator`/`Filter`/`Combine.Kind`, so input count *is* the variant (`generate(.shader(s))` 0, `layer.filtered(.shader(s))` 1, `a.combined(with: b, .shader(s))` 2) — with no new render-graph plumbing. Each shader compiles **once per composed-source hash** (its own `makeLibrary(source:)`; failed compiles are cached too, so a broken shader doesn't retry every frame; all caches clear on a framework-shader reload). **Friendly errors are the headline:** `Shader.init` captures its call site and the wrapper emits a `#line` directive, so Metal diagnostics land at the **real, IDE-clickable** sketch line (`ShaderErrorTests` pins the off-by-one semantics for no-newline literals); errors surface via stderr (once per hash) *and* the pull-model `onUserShaderError` channel into the OllinLive overlay — a shader error and a Swift compile error never clear each other. The **`OllinShaderLib`** helper segment (color/hash/noise/sdf/domain + `palette`/OKLab + the once-compute-only helpers) is **one source of truth used three ways**: the framework's own segments build on it, it splices into every user shader, and `composeComputeSource` splices it into every compute kernel (one helper set, `hashNM` names, everywhere); the 2D `sd*` distance catalog lives in its `sdf` section, shared with `ShaderShapes`; `using: [.noise, .sdf]` keeps only marked sections — a *compile-time* lever, default `.all`. **Hot-reload** covers both forms in OllinLive: an inline shader reloads with the sketch; a `.metal` *resource* shader reloads on its own with **no swiftc pass** (`Shader(resource:in:)` stores the bundle-resolved path; a watched-`.metal` change routes to `SketchRunner.invalidateUserShaders()`); a framework-segment `.metal` still routes to the full library reload (dispatch by path). Examples `Shaders/HelloShader` (a Toby-Howard-homage plasma generator) / `ShaderFilter` / `ShaderBlend` / `ShaderFile` (the hot-reload demo); snapshot `user-shader`. Mechanism: [`ARCHITECTURE.md`](ARCHITECTURE.md) § *User-supplied shaders*. `Docs/Shaders/Shaders.md` + the per-function `Docs/Shaders/ShaderLibrary.md`.

- **Visual chains** (`Sources/Ollin/Effects/Visual.swift` + the `visual` section of `OllinShaderLib.metal`). The fluent composition tier over the effect substrate: **`Visual`** is a value type (struct over an `indirect enum Node`, the `SDF` shape) recording a chain of per-pixel ops in five families — sources (`.oscillator`/`.noise`/`.voronoi`/`.shape`/`.gradient`/`.solid`/`.layer`), coordinate warps (`.rotated`/`.scaled`/`.pixelated`/`.repeated`/`.kaleidoscope`/`.scrolled`), color adjustments (`.brightness`/`.contrast`/`.saturation`/`.inverted`/`.posterized`/`.thresholded`/`.luma`/`.hueShifted`/`.tinted`/`.colorCycled`/`.channel`), combines (`.blended(with:_:amount:)` over the typed `BlendMode`, `.mixed`/`.differenced`/`.masked`), and modulations (driver overloads of the warps: `.displaced(by:)`, `.rotated(by:)`, `.scaled(by:)`, `.pixelated(by:)`, `.kaleidoscope(by:)` — one chain's color perturbs another's sampling coordinate). `compile()` walks the tree once and emits a `shade(uv, info)` body over `ollin_vis_*` library functions; the whole chain (drivers included) is **one fragment shader, one pass**, run through the user-shader path (`Shader` + the compile-once source-hash cache). **Load-bearing:** the generated source depends on the chain's *structure only* — every numeric argument becomes a params-buffer slot in traversal order (`VisualTests` pins this), so a chain rebuilt each `draw()` hits the cached pipeline and animated values/`@Param` knobs never recompile (structural selectors — blend mode, channel — are spelled as literals *by design*); the params buffer is **16 rows / 64 floats** (`OLLIN_SHADER_PARAM_ROWS`, raised from 8 — `paddedParams` and the wrapper both read the macro), overflow bakes literals with a one-time note; the codegen's fresh-variable counter **starts at 1** (`st0` is the root the body declares — 0 collides); `.layer` leaves dedupe by target identity and cap at the wrapper's **2 textures** (routing by count: 0 → `generate`, 1 → `.filtered`, 2 → `.combined`; extras sample black + one-time note); `.layer(_ feedback:)` reads the previous frame (video feedback); `BlendMode.visualIndex` is kept in step with `ollin_vis_blend`'s switch; ops aspect-correct *inside* the function (coordinates stay 0…1 so `fract` wraps compose), colors are straight sRGB per the user-shader contract, and `.noise` is signed so displacement wobbles about zero; `Modules.visual` splices the ops into every user shader (dependency `visual → hash+noise` in `filterLibModules`). Example `Shaders/VisualSynth`; snapshot `visual-chain` (all five families + a layer read); `Docs/Shaders/Visuals.md` + the module's function table in `Docs/Shaders/ShaderLibrary.md`.

- **Layered effects** (the substrate; `Sources/Ollin/Effects/`). Draw into off-screen layers, filter them on the GPU, composite back with blend modes (the OPENRNDR `RenderTarget`/`Filter`/`compose` model): `renderTarget(scale:)` → an off-screen `rgba16Float` `RenderTarget`; `withTarget(_:) {}` redirects drawing into it (scoped like `withState`; `background(_:)` inside clears *that* layer); `target.image` composites it back; `target.filtered(_:)` → a new layer; `postProcess(_:)` filters the whole frame. **`Filter`** is a ~55-entry catalog in six families — blur/glow, color/tone, stylize/optical, retro, uv-warp distortion, and the **design filters** (`.flutedGlass`/`.water`/`.paperTexture`/`.melt` transform the layer; `.liquidMetal`/`.heatmap`/`.gemSmoke` read its **alpha shape** — draw a shape, filter it); **`.melt`** is the luminance melt (one displacement vector both warps the field's domain and shifts the layer sample — keep them shared, that coupling *is* the recipe; sampled brightness mixes into the field before the 4-stop sRGB ramp; the sway sines are non-commensurate, so it never perfect-loops — example `Images/LuminanceMelt`, snapshot `luminance-melt`); full per-filter list in `Docs/Drawing/Effects.md`; new built-ins are written from the published technique, credited in `ATTRIBUTION.md`, never in `.swift`. **`Generator`** + `generate(_:)` fills a procedural pattern into a target: the four basics (`.noise` takes a `warp:` domain-warp knob — 0 skips the warp path so it stays byte-identical), the **`.cellular`** Worley source (`.cells`/`.borders`/`.mosaic` styles; feature points wander sinusoidally on hashed orbits, so `phase` is periodic over 2π and a whole lap loops — example `Effects/Cellular`, snapshot `noise-toolkit`), the **design-pattern set** (cross-read against Paper Shaders, written from the canonical techniques, per-technique credits in `ATTRIBUTION.md`), the **pattern fields**, and the **escape-time fractals**; `generate(_:width:height:)` fills an explicit-size layer so patterns compose per-layer instead of squashed full-canvas fills (the Shader and Visual `generate` forms have the same overloads). **Feedback** — `feedback(scale:)` → a *persistent* `Feedback` layer (made once in `setup()` and held — its identity carries the ping-pong state across frames) + `withFeedback(_:) { prev in … }`. **Simulation fields** — `simField(_:scale:)` → a persistent `SimField` running a `Sim` (`.reactionDiffusion(feed:kill:)` Gray-Scott / `.gameOfLife` Conway / `.lenia(...)` continuous CA / `.ripples(speed:damping:)` wave-equation pool / `.sandpile(pour:topplings:)` Bak-Tang-Wiesenfeld / `.fluid(...)` real-time fluid / `.multiScaleTuring(scales:seed:)` McCabe) that you seed/force by drawing into it (`withField(_:_:)`, `force:`); `SimField.image` is the raw state, `.filtered(_:)` recolors it through the `Filter` catalog, and **reading either registers the field so it steps** (`Drawer.ensureFieldSteps`) — a sim only evolves while its layer is one of the frame's render targets, which used to mean a `withField` block was mandatory, and a self-organizing sim that needs no seeding would otherwise sit frozen and read black with no hint why (an empty-block wart the Turing work surfaced; existing sketches register via `withField` first, so target order is unchanged). **Depth-capturing targets** — a target a 3D scene is drawn into resolves a sampleable depth buffer; `target.depth` is a gray layer (0 near … 1 far) that feeds `.defocus` (set the camera `near`/`far` to bracket the scene); two-tier opt-in so *2D never pays*. **Compose DSL** (`Effects/Compose.swift`) — `compose { layer { … }.post(_:).blend(_:).scale(_:) … }`, composited bottom-to-top in declared order; pure sugar over the substrate. **Multi-input combine** (`Effects/Combine.swift`) — `base.combined(with: aux, op)` with six ops: `.mask` / `.displace` / `.mix` / `.defocus` (near/far-separated bokeh-gather depth of field; artifact history in [[ollin-depth-of-field-gather-artifacts]]) / `.ambientOcclusion` (SSAO over depth + the mesh-normal G-buffer) / `.screenSpaceReflections` (SSR: trace → resolve → temporal → composite); `aside { }` + `.masked(by:)` / `.displaced(by:amount:)` / `.mixed(with:amount:)` / `.defocused(by:)` is the combine's `compose` sugar. **Load-bearing invariants (terse — mechanism and why in [`ARCHITECTURE.md`](ARCHITECTURE.md) § *Layered-effects substrate* + § *Screen-space combine effects*):** the `Drawer` stays a pure recorder and the renderer resolves the deferred graph at `render()`/`image(of:)` time, entirely GPU-resident (layers are attachments, filter inputs sampler binds, never a CPU round-trip), premultiplied linear, the no-targets path byte-identical; every `GeometryBatch` begin snapshots **all** the `*Start` offsets (omitting one zeroes a *preceding* batch's count — the `SceneDefocus` bug); `withTarget`/`withFeedback`/`withField` push/pop the drawing state around the block (a `blendMode` set inside a layer block must never leak to the canvas — was a real bug); design-pattern fragments animate by an explicit `phase` param (deterministic export, no hidden clock) and blend palettes **internally in sRGB** (mixed in linear they read washed-out pastel — a real bug); `Feedback.filtered(_:)` mirrors `SimField.filtered(_:)`; **`Sim.params` is an array of `SIMD4` rows** the renderer binds after the texel row (a new sim never widens the `runSimulation` call site, it returns more rows), **`SimField.sim` is a public var** re-read every frame (live growth knobs; the evolved state carries), and **Lenia** is the paper-faithful continuous CA (exponential kernel core in up to 3 β-rings, **normalized in-loop by the summed weight**, gaussian growth, clipped dt integration; Orbium defaults R=13/T=10/μ=0.15/σ=0.015) whose dying / labyrinth / rings / maze looks are all real regimes — **seed it densely** (sparse mass starves, the documented envelope, not a bug); **`.ripples`** is the height/velocity interactive-water kernel (state h in r, v in g, both signed; 6 substeps; clamped taps, soft absorbing border rim) whose **per-sim inject is load-bearing**: `Sim.injectFragment` routes it to `ollin_sim_inject_height`, which *adds* the drawn mark's premultiplied luminance to height only — the generic replace-by-alpha inject pins the surface and writes color into the velocity channel (the exact bug that parked the 2026-07-07 attempt, see [[ollin-ripples-sim-parked]]); the **coupling gain `speed` must stay under 2** (clamped to 1, default 0.5): 2 is the CFL boundary where the Nyquist checkerboard sits on a repeated λ=−1 root and grows linearly, settling ~22× the ring amplitude under any damping and shading into glitter — substeps went 3→6 so the ring pace is unchanged (wave speed ∝ √gain); soft radial-gradient dabs are the drop contract (hard discs legitimately ring at every frequency), and the raw `image` is a debug view (shade with `.relight`); **`.sandpile`** is the Bak-Tang-Wiesenfeld toppling CA on the generic single-texture step path (no dedicated pipeline; Dhar's abelian property licenses any parallel schedule, and `SandpileTests` pins the settled pile **cell-for-cell against a sequential CPU reference**, the edge variant verified red against its counterfactual) with five terse invariants: the step is the **k-fold parallel form** `q' = fract(q) + Σ floor(q_n)/4` — topple as many times as a cell can per pass, identical to single toppling in the critical regime but load-bearing at a hot source, where single toppling pools for thousands of passes (a saturated blob's interior is net zero: lose four, receive four back); grains stored in **quarters** (stable cells read 0/¼/½/¾, gradientMap-ready; exact in half float to 2048 grains), state **read from `.r` only** (a luminance dot is off by an ulp and the toppling floor/fract must be exact), boundary **open — reject the position before sampling, never wrap or clamp** (the clamping sampler reads the edge texel back as its own neighbour, a reflecting wall; on a torus sand accumulates until every cell topples forever), and the inject **adds `rint(luma·pour)` whole grains** (an AA fringe pours whole grains or none; nothing erases); `topplings` is `subSteps`, the pacing dial (an avalanche front moves one texel per pass), the **classic relaxed figure is drop-and-relax** (one heavy mark on one frame, then settle — a steady heavy pour keeps a molten super-critical core by design), and `runSimulation` binds **`sim.params` to the inject pass too** (pre-existing injects read none — byte-identical); **`.relight` measures slope over a span, not a texel** (taps on the half-texel grid for a free 2×2 box, gain divided by the span, span growing with resolution past 1080) — slope weights a wavelength by 1/L, so a per-texel Sobel lights few-texel content as steeply as the real relief; smooth relief is preserved to ~1% (`effects-relight` did **not** need re-recording). **Don't soften a finish's specular exponent to chase speckle** — a broad lobe lifts the highlight on *flat* areas too (a `pow(sh,60)` `.liquid` washed the Guide's Organism background several shades lighter), so fix the normal, not the lobe; headless `image(of:frame:)` renders *every* warmup frame when `drawer.usesFeedback` (feedback / sim fields / SSR — else the ping-pong never evolves); fluid keeps its own `fluidSlots` beside `FeedbackSlot` so the single-field sims stay byte-identical; **`.multiScaleTuring`** is McCabe's rule (activator/inhibitor disc averages at several scales, least-disagreement scale fires, renormalize each step) and runs its **own multi-pass pipeline** (`runMultiScaleTuring`) like the fluid while sharing the one-pair `FeedbackSlot` (so flip/prune/repeat are the shipped ones), with four load-bearing pieces, each of which was a real defect first: the field **starts as seeded noise, not a constant** (a flat field is a fixed point of the rule, so `feedbackSlot` grew a `fill:` override beside `restState`); the blur pyramid is **Gaussian (Burt-Adelson binomial), not a 2x2 box** (a square kernel has square preferred directions, so the labyrinth came out rectilinear); a radius **gathers a 9-tap disc two rungs *finer* than the radius match**, never a single tap at the matching rung (that rung holds one sample per feature, so the pattern locks to the lattice, unfixable by any kernel since the information isn't there); and **variation is averaged over a per-scale `variationRadius`** (read at a point, a fine scale's disagreement crosses zero along every contour of its own structure and, since *least* disagreement wins, it claims a dense web of pixels and buries the coarse scales). Two parameter facts are equally load-bearing because renormalization couples the scales: **equal `amount`s across rungs** (the hardest-pushing rung sets the range and squeezes the rest toward mid gray) and a **ladder starting at radius 2** (radius 1 works on single texels and reads as speckle). `TuringScale.maxScales` (6) must equal `OLLIN_TURING_MAX_SCALES`, and `MetalRenderer.turingPyramidLevels` (12) must equal `OLLIN_TURING_LEVELS` (it sizes the texture-array binding; a shallower pyramid repeats its top rung to fill it). Deliberate v1 cut: the winning-scale index is **not** carried in the state (colour-by-scale would muddy a channel the display and the extent reduce both read as gray); `layer(_:)`'s draw closure is `@escaping @_implicitSelfCapture` (without it, bare draws inside `layer { }` need `self.` and the p5 feel breaks); SSAO's and SSR's stabilizer sets each fixed a worse artifact — don't undo any of them ([[ollin-ssao-normal-gbuffer-followup]], [[research-before-implementing]], [[verify-perceptual-temporal-fixes]]); DoF/SSAO/SSR `quality` is a `RenderQuality` tier resolving to a per-GPU budget (DoF: the bokeh *tap* budget via `resolveDofTaps` — M2 1080²: `.default` 192 ≈ 14 ms; **benchmark back-to-back on a cool machine**, single runs drift 50% under thermal load, enough to invert an A/B). **`.defocus` is two passes:** a **pre-pass** reduces the aux depth to `(scatter, depth, receive)` per pixel and the gather reads *that*, so a tap is one sample and no math (~35% faster than decoding depth per tap). The two sizes answer the depth map's **anti-aliased rim** from both sides and are load-bearing: **receive** = neighbourhood **max** (the seam dilation, kills the dotted in-focus ring), **scatter** = neighbourhood **min at radius 2** (kills the hard-edged `maxBlur`-radius halo a *sharp* object leaked; radius 2 not 1, or the gather's bilinear averages half the rim back in). The gather's own rules: both accumulators seed with the **centre texel** (black-seeded near field composites a dark ring at partial coverage), **alpha rides with the colour** (premultiplied layers), a farther tap is **occlusion-clamped to 2× the centre's blur** (else a heavy backdrop dissolves a barely-defocused midground for the full `maxBlur`, the pop that made a rack unwatchable), compositing is **far first, near over it** (`mix(mix(centre, bg, dof), fg, α)`; one `max(dof, α)` mix double-applies the coverage), a pixel **under a near blur lets the bg field gather from inside that blur** and **fg coverage is an area fraction of the *near* blur, not the disc** (`total·(r/maxBlur)²`) — without both, a foreground blurs outward but keeps a razor *inner* edge (a 40% step in one pixel). The snapshots' mean-diff tolerance averages every one of these away (they all live in a band around a silhouette), so **`DefocusTests` pins them behaviorally** — reach for a probe here, not a whole-frame diff. Credits: AsyncGraphics + ofxFX + orx-fx/OPENRNDR + Apple MPS (`ATTRIBUTION.md`, never in `.swift`). `Docs/Drawing/Effects.md`; examples `Effects/*` (Bloom / ColorFilters / BlurFilters / StylizeFilters / RetroFilters / Glitter / Distortion / Patterns / MeshGradient / DesignPatterns / DesignFilters / Feedback / Compose / Aside / Defocus), `Simulation/GrayScott` / `GameOfLife` / `Lenia` / `Ripples` / `Sandpile` / `Fluid`, `3D/SceneDefocus` / `AmbientOcclusion` / `ScreenSpaceReflections`; snapshots `effects-*` + `lenia` + `ripples` + `sandpile` + `mesh-gradient` / `design-patterns` / `design-filters` / `scene-defocus-3d` / `ssao-3d` / `ssr-3d`. `RipplesTests` pins the wave sim behaviorally (rest stays rest, rings expand, channels decay).

- **Compute & GPU particles** (a *core* capability, GPU/Metal in `Ollin`). Runtime-compiled MSL kernels over persistent buffers/textures feeding the instanced render path — 10⁶ particles update + draw GPU-side, the sandpainting depth-of-field at scale. Types in `Sources/Ollin/Compute/`: `ComputeKernel` / `ComputeBuffer` / `PingPong` / `ComputeParams` / `Particles` (buffer half) and `ComputeTexture` / `PingPongTexture` / `Simulation` (texture half — reaction-diffusion / cellular automata / image kernels drawn as an `Image`). Facade: `compute(_:over:)` / `compute(_:reading:writing:)` / `compute(_:writing:)` / `updateParticles` / `updateSimulation` / `drawParticles`. Kernels inline or in a `.metal` file (`ComputeKernel(resource:in:)`); shared types + the full `OllinShaderLib` (the same hash/noise/curl/disc + palette/OKLab/`sd*` helpers fragment shaders get, `hash12` not the old `hash21`) auto-splice. **Gotchas:** ping-pong (not in-place) for any buffer the render path also reads; a file using `OllinParticle` must `import COllinShaders`; the `COllinShaders` clang module is stale-cache-prone (`swift package clean` after adding a shared struct); kernel compile errors are swallowed → missing particles, not a crash; headless export runs intermediate sim steps via `stepCompute`; seed once via a flag (`frameCount` is 1 on the first `draw()`). `Docs/Shaders/Compute.md`; examples `Compute/CurlField`, `Compute/ReactionDiffusion`, `Rendering/DepthOfField`.

- **GPU artificial life** (`Sources/Ollin/Compute/SpatialHash.swift` + `ParticleLife.swift` / `PPS.swift` / `Physarum.swift`). The compute path's first *cooperative* primitive + three emergent-behavior sims on it. **`SpatialHash`** (public) is a GPU uniform-grid fixed-radius neighbor search (Hoetzlein **counting sort**: clear → count → serial exclusive-prefix-sum → scatter, the four kernels sharing one source); it's the enabler `ParticleLife`/`PPS` query and a sketch builds its own sim on via the `neighborStep(_:over:reading:writing:)` facade + the `OLLIN_FOR_NEIGHBORS` macro. **Load-bearing:** the shared struct is **`OllinSpatialGrid`** in `OllinShaderTypes.h` (stride 32; **distinct from the pre-existing `OllinGridParams`**, the 3D ground-grid uniform, whose name it must not reuse); the grid/neighbor helpers (`ollin_grid_cell`, `ollin_torus_delta`, the `OLLIN_FOR_NEIGHBORS`/`OLLIN_END_NEIGHBORS` macro pair) live **unmarked** at the tail of `OllinShaderLib.metal` so `filterLibModules` always keeps them (compute-only; never bloats a user fragment shader's `using:` subset); cell edge = query **radius** over a **toroidal** domain so all in-radius neighbors sit in the wrapped **3×3** block, which needs `gridW`/`gridH` **≥ 3** (init clamps, so `worldSize` may round up past `bounds`) or the wrap double-counts a cell; **atomics** power the count/scatter (`atomic_fetch_add`) and never appeared in this codebase before, and the **serial compute encoder** (default `makeComputeCommandEncoder()`) orders the build passes with no explicit barriers (same as `Simulation`'s substeps); buffer-index contract `0/1` particles read/write, `2` sortedIndices, `3` cellStart, `4` cellCount, `5` grid, `6+` sim-specific (`ParticleLife`'s matrix at 6); `nil` gaps in a `RecordedDispatch.buffers` array bind nothing (index = array position). All three sims reuse **`OllinParticle`** (no new struct): type/heading ride the free `seedA` scratch; each is a `@MainActor final class` recording its step by appending `RecordedDispatch`es directly (the `Particles.recordStep` model, **not** the 1-2-buffer `compute()` facade). `Physarum` uses **no hash** (trail path): agent buffer + `r32Float` **`PingPongTexture`** trail + an **atomic `UInt32` deposit grid** (fixed-point, avoids Metal's undefined concurrent-`texture.write` race), stepped clear→agents(sense/steer/move/deposit)→diffuse+decay→colorize; draw via `.image`. **Load-bearing anti-lattice-artifact trio:** **bilinear** trail sensing + a **per-step motor noise** (heading jitter) + a **1-2-1 Gaussian** diffuse (not a box mean); without them the faithful-Jones steering (which keeps heading exactly when all three sensors read equal, i.e. in the empty region an agent forages into) sends escaping agents ballistically straight, and those coherent parallel escapes self-reinforce on the square lattice into **cardinal-axis spurs** (the motor noise is the decisive one, turning the ballistic escape into an even-spreading wander). **Reproducibility caveat (load-bearing):** the counting-sort **scatter order within a cell is GPU-atomic-race-ordered** and the sims are chaotic, so there's **no frame-exact export guarantee and no pixel snapshot** (matching the compute path); correctness is pinned instead by `SpatialHashTests` (`hasMetal`-gated: counts sum, exact prefix sum, `sortedIndices` a permutation, per-cell bucketing, and a neighbor **set** matching brute force — all order-independent). Facade factories `spatialHash(in:radius:count:)` / `particleLife(count:kinds:radius:…)` / `primordialParticles(count:radius:…)` / `physarum(agents:resolution:…)` default `bounds` to the canvas and `seed` to `variation`. `Docs/Simulation/ArtificialLife.md` + `Docs/Shaders/Compute.md#spatialhash`; examples `Simulation/ParticleLife` / `PrimordialParticles` / `Physarum` + `Compute/NeighborSearch` (the raw primitive, the one example that `import COllinShaders` for `OllinParticle`, wired via the extended `example(…, dependencies:)`).

- **GPU fluids & soft bodies** (`Sources/Ollin/Compute/ParticleFluid.swift` + `SoftBodies.swift`). Particle dynamics in a walled box over `SpatialHash`, reusing `OllinParticle`; shared header structs `OllinSPHParams` (stride 112) / `OllinSoftBody` (32) / `OllinSoftBodyParams` (64). **`ParticleFluid`** is 2D SPH (Müller 2003 symmetrized EOS pressure + Clavet 2005 near-density/near-pressure for anti-clumping and emergent surface tension; quadratic/cubic spike kernels normalized for 2D by integration): per substep, predict (gravity + one-frame `pull`/`push`) → rebuild hash over **predicted** positions (fixed 1/120 s look-ahead) → density pass → force/integrate pass, all **pure gathers** off one snapshot so pair forces cancel equal-and-opposite with no atomics. **Load-bearing:** densities are normalized to the *seeded lattice* (`restDensity` 1 = as packed, knobs survive a radius change); the box is **walls, not a torus** — kernels use plain deltas and the `r < h` test rejects the wrapped 3×3's cross-wall candidates (safe because the hash world rounds down by less than one cell); the velocity clamp (0.45·h per substep) makes tunneling impossible, so a hitch can't detonate the sim; wall under-density compression is countered by near-pressure (raising `nearStiffness`, not clamping pressure, is the intended lever). **`SoftBodies`** is meshless shape matching (Müller 2005): a serial one-thread-per-body reduce (the prefix-sum trade) fits centroid + closed-form 2D rotation, then particles steer to rotated rest offsets at `squish` (converted per substep, feel is frame-rate-independent); contacts are **cross-body only** and inelastic (overlap repulsion + each side sheds half the approach speed). **Load-bearing:** rest offsets are taken about the **rest centroid** (the lattice mean, not the disk center) — anything else leaves `Σ(goal − x) ≠ 0` and the body silently self-thrusts (a real bug: blobs hovered and pinned into corners; pinned by `FluidDynamicsTests.isolatedBodyConservesMomentum`); spawns separate via `relaxCircles` before particle fill (an overlapped spawn explodes). Both run fixed substeps against a clamped frame clock (≤ 1/30 s), pack params at buffer 11 per substep, and follow the artificial-life reproducibility rule (**no pixel snapshots**; behavioral `FluidDynamicsTests`). Facades `particleFluid(count:radius:)` / `softBodies(count:radius:)` + `updateParticleFluid` / `updateSoftBodies` / `drawParticles` overloads default `bounds` to the canvas and `seed` to `variation`. Examples `Simulation/ParticleFluid` (dam break, drag to grab) + `Simulation/SoftBodies` (jelly pile, drag to knead). `Docs/Simulation/Fluids.md`.

- **Text.** Three font kinds over one `drawText` / `textToShapes` / metrics surface (`textFont` / `textSize` / `textAlign` / `textWidth`): `BitmapFont` (default Cozette, BDF + Playdate `.fnt`), `OutlineFont` (Core Text `.ttf`/`.otf`, each glyph a `Shape` taking fill *and* stroke, variable-font axes; default `.systemMedium`), `StrokeFont` (Hershey `.jhf`, plotter polylines). `textMode(.atlas)` flips outline text to an SDF glyph atlas (`GlyphAtlas`, exact EDT, fourth glyph pipeline). Per-glyph / on-path / box-wrap / metrics helpers; standard notices `drawStatus` / `drawCaption`. **Gotchas:** glyph `Shape`s must be `.nonZero` (even-odd cuts `e`/`a`/`g` terminals); San-Francisco overlapping sub-contours need a Clipper2 union clean at reference scale 1024 then RDP-simplify (`OutlineFont.simplifyClosed`) per render size, or a per-frame fps regression; warp via bounded `signedNoise`, not raw `curlNoise`; `textToShapes` returns RAW uneven Core Text vertices (`resampled(spacing:)` respaces them evenly for dot/jitter effects); bare `textFont(.builtin)` is ambiguous — use the typed form. `Docs/Drawing/Text.md`.

- **Images.** `Image` value type (ImageIO decode — PNG / JPEG / HEIC / TIFF / GIF — from path / url / data / resource / CGImage; `loadImage` sugar), drawn by `drawImage` on the textured-quad pipeline in call-ordered batches; lazy device texture cached on the `Image`, premultiplied, loaded `.SRGB:true`. `tint(_:)` / `noTint()` stack state. Pixel get/set via the `Image[x, y]` subscript (premultiplied, top-left origin); blank `Image(width:height:color:)` + bulk `Image(width:height:premultipliedRGBA:)`. **Gotchas:** `MTKTextureLoader` honors `.SRGB` only for ImageIO-backed CGImages — a context-made CGImage washes out lighter, so `Image.texture(for:)` self-heals; don't add a y-flip to the pixel buffer (it's already top-down). `Docs/Drawing/Images.md`.

- **Image as input** (glyph mosaic `Sources/Ollin/Text/GlyphMosaic.swift`, pixel sorting `Image/PixelSort.swift`, single line `Geometry/SingleLine.swift`, slit scan `Image/SlitScan.swift`, halftone `Image/Halftone.swift`). Five CPU renderings that turn a picture into something else, all following the Dither bulk-pixel precedent (`premultipliedPixels()` in, `Image(width:height:premultipliedRGBA:)` out) and all deterministic. **Glyph mosaic** — `drawGlyphMosaic(_:columns:characters:in:inverted:colored:glyphScale:)` + the data form `glyphMosaic(of:)` (`[GlyphMosaicCell]`): brightness → glyph by **measured ink coverage** in the active font (bitmap lit-pixel count / outline signed shoelace area / stroke pen travel; ramp cached per (font, characters), normalized to its densest member); **empty floor** below half the sparsest glyph (shadows stay voids); transparency carries no ink in either mapping; `GlyphSet.technical/classic/blocks` curated sets (Cozette covers the whole technical set); `glyphScale` default 0.85 keeps gutters so dense glyphs read as discrete marks (`1` tiles blocks seamlessly) — for the reference "marks" look, use a set that tops out below the solid blocks (the example does). **Pixel sorting** — `Image.pixelSorted(_:by:threshold:reversed:)` (Asendorf's interval model): threshold-bounded runs along rows/columns sorted by brightness/hue/saturation, keys from straight color, alpha rides its pixel, position tie-break → byte-deterministic; the iconic jagged look needs *texture* in the source (run boundaries must vary per line — a clean gradient sorts invisibly, and ascending on an already-dark-to-bright gradient is a near no-op; `reversed: true` is the dramatic direction there). **Single line** — `singleLine(through:closed:)` (pure, rng-free: grid-accelerated NN construction + neighbor-list 2-opt, ring searches use the `(r−1)·bucketSide` stopping bound; open form cuts the longest edge) + the `singleLine(of:points:in:iterations:cutoff:closed:)` sugar over the seeded stipple; `cutoff` rounds bright grays to paper (the Kaplan contrast rule — without it light regions collect a wandering thread). **Slit scan** — `SlitScan` class (`push` + `image(delay:)` closure/map forms): rolling CPU frame ring, delay 0 = newest / 1 = oldest held, first push fixes the size (mismatch/GPU-backed frames skipped with a one-time note), memory is w×h×4×frames so push modest sizes. **Halftone** — `drawHalftone(_:pitch:angle:in:inverted:colored:)` + the data form `halftone(of:)` (`[HalftoneDot]`): classic AM dot screening as real circles (45° default screen); coverage is the per-cell mean of (1 − linear luminance)·alpha over **exact rotated-cell pixel binning**, and the radius **inverts the shared `halftoneCoveredFraction` spot function** — the same function the separations' raster screen thresholds (one source of truth in `Image/Halftone.swift`; `PrintSeparation.halftonePlane` calls it), so tone is area-exact and shadow dots merge into the checkered diamonds; the separations' 2% printable-dot clamp applies (below → no dot, above 98% → the corner-flooding `pitch/√2` cap); `pitch` floors at 2 (dot-count guard); dots emit through `drawCircle`/`drawCircles`, so SVG/PDF export writes true circles (the plotter path). All five need CPU pixels (texture-backed images go through `snapshot()` first; `pixelSorted` returns self, the others note once and return empty/nil). The example sketch class `SlitScan` shadows the type → `Ollin.SlitScan` (the `@SwiftUI.Environment` precedent). Examples `Images/GlyphMosaic` / `PixelSort` / `SingleLine` / `SlitScan` / `Halftone`; snapshots `glyph-mosaic` / `pixel-sort` / `single-line` / `slit-scan` / `halftone`; `GlyphMosaicTests` / `PixelSortTests` / `SingleLineTests` / `SlitScanTests` / `HalftoneTests`. `Docs/Drawing/GlyphMosaic.md`, `Docs/Drawing/PixelSorting.md`, `Docs/Generators/SingleLine.md`, `Docs/Video/SlitScan.md`, `Docs/Drawing/Halftone.md`.

- **Audio** (`OllinAudio` satellite). `AudioAnalyzer` (a rolling last-`fftSize` sample window — Hann + vDSP FFT re-run per chunk, so `waveform` is a true rolling window and chunk size never costs resolution) → `amplitude` / `spectrum` / `waveform` / band queries (`bass` / `mid` / `treble` / `magnitude(in:)`), plus the draw-shaped `bands(_:)` (log-spaced, adaptively normalized, attack/release) and beat detection (`beatCount` / `timeSinceBeat` / `beat` / `beatSensitivity`): spectral flux over **log-compressed magnitudes** (gain cancels in the log difference, so the threshold is loudness-invariant), peak-picked with the **online conditions** — trailing local max + an **additive** margin over a trailing mean (multiplicative-only false-fires on steady material — don't regress it) + a sample-counted refractory — and the whole beat surface on the **sample clock** (`timeSinceBeat` = samples, not wall clock), so detection is deterministic + testable. Sources: `AudioInput` (mic), `AudioPlayer` (file), `Tone` (oscillator), `Soundtrack` (any core `AudioTapSource`, i.e. a playing video's audio). **Headless export is deterministic for `AudioPlayer`** (the `HeadlessVideoReader` sibling): under `isRenderingHeadless` it skips the engine and follows the export clock — a `FrameAdvancing` sample playhead feeds each frame's slice of the decoded buffer to the analyzer (analyzer runs at the *file's* rate there, not the mixer's — hardware-independent), so frame `k` reads the file at `k/fps` and audio-reactive exports reproduce byte-identically; create the player by the end of `setup()`. Mic/`Tone`/`Soundtrack` stay live-only. Example `Audio/Spectrum`; `Docs/Helpers/Audio.md`.

- **OSC** (`OllinOSC` satellite). OSC 1.0 wire format written from spec (`OSCArgument` / `OSCMessage` / `OSCBundle` / `OSCTimeTag` / `OSCPacket`, total `init?(data:)` decode — malformed datagrams return `nil`, never trap) over UDP (`OSCSender` / `OSCReceiver`). Three read surfaces: latest-value cache (`float` / `int` / `string` / `bool`), event drain (`messages()`), and `@Param` binding (`bind("/addr", to: $param, from:)`). Example `Integration/OSCLoopback`; `Docs/Integration/OSC.md`.

- **MIDI** (`OllinMIDI` satellite). MIDI 1.0 written from spec (`MIDIMessage` incl. `.songPosition`, total `init?(status:data1:data2:)` / `init?(umpWord:)`) over the modern Core MIDI UMP API (`MIDIInput` / `MIDIOutput`, virtual source via `openVirtual`). Read surfaces: control cache, held-note state, `messages()` drain, `@Param` binding. **`@Param` smoothing** (`.eased` / `.smoothed`) lives on the param, so a softened knob applies to MIDI, OSC, and inspector drag alike (rides the per-frame advance pass with `@Eased` / `@Smoothed`). **Tempo sync** — `TempoClock(from: input)` follows MIDI clock into musical time (`tempo` / `beats` / `beatCount` / `phase` / `beat` decaying pulse / `bar` / `barPhase` over declared `beatsPerBar` / `progress(over:)`, the `loopProgress` sibling). **Load-bearing:** *position counts ticks* (each F8 is exactly 1/24 beat — the grid can't drift); the smoothed interval window (~2 beats, flushed on a >±40% jump or a >0.25 s break) only glides `phase` *between* ticks, clamped so position never runs backward; `start`/`continue` arm and **begin on the next tick** (the MIDI convention; song position = sixteenths × 6 ticks, and an SPP jump clears the pulse anchor so the next tick lands on it exactly); a transport-less master (DJ gear) **free-runs** from its first tick; the math is the pure `TempoEngine` fed host-time-stamped events through `MIDIInput`'s internal listener seam (packet `timeStamp` → seconds; `MIDIOutput` stamps sends with real host time so in-process loopback timing is exact), so `TempoClockTests` pin it deterministically. Examples `Integration/MIDILoopback`, `MIDIMonitor`, `TempoSync` (internal-timer loopback: an in-sketch master sends clock through a virtual source; turn it off and real gear drives the same sketch); `Docs/Integration/MIDI.md`.

- **Syphon** (`OllinSyphon` satellite). Live GPU frame-sharing with other Mac apps over the **vendored** Syphon Framework (BSD-2, `External/CSyphon`, Metal portion only — wrapped, off the public surface). `SyphonServer` / `publishSyphon(name:)` publishes the rendered texture; `SyphonClient.newFrame()` draws an incoming feed as `Image(texture:)`. Brought two core seams (shared with future layered effects): the GPU-texture frame hook (`wantsRenderedTexture` + `frameRendered(_:texture:)` → `MetalRenderer.texture(of:)`, no CPU readback) and texture-backed `Image(texture:)`. **Gotchas:** publish `flipped: true` + client flips back (orientation); hand the server a non-sRGB `.bgra8Unorm` view and read back through a `.bgra8Unorm_srgb` view (color — both need `.pixelFormatView` usage); single-GPU Macs only. Examples `Integration/SyphonLoopback`, `SyphonViewer`; `Docs/Integration/Syphon.md`.

- **Virtual camera** (`Apps/OllinCameraApp` host + CMIO system extension, plus the `OllinCamera` satellite). Registers a system-wide "Ollin Camera" every webcam app reads; `publishVirtualCamera()` letterboxes the canvas texture into a 1280×720 IOSurface-backed `CVPixelBuffer` enqueued on the device's sink stream, with a built-in "no signal" test card while idle. **Gotchas:** the sink is the **direction-0** stream; `CMIOStreamCopyBufferQueue` returns a nil queue unless the queue-altered proc is non-nil (pass an empty closure); the extension is an installed, signed, notarized `.app` (xcodegen, *not* SwiftPM) with strict activation rules (bundle-id filename, `CFBundlePackageType = SYSX`, run from `/Applications`, install with `ditto --noextattr --norsrc`) — see the app README. Example `Integration/VirtualCamera`; `Docs/Integration/VirtualCamera.md`.

- **Video playback** (`OllinVideo` satellite). `VideoPlayer` (`@MainActor`) plays a file as a live texture-backed `Image` via `AVPlayerItemVideoOutput` → `CVMetalTextureCache` → `drawImage` (zero CPU copies); `play` / `pause` / `seek(to:)` / `loops` / `rate` / `volume`, `duration` / `size` (async), `snapshot()` for CPU/vision. Conforms `FrameSource` + `VideoFeed`, so a tracker attaches and analyzes the footage as it plays (via a second video output). **Headless export is deterministic:** under `OllinApp.isRenderingHeadless` (a `package` flag every offline driver sets) the player swaps to a virtual playhead advanced by the sketch's per-frame pass (`FrameAdvancing`, now `package` so satellites conform; isolated `@MainActor` conformance) and decodes by timestamp through a sequential `AVAssetReader` (`HeadlessVideoReader`), so exported frame `k` always shows the clip at `k/fps` after `play()` — create the player by the end of `setup()` or the advance pass never finds it. **Soundtrack tap:** `audioTap` (the core `AudioTapSource` seam) delivers mono PCM off an `MTAudioProcessingTap`; the tap's C callbacks touch only a storage object the tap itself retains (released in `finalize` — never reference the player from the callbacks; the tap can outlive it), and OllinAudio's `Soundtrack(of:)` runs the analyzer surface over it (rebuilds once at the stream's real sample rate). **Gotchas:** `.bgra8Unorm_srgb` texture; no y-flip (top-down); keep a short `CVMetalTexture` ring alive; `isMuted = true` starves the audio tap (a hard mute stops audio processing) — use `volume = 0`; a `Soundtrack`/vision tracker reads nothing during a headless export (live-clock paths). Examples `Video/VideoPlayback`, `Video/SoundReactive` (spectrum bars + beat ring over its own clip's music); `Docs/Video/Video.md`.

- **Physics** (`OllinPhysics` satellite). One `World` stepped each frame, hybrid by design: a **soft Verlet side** (`Particle` — a `final class`, no stored velocity, `previous`-based; `Spring`, a positional distance constraint; `World` with gravity / drag / bounds / bounce / opt-in disk `collisions` broad-phased through a uniform spatial hash) and a **rigid side** backed by **vendored Box2D** (MIT, `External/CBox2D`) — `addBody` / `Body` / `Collider` (`.circle` / `.box` / `.capsule` / `.polygon`) / `connect` / `Joint` (`.revolute` / `.distance` / `.weld` / `.prismatic`) / `grab` (mouse joint), sharing the world's gravity / bounds / bounce / drag, bridged by `pixelsPerMeter`. **Gotchas:** `drag` compounds per step (a weightless drift field needs `drag = 0`); Box2D's world pool is a non-thread-safe global → every Box2D-touching test in one `@Suite(.serialized)`; spawn a `Body` *inside* `bounds`, not above; disk-collision resolution walks particles in **index order with a fixed cell scan** — never iterate the spatial-hash Dictionary directly (per-process hash order reordered the position-mutating pair resolution, so a seeded pile settled differently on every run; a real Guide-figure flake). Examples `Physics/Packing` + `Blobs` (soft), `Stack` / `Tumble` / `Chain` (rigid); `Docs/Simulation/Physics.md`.

- **Computer vision** (`OllinVision` satellite). A `Camera` (or any `FrameSource`) feeds `VisionAnalyzer` (drops frames it can't keep up with; the display frame is never dropped) and composable trackers over Apple's modern Swift `Vision` API. **Sixteen trackers:** `FaceTracker` / `Face` (76-pt landmarks + roll/yaw/pitch), `ContourDetector` (camera → vector `Shape`), `HandTracker` / `Hand` (21 joints), `BodyTracker` / `Body` (2D pose), `RectangleDetector`, `BarcodeScanner`, `TextRecognizer` (OCR), `ObjectTracker` (lock + follow), `TrajectoryTracker` (parabolic arcs, predicted), `FlowTracker` / `MotionField` (dense optical flow), `PersonSegmenter` / `SubjectSegmenter` (matte + cutout `Image`s), `ImageClassifier`, `SaliencyTracker` (heat-map + regions + point query), `BodyTracker3D` / `Body3D` (17 joints in meters; canvas / model / camera space), and `ModelTracker` (*any* Core ML model — classifier labels / detector boxes / image map / semantic-segmentation `ClassMask`, decoded by observation kind). Each also runs one-shot on a still `Image` via `detect(in:)`; sync contexts (`setup()`, deterministic figures/exports) run those inline via `waitFor(image) { try await …detect(in: $0) }` — the stills ride as *arguments* (not captures) because `Image` isn't Sendable, it parks the caller (never call it from an async context), and the `@Sendable` closure type is what keeps the work off the caller's actor (a plain closure would inherit main-actor isolation and deadlock the parked main thread). **Coordinate mapping is the crux:** `VisionSpace` (+ each result's `bounds(in:)` / `landmarks(_:in:)`) maps Vision's normalized lower-left origin → Ollin's pixel top-left canvas (y-flip + rect-scale + a `mirrored:` selfie flag) — map results into the *same* `camera.fittedRect(in:)` you drew the frame into, and use Apple's per-result `*InImage` helpers (never hand-roll the conversion — it over-spreads landmarks). Feeds draw via `drawFrame(_ feed:in:waiting:)`. **Hardware split:** classical CV (rectangles / barcodes / contours / object-track / trajectory / optical-flow) *and* OCR run on any Mac; the neural models (body pose, segmentation, saliency, classification, 3D pose) need Apple silicon — surfaced via `isAvailable` / `unavailableReason`. **Gotchas (pinned by probe):** trackers bind to `any FrameSource` via the core `FrameSource` seam + a per-source shared analyzer kept alive by the tap closure (formed in a free function — the render-thread rule); point queries on single-channel buffers (`flow(at:)` / `pixel(at:)`) are clampless nearest-neighbor that *trap* past the last pixel — clamp to `(n−1)/n`, and they take top-down fractions so flip y before the lookup; `InstanceMaskObservation.allInstancesMask` holds *labels*, not a soft matte (use `generateMask`); a Core ML model must load from a *stable* compiled path or it re-specializes (~10 s) every launch; `DetectTrajectoriesRequest` only works through timestamped `CMSampleBuffer`s. Model weights download via `Scripts/fetch-models.sh` (never committed; `StyleMirror`'s is user-trained). Examples span `Vision/WebcamFeed` / `FaceTracking` / `FaceAlign` / `ContourTrace` / `HandTracking` / `BodyPose` / `RectangleScan` / `BarcodeReader` / `TextScan` / `ObjectTracking` / `VideoTrace` / `TrajectoryTracking` / `PersonSegmentation` / `SubjectLift` / `OpticalFlow` / `SceneLabels` / `EyeCatcher` / `BodyPose3D` / `DepthRelief` / `ObjectDetection` / `DigitReader` / `StyleMirror` / `PaintByClass`. `Docs/Vision/Vision.md`.

- **Geometry & color.** `Vector2` / `Vector3`, `Rectangle` (+ the `Rectangle(fitting:in:)` letterbox everything shares, `contains`, `inset(by:)`, the normalized `point(u:v:)`/`uv(of:)` pair — canvas sugar `Sketch.uv(_:_:)`, example `Basic/NormalizedCoordinates`), `Circle`, `Shape` / `Contour` (arc-length walk helpers + even `resampled(spacing:)`, on both), the `Path` curve builder; per-frame transform stack (`translate` / `rotate` / `scale`, scoped via `withState {}`). The generator/technique catalog below is terse invariants + pointers only — **mechanism, history, and the example/snapshot/test pointers live in [`ARCHITECTURE.md`](ARCHITECTURE.md) § *The geometry and generator catalog* + § *Color: palettes, dithering, and print separations*** (user-facing API in the named `Docs/` pages), and new catalog depth routes there, not here. Three catalog-wide rules: **determinism is a promise** — no rng decision or position-mutating pass ever walks a Swift `Set`/`Dictionary` directly (sorted candidates, fixed iteration orders; the WFC rule, shared by mazes / space colonization / boids / the physics resolver); **fit a figure by scaling its *points*, not `scale()`** (the CTM scales stroke width too; the trochoid/Lévy rule); and **a geometry cost dial is an artwork parameter, never a `RenderQuality` tier** — geometry is data a sketch reads back and the tiers resolve per-GPU, so a tier would change the *piece* (and make it machine-dependent), not its rendering fidelity; `maxVertices` / `levels` / `resolution` / erosion stay explicit numbers, with `@Param` + `--export-sweep` the tuning path (`RenderQuality` stays GPU-sampling-only; reasoning in ARCHITECTURE.md § *Performance and the quality dial*). The catalog: **`Grid`** — typed `columns × rows` over a `Rectangle`; indexed `points`/`cells` elements (indexed cases are a single loop); `.center`/`.spanning`; `padding`/`gutter` over typed `Insets`; element-type design history settled, don't re-litigate. **Shape booleans & offsets** (vendored Clipper2, BSL-1.0) — offset output must be `SimplifyPaths`'d or cascaded insets balloon; **stroke as shape** `stroked(width:join:cap:)` — the input is *not* normalized first (that would erase open paths); **convex hull** `convexHull(of:)` (monotone chain). **SVG import** — `SVG` + `loadSVG` / `drawSVG` / `fitted(in:)`; control points transform **before** flattening; initial fill **black**, initial winding **`.nonZero`** (don't "harmonize" with Ollin's `.evenOdd` default); unresolvable `url(#…)` → mid-gray, never dropped; unsupported containers skip their whole subtree. `Docs/Drawing/SVG.md`. **Fourier epicycles** — `Epicycles` + `drawEpicycles`; `terms:` is a largest-first *prefix*; deterministic sort tie-break; `point(at:)` periodic; exact at full terms (`EpicyclesTests`). **Classic curves** — `phyllotaxis` / `lissajous` / `rose` (parity-dependent closure span, no retraced doubling) / `hypotrochoid`/`epitrochoid` (integer gears, exactly `wheel/gcd` laps) / the damped-pendulum `Harmonograph` / Chaikin `smoothed(iterations:)` (open contours keep exact endpoints); all rng-free. `Docs/Drawing/Curves.md`. **Shape morphing** — `ShapeMorph` (build once, read `shape(at:)`) + `Shape`/`Contour: Tweenable` + `morphed(toward:_:spacing:)`; orientation mismatch reverses **all** target contours (never per-pair); spacing derives per outline, **not** per pair; unmatched contours lerp to their own centroid; originals verbatim at 0/1 (`ShapeMorphTests`). `Docs/Drawing/Morphing.md`. **Voronoi & Delaunay** (Bowyer–Watson + clip to `bounds`, `relaxed()` Lloyd) + **blue noise** (Bridson `poissonDisk`, a public generic over any `RandomNumberGenerator`). **Low-discrepancy sampling** — `halton` / `haltonPoints` / `sobolPoints`: pure index functions (no rng, never touch the sketch's `rng`); `startIndex` defaults **1**; Halton bases stay **coprime** (`SamplingTests`). **Stippling** — Secord weighted-Voronoi `stipple`; the ring search may stop before ring r only once the best find is ≤ **(r−1)·bucketSide** (the r·bucketSide bound was a real lattice-locking bug); zero ink → **empty array**; density = (1 − linear `luminance`) · alpha (`StippleTests`). **Random walks** — `randomWalk` / `levyFlight` (`minStep` > 0 — the power law diverges at zero) / `selfAvoidingWalk` (visited-forever marks + parent-link reconstruction); all seeded (`WalkTests`). **Truchet** — arcs centered on cell *corners*, so abutting cells join under any flip. **Tiling & layout** — `HexGrid`/`TriangleGrid` (true-aspect blocks, never stretch; axial math; `cell(at:)` via **cube rounding**), `Subdivision` (the root always splits; `chance` from depth 1), `Maze` (three carvers, all perfect; collinear walls merged; double-BFS `longestPath()`), `apollonianGasket` (Descartes/Vieta other-root — linear recursion); `TilingTests`. `Docs/Drawing/Tiling.md`. **Aperiodic tilings** (`Geometry/Penrose.swift` / `WangTiles.swift` / `Girih.swift` / `Spectre.swift`) — `penroseTiling` (P2/P3 by Robinson half-tile deflation from a ten-half wheel, merged into whole tiles; **the P2 child orderings encode each half's axis leg** — wrong assignments T-junction at depth 2, crack-free-pinned; **rhomb outlines lead with the positively-wound half's apex** — the intrinsic orientation the arc decoration needs, since C2-symmetric arc placement collapses to midpoint crossings (a solved dead end, not a tuning knob); arc fractions are solved continuity constants; rng-free); `wangTiling` (scanline fill honoring west/north edges; `WangTiling.completeSet` never dead-ends, arbitrary sets retry then return nil; weighted picks off the sketch rng); `girihPattern` (Hankin/Kaplan inference: two rays per edge midpoint at the contact angle, paired greedily by summed length with `(cost, i, j)` tie-breaks; unmatched rays drop; `Girih.Tile.points(onEdge:_:)` is the exact patch-composition primitive); `spectreTiling` (the chiral-monotile paper's 9-metatile / 8-slot substitution; **every generation mirrors whole**, so leaves at any depth share one handedness — pinned by the cyclic turn-sequence test; gamma carries the 30°-rotated `isOdd` mystic; `curve:` bends edges into the strictly chiral spectre, alternation cyclically consistent because 14 is even; rng-free). All four crack-free-tested (`AperiodicTilingTests`); examples `Patterns/Penrose` / `WangTiles` / `Girih` / `Spectre`; snapshots `penrose` / `wang-tiles` / `girih` / `spectre`. `Docs/Drawing/AperiodicTilings.md`. **Circle & shape packing** — grow-to-touch `packCircles` + closed-form `packCircles(around:)` + `relaxCircles` (fixed-axis parting); geometry-aware `packShapes` over the stateful `ContinuousPacking` (fits actual outlines); an unbounded `maxRadius` clamps the grid cell **and** reach to the region size (`PackingTests`). **L-systems** — 13 verified presets; 2M-symbol expansion cap; one `Contour` per unbroken turtle run (a branch shares its branch point with the trunk). **Differential growth** — stateful class, order-stable → a fixed-`frame` snapshot reproduces. **WFC** — simple-tiled (Gumin); sorted candidates + fixed cell order or the seeded solve flakes. **1D CA & turmites** — `elementaryCA` / `totalisticCA` (pure functions; the Sketch facade mirrors the full free-function signatures — the shadowing rule) + `Turmite` (published turn codes, no rng, `Preset` catalog; `CellularAutomataTests`). `Docs/Generators/CellularAutomata.md`. **Flow fields** — `FlowField` + Jobard-Lefer `streamlines` (a batch commits to the occupancy grid only *after* tracing, so a line never blocks itself); the seeded sugar captures the sketch's noise → transient, don't store the field (it retains `self`). **Boids & steering** — `Boids` (order-stable force sums → seeded flocks reproduce) + `Vehicle` (behaviors *return* forces; only `wander` rolls — a unit roll scaled by jitter; arc-length path follow, `closed:` wraps; `SteeringTests`). **Force-directed graph layout** (`Geometry/ForceLayout.swift`) — `ForceLayout` + `drawGraph`, the FR spring embedder (repel `k²/d` all pairs, pull `d²/k` on edges, temperature-capped moves clamped to bounds, linear cooling); **`idealDistance` is a scale dial, not a promised edge length** (a ring's equilibrium spacing sits well under k — measured on-equilibrium, so raise k to open the web, never iterate longer) and **a settled layout is frozen** (`step()` early-returns; `reheat(_:)` is the one path back into motion — the growth/drag idiom); pinned nodes hold, `gravity` (default 0) centers disconnected pieces, coincident nodes part deterministically; taught in Guide Ch 9. **Space colonization** — single-closest-node growth; keep `killRadius` > `stepLength`; sorted Dictionary iteration + a stall counter (`isFinished`). **DLA** — off-lattice walkers; arrival order = index. **Strange attractors** — `StrangeAttractor` (RK4) + `ChaoticMap`; RK4 shared in `Math/Integration.swift`. **Chaotic 1D maps & bifurcation** (`Geometry/Bifurcation.swift`) — `IteratedMap` (logistic/sine/tent/gauss families + custom rule, optional analytic `derivative`): `orbit`/`graph`/`cobweb` + `bifurcation` points / `bifurcationImage` density (ink-on-white, ONE global log factor — the flame rule) / `lyapunovExponent` (exact-value-pinned: ln 2 at logistic r=4, ln r for tent) + `drawBifurcation` sugar; rng-free throughout. **Sweeps sample column centers, never range endpoints** (exactly-r=2 tent collapses to 0 in floating point, one doubled bit at a time) and **factory starts avoid the critical point** (the logistic critical orbit dies in two steps at exactly r=4 — start is 0.3, not 0.5); gauss can hold two attractors at once, so its diagram legitimately depends on `start`. Example `Patterns/Bifurcation` (diagram + Lyapunov trace in a retained batch); snapshot `bifurcation`; `BifurcationTests`. `Docs/Generators/Bifurcation.md`. **Fractals** (`Geometry/IFS.swift` / `InversionFractal.swift` / `Kleinian.swift`, `Image/FractalFlame.swift`) — `IFS` (affine `Map`s + weighted chaos game; presets fern/triangle/carpet, the fern grows +y up so examples flip); `FractalFlame` (written from the flame paper ONLY — flam3/Apophysis are GPL, never read; variations use **θ = atan2(x, y)**, the y-axis convention, pinned by `FractalTests` — swapping args is the classic porting bug; ONE log factor from density scales all three channels (independent logs shift hue), gamma at the downfilter; `Renderer` is the progressive form, deterministic per (flame, size, seed, total samples)); `inversionLimitSet` (the canonical walk: start OUTSIDE the discs, random circle but **never the one just used**, no inside/outside test — the pull-inward-only variant collapses tangent arrangements into the tangency points, a real bug); `kleinianLimitSet` (trace recipe on the Markov minus root; the DFS's **cyclic child order + repetend fixed points** are jointly what make the output one ordered closed polyline — change either and the curve disconnects); the public **`fitted(_:in:)`** point-cloud placement helper (Rectangle.swift). Examples `Patterns/IteratedFunctions` / `FractalFlame` / `InversionFractal` / `Kleinian`; snapshots `ifs` / `fractal-flame` / `inversion-fractal` / `kleinian`; `FractalTests`. `Docs/Generators/Fractals.md`. **Schottky circle orbits** (`Geometry/Schottky.swift` + the shared `Geometry/MobiusMap.swift`, the internal `ComplexValue`/`MobiusMap` lifted out of `Kleinian.swift` — the Kleinian walk is byte-identical after the move) — `schottkyCircles` / `schottkyLimitSet` over `SchottkyPairing`, the *circle*-emitting sibling of `kleinianLimitSet`: it renders the disconnected regimes the ordered-curve walk can't, and its `[Circle]` output means SVG/PDF write **true circles** (the plotter path, halftone precedent). **Load-bearing:** a circle's Möbius image is the closed-form **Hermitian** transform `H' = (M⁻¹)†H M⁻¹` (a line image → `nil`, dropped as a branch, measure-zero for circles clear of the pole), and the map must be **unit-determinant** for `inverse` (the adjugate) to be exact; the walk emits `w(C_k)` for every non-cancelling letter and **prunes on that same circle's radius** (the whole subtree nests inside it, so the radius *is* the branch's error bound — no second pass); the pairing's twist zero point folds in **`−u²`** so two *touching* circles paired at `twist: 0` hold their tangency point fixed and the generator is **parabolic**, which is the entire reason a picture stays dense (measured: tangent-pair `schottkyCuspedPairs` 22,660 circles vs cross-ring `schottkyNecklace` 3,208 at its best twist — no twist rescues the necklace), so **`lean` is the animatable dial** (it swings each pair about its own tangency point in opposite senses, keeping both generators parabolic) where **`twist` thins the lace as it grows**; overlapping discs make the group non-discrete and a 2M-circle ceiling ends such a walk rather than exhausting memory. **Viewing, containing, and the trace bridge:** a `viewpoint` re-seats the whole orbit by conjugating with the involution sending that point to the horizon (walk runs in the *viewed* plane so `minRadius` prunes drawn sizes; the disc holding the viewpoint flips to the picture's outer boundary; a viewpoint ON the limit set makes circles above any floor infinite — nudge off it); a pairing disc may be its circle's **exterior** (`fromExterior`/`toExterior`), the containing-circle framing, and the walk's orientation rides the **sign of the Hermitian form's `A'`** (`discImage`), with exterior discs never radius-pruned (their radius bounds nothing — depth stops them); `schottkyCircles(ta:tb:in:)` bridges to the grandma-recipe groups via **isometric circles** as pairing discs (letter k's disc = `gens[k].inverse.isometricCircle`; bases may overlap — nesting is per-map, pruning stays sound; gasket traces `(2,2)` = the Apollonian-gasket packing, the classic figure; grandma's minus-root makes ±imaginary trace perturbations asymmetric — the example ping-pongs the upper arc only). Example `Patterns/Schottky` (declares `loopDuration`); snapshot `schottky`; `SchottkyTests`. **Spanning-tree line art** (`Geometry/SpanningTree.swift`) — `spanningTree(through:/of:)`, the branching sibling of `singleLine`: exact EMST (Kruskal over Delaunay edges, deterministic tie-breaks; collinear input → lexicographic chain, coincident duplicates → nearest-straggler joins), decomposed into the **minimal** open chains — a walk may start only at a *currently*-odd vertex (originally-odd starts strand edges into extra chains, a real bug, pinned by `SpanningTreeTests`). **Isolines** (`Geometry/Isolines.swift`) — `isolines(at:in:resolution:field:)` + levels + image forms, marching squares: **oriented** case table + cell-average saddle rule; **null segments dropped** (a contour through an exact grid corner emits them — two-point litter, a real bug); directional stitch → boundary-exiting contours come back **open**; endpoint maps indexed, never iterated; levels share one sampling; image form = linear-luminance-on-white, levels in **sRGB tone** (the `cutoff` convention). Examples `Images/SpanningTree` + `Patterns/ContourMap`; snapshots `spanning-tree` / `isolines`; `IsolineTests`. `Docs/Generators/SpanningTree.md`, `Docs/Generators/Isolines.md`. **Hulls & medial axis** (`Geometry/ConcaveHull.swift` + `MedialAxis.swift`; `Shape.contains(_:)` added alongside, a winding-honoring ray test) — `concaveHull(of:concavity:)` (chi-shape: erode border triangles longest-boundary-edge-first, blocked when the opposite vertex is already on the boundary; **hull-notch capping is load-bearing** — the substrate's finite super-triangle drops hairline hull slivers, so missing hull edges are capped with sliver fans before erosion, making concavity 0 *exactly* `convexHull` (seed-7 regression in `HullTests`)); `alphaShape(of:alpha:)` (alpha complex, circumradius ≤ alpha; islands + holes as `Shape`s); `medialAxis(of:spacing:prune:)` (Voronoi subcomplex of a boundary resample — non-ring-neighbor + inside filters; radii are exact clearances; **prune collects each round's twigs against the round-start graph and removes them together** — mid-round removal turns a junction into a pass-through and the sibling's walk runs past it, a real bug; a whole-component chain never prunes). All three **canonically order output by geometry, never triangle order** (Bowyer–Watson's Dictionary refan makes triangle *order* process-varying while the *set* is stable — loops start at their lex-min vertex, islands and branches sort by points). Examples `Shapes/Hulls` + `Shapes/MedialAxis`; snapshots `concave-hull` / `medial-axis`; `HullTests` / `MedialAxisTests`. `Docs/Generators/Hulls.md`, `Docs/Generators/MedialAxis.md`. **Straight skeleton** (`Geometry/StraightSkeleton.swift`) — `straightSkeleton(of:)` (exact shrinking-wavefront skeleton: arcs + one face per boundary edge) + `StraightSkeleton.inset(by:)` (mitered insets cut from the faces as roof planes, the contour-ladder tool; `maxInset` the vanish depth; Clipper2 offsets stay the rounded/outward tool). **Load-bearing:** events process in **levels clustered by meeting point / shared parent** and dispatch as chains (the degenerate-pile-up treatment; naive one-at-a-time is famously wrong); events computed during a level that land back on it are **purged as stale echoes**, which is why a 2-vertex LAV closes as a **bridge** (its pair's mutual event always re-lands on the level) and why **coincident LAV neighbors must fuse at level end** (a ghost corner around a zero-length front otherwise starves its LAV — a real bug, 8% of a spiky blob's area vanished, pinned by `facesPartitionSpikyBlobs`); output assembles by **node identity** (each vertex's origin→death arc; faces stitch from their edge's endpoint-role arcs; inset chords match across faces by arc node-pair, no epsilon), and **Σ face areas == shape area** is the test net. Example `Shapes/StraightSkeleton` (island contour ladder); snapshot `straight-skeleton`; `StraightSkeletonTests`. `Docs/Generators/StraightSkeleton.md`. **Painterly marks** (`Geometry/Marbling.swift` + `Watercolor.swift`) — `Marbling` (Jaffer closed-form: area-preserving `drop` to √(d²+r²), the stylus family `tine`/`comb`/`tine(around:)`/`swirl` all displacing `strength · 2^(−d/falloff)` along the tool's motion; rng-free, `add` floats arbitrary shapes holes-intact) + `drawMarbling` (oldest-first fills); **refinement subdivides at *pre-transform* midpoints while the transformed span exceeds `spacing`** (post-hoc refinement can't recover a fold; depth-capped + source-length floor so a drop centered on an outline can't recurse away); rotational tools preserve radius exactly (`MarblingTests`). `Watercolor` (Hobbs recursive midpoint deformation: per-edge variance inherited at 0.4...0.8 with jittered 0.2...1.8 starts; immutable base + further-deformed `layer`s) + `drawWatercolor`; **layers must fill non-zero-wound via `layerShape`/`drawShape`** (they self-cross: even-odd cuts pinholes, and `drawPolygon`'s convex fan shatters them into radial spokes — both real first-render bugs); the **`detail` floor** (edges below it stop subdividing) bounds layer cost regardless of rounds (~40 s → seconds, invisible); painting is setup-shaped (`makeBatch` or `noLoop`). Examples `Patterns/Marbling` + `Shapes/Watercolor`; snapshots `marbling` / `watercolor`; `MarblingTests` / `WatercolorTests`. `Docs/Generators/Marbling.md`, `Docs/Generators/Watercolor.md`. **Chladni figures** (`Math/Chladni.swift`) — `chladni(x,y,m:n:a:b:)`, the center-driven square plate's standing wave in closed form, normalized to −1…1 over plate coordinates 0…1: **`m == n` cancels to zero at the default amplitudes** (1, −1) and the diagonal is always nodal (document, don't "fix"); fractional modes morph figures, so animated tours keep `m > n` at every stop to never cross the degenerate diagonal; mirrored in the shader lib's noise module (`chladni(float2, m, n[, a, b])`) and read by the **`.chladni` Generator** (`ChladniStyle` `.sand` Gaussian gather + hash speckle re-thrown by phase / `.wave` signed swing by `cos(phase)`; `rawIndex` kept in step with `ollin_gen_chladni`). Nodal lines fall out of `isolines(at: 0, ...)` over the field. Examples `Patterns/Chladni` (mode tour) + `Audio/ChladniResonance` (modes picked by *listening*, `magnitude(in:)` per mode pitch); snapshot `chladni`; `ChladniTests`. `Docs/Generators/Chladni.md`. **Terrain** (`Geometry/Heightfield.swift`) — `Heightfield` (closure/`values` inits, bilinear `value(atU:v:)`, `normalized()`, heights 0…1 by convention), `.diamondSquare` (size rounds up to 2^k+1, output normalized, seeded/generic-RNG), `eroded(_:seed:)` over **`Erosion.hydraulic`** (the particle droplet method: inertia-blended bilinear gradient steering, slope·speed·water·capacity carry rule, bilinear-4 deposits, distance-weighted brush erosion; **the speed update is the energy form** `√(max(0, v² − Δh·g))` — the thesis's printed `+Δh·g` slows downhill drops and goes imaginary, a known erratum; heights clamp ≥ 0 so border runoff can't dig unbounded drains; mass only decreases, drops carry sediment off-map) and **`Erosion.thermal`** (talus relaxation, **Jacobi-gathered against the frozen field** so it's order-free, deterministic, exactly mass-conserving; transfer halved so a symmetric pair can't overshoot); `mesh(width:depth:height:)` emits +y-up terrain (CCW-viewed-from-above winding, central-difference normals, plan-view 0…1 UVs — a height-walked `Ramp` texture paints altitude bands) and `image()` the grayscale heightmap. Setup-shaped CPU work (erode once, keep the mesh). Example `3D/Geometry/Terrain` (precomputes raw + eroded meshes for the `weathered` toggle); snapshot `terrain-3d`; `HeightfieldTests`. `Docs/Generators/Terrain.md`. **Isosurfaces & metaballs** (`Geometry/Isosurface.swift` + `Metaballs.swift`) — `isosurface(at:in:resolution:field:) -> Mesh`, marching cubes, deliberately mirroring `isolines`' signature (its 2D sibling); `Metaballs` (`add(at:radius:strength:)` / `value(at:)` / `bounds` / `mesh(resolution:)`) is the headline field. **Load-bearing:** the convention is **inside = field > level** (normals `−∇f`, triangles wound to match, pinned by a positive `signedVolume`), so an SDF must be **negated** to mesh; there is **no 256-case triangulation table** — each cell's surface is built from its **six face contours** (per face: 0/1/2 directed segments with the enclosed region on its left seen from outside, walked CCW) chained into loops and emitted **reversed** (the loop faces inward), and the ambiguous 4-crossing face is settled by the **asymptotic decider** `δ = (a·c − b·d)/(a + c − b − d)` over the face's own four corners, which is *why* it can't crack (both cells read the same four values and the reversed walk swaps starts/ends symmetrically); vertices weld on a **flat lattice-edge array** keyed `(low corner, axis)`, never a Dictionary (determinism), and normals come from the **sampled** gradient (central differences, one-sided at walls) so a smooth field costs no extra field calls; a quad loop splits on its **shorter diagonal**, 5+ fans from an added centroid vertex (cell-interior, so no crack); cells stay **cubic** (`resolution` counts across the longest side) and the grid centres its overshoot; `Metaballs.radius` is the **visible** radius and reach is **2·radius**, because Wyvill's polynomial is exactly 1/2 at u = 1/4 (`falloff` pinned at its three known values), and `mesh()` pads the box by 1.5 cells so the surface closes. Cost is cubic in `resolution` (M2 release, 6 balls at 56: ~2 ms/frame, real-time) and the per-face work is **allocation-free** (flat `faceCorners` + caller-owned scratch, worth ~2× over the obvious per-face arrays); **no UVs** (a blob has no natural parameterization). **Deliberate v1 cut: meshing an `SDF3D` is out** — its evaluation lives only in `ollin_sdf3d_eval`, so a CPU evaluator would be a third hand-synced surface across 15 leaves / 20 combines / 10 transforms. Example `3D/Geometry/Metaballs`; snapshot `metaballs`; `IsosurfaceTests` (watertightness over a saddle-rich field at three resolutions, sphere volume + outward winding, determinism). `Docs/Generators/Isosurface.md`. **Subdivision surfaces** (`Geometry/SubdivisionSurface.swift`) — `mesh.subdivided(_:levels:)` refines any `Mesh` as a control cage: `.catmullClark` (default, general polygon faces) / `.loop` (triangle rules with Warren's β, plus a final **limit push** — γ = 1/(n + 3/(8β)) interior, (3v+b₁+b₂)/5 on rims). **Load-bearing:** the positional **weld tolerance is ~1e-6 of the mesh extent** — libtess2 caps come back Float-quantized (~1e-8 relative) against exact-double walls, so a tighter weld leaves every cap rim open (a real bug); **quad recovery** pairs the consecutive `(a,b,c),(a,c,d)` triangle pattern `gridQuad` emits (run *before* any winding flip — flipping first breaks the pattern); the **coplanar face merge** (CC only) re-derives whole polygon faces for exactly-flat patches (single simple boundary loop, no interior vertices — a deliberately tessellated flat grid is untouched) because a triangulator's arbitrary cap diagonals give identical corners different valences and an extruded star smooths visibly lopsided (0.2 arm spread → 1.7e-8, the five-fold test); **orientation follows the input's normals, not its winding** — `Mesh.extrude` winds inward while shading outward (a find worth knowing), so agreement is area-weighted-summed and a disagreeing mesh's faces flip; no normals → authored winding kept; open/non-manifold edges take the B-spline crease rules, a boundary vertex with one incident face **pins** (the edge-and-corner convention, so open sheets keep their extent), flat sheets stay exactly flat; deterministic (dictionaries lookup-only, first-come weld ids); 2M-face cap with a printed note; output = smooth area-weighted normals, **no uvs**, material carried. Example `3D/Geometry/SubdivisionSurfaces`; snapshot `subdivision-surfaces`; `SubdivisionSurfaceTests` (hand-derived mask pins — cube corner 5/18 + edge 3/8, the Loop strip's odd/crease points — plus watertightness, Euler, convex-hull containment). Taught in Guide Ch 17. `Docs/Generators/SubdivisionSurfaces.md`. **Mesh growth** (`Geometry/MeshGrowth.swift` + `MeshOperators.swift` + `MeshReactionDiffusion.swift`) — `MeshGrowth`, a stateful stepper that grows a `Mesh` surface until it folds, over four `GrowthDriver`s (`.uniform` / `.curvature` / `.field(closure)` / `.chemical(SurfaceChemistry)`); the public `MeshReactionDiffusion` runs the same Gray-Scott on a *fixed* mesh (Turk's surface-texture use, `displaced(by:)` to see it). **Load-bearing:** forces run **along edges only**, which lie in the surface, so nothing can inflate the form and *every* driver folds — the driver picks **where and at what scale**, not whether (documenting `.uniform` as "inflates smoothly" was wrong, caught by render, and the buckling is correct); **self-avoidance excludes the 2-hop neighborhood, not just the 1-ring** (a 2-ring vertex sits ~2·`edgeLength` off on any smooth surface, so leaving it in makes every vertex shove its own neighborhood and the whole form inflates with growth *off* — the defect `aZeroFieldDoesNotGrow` pins), radius default 2·`edgeLength`, past ~3· it inflates again; **`stiffness` is the fold-size dial** (the normal half of the relaxation Laplacian — a sheet with no bending resistance buckles at the triangle scale and reads as crumpled paper, which is the thin-shell term the reference has and the first cut omitted); remeshing is **Botsch-Kobbelt isotropic** (split >4/3·L, collapse <4/5·L guarded by the **link condition** + both rings' resulting lengths + no normal flip, flip toward valence 6, tangential relax at 1/5) and **growth must stop at `maxVertices`** or the springs stretch triangles the splitter can no longer subdivide; per-vertex `channels` (the chemicals) interpolate at a split and average at a collapse so a pattern survives the topology churning; the boundary is **never collapsed or flipped** and a rim vertex relaxes *along* the rim, so an open sheet keeps its edge. **Both types weld first via the shared `WeldedMesh`** — Ollin's generators emit flat-shaded meshes whose triangles share no vertices (an icosphere is degree-2 everywhere), so an unwelded surface cannot diffuse at all and the reaction silently dies; `MeshReactionDiffusion` simulates on the welded copy and republishes through `remap` so `values` still matches `mesh.positions` by index. The **cotangent Laplace-Beltrami operator with mixed areas** (Meyer et al.) is the shared substrate (`MeshOperators`, internal): scaled by mean-edge-length² it is dimensionless and lands at **exactly 4 on a regular triangulation, matching a grid's 5-point stencil**, which is *why* the classic Gray-Scott constants (0.16 / 0.08, feed/kill matching `Sim.reactionDiffusion`) transfer unchanged; a seed patch is **u=0.5, v=0.25 floored at 3·edgeLength across** (pure reagent has nothing to react with and decays; a patch a vertex or two wide diffuses away before igniting). **`settleSteps`** (default 0, `.chemical` only) runs the opening chemistry all at once at the first step so growth answers a formed pattern — and it **must refine the surface to the target edge length first**: chemistry settled on the coarse seed cage dies at exactly zero (the patch is a vertex or two wide there, the ignition floor above — a probe-caught bug); it's the *ramp* fix only, since the pattern covers part of the surface and chemical growth stays a few times slower than `.uniform` at equal `growthAmount` (rate compensation stays with the sketch). Determinism: edges come from a **sorted key array**, never a Dictionary walk, and the repulsion hash grid dedupes its 27 slot reads (two cells can share a slot and would otherwise double-count). Cost is real CPU mesh processing (~30 ms/step at 9k vertices on M2 after a 3× pass: ring table built once, flat edge/boundary table, counting-sort grid, allocation-free `EdgeFaces`), so `maxVertices` is the interactivity dial. Example `3D/Geometry/MeshGrowth`; snapshot `mesh-growth`; `MeshGrowthTests`. `Docs/Generators/MeshGrowth.md`. **Point-cloud surfacing** (`Geometry/ParticleSurface.swift` + `SurfaceReconstruction.swift` over the internal `PointGrid3` + `partialIsosurface`) — `particleSurface(of:radius:blend:resolution:)` (the blended union-of-balls skin: field = radius − |p − kernel-weighted local average|, so an isolated point is an exact ball; no normals, always closes) and `reconstructSurface(of:spacing:resolution:orientedToward:maxGap:neighbors:fitting:keepingLargestComponent:)` (the faithful path: PCA tangent planes over k-NN, signed distance to the nearest plane, marched by the internal **`partialIsosurface`**, whose nil-mask path is byte-identical to `isosurface`). The **`fitting:` choice** (`SurfaceFitting`, an artwork parameter per the catalog rule, never a `RenderQuality` tier): `.planes` default (byte-identical to the shipped path — pinned), `.robust(sharpness:iterations:)` = **RIMLS** (Öztireli/Guennebaud/Gross 2009, written from the paper — MeshLab's implementation is GPL, never read): IMLS base (kernel-weighted average of plane heights, kernel `(1−d²/h²)⁴`, per-point h = reach clamped to [1.5, 4]·spacing) iteratively re-weighted by a residual Gaussian (Welsch, σ_r 0.5 **in units of h**) × a normal-vs-gradient Gaussian (σ_n = 1/sharpness floored at 0.5, where the surface genuinely disconnects); **the first pass is unweighted on purpose** (a robust start breaks the reconstruction's continuity — the paper's deliberate deviation from textbook IRLS, so `iterations: 1` is plain smooth IMLS) and **the gradient keeps the kernel-derivative terms** (`(Σαφ′f − f·Σαφ′ + Σαφn)/Σαφ` — the weighted normal average alone is the classic shortcut bug); both fittings share **one plane-based validity gate**, so holes open identically; the evaluator reuses scratch buffers across the serial march (per-lattice allocation is the debug-build trap) with flat scalar inner loops. Measured on the staged partial-coverage ball (the phantom-fringe scene): mid-air phantom vertices 5,480 → 43, ball-annulus 1,747 → 0, the ball's observed front kept (3,169 vs 3,210 on-ball vertices) — so the fringe envelope below is now `.planes`-specific. **Colors carry:** the `PointCloud` overload transfers the cloud's colors (nearest sample, `Mesh.colored(from:)`), skipping all-white clouds so bare reconstructions stay colorless; `largestComponent` remaps colors with positions. **Load-bearing:** orientation is **viewpoints-first**, by a **distance-weighted vote (1/d⁴) over the (nearest ≤16) viewpoints — never the nearest alone** (the nearest camera sees a terminator sample edge-on, dot ≈ 0, sign = noise → flipped planes raise phantom curtains, probe-confirmed; the 1/d⁴ falloff keeps thin walls decided by proximity); the spanning-tree fallback weights edges 1−|nᵢ·nⱼ|, which reads antiparallel as agreement, so it **can walk through thin double-sided structure and flip a region** (documented limit, not tunable); the field is **undefined, never guessed, where the projected foot lands beyond the validity cutoff** (nil = per-point k-th-neighbor reach **capped at 3.5·spacing** — uncapped, the reach *inflates* exactly where data is missing and phantom bowls grow in occlusion shadows; tighter caps pinhole randomly-sampled clouds, whose local gaps routinely pass 2.5·spacing; `.infinity` closes everything), which is what keeps scan holes open — and cells touching an undefined sample are skipped whole; **partial coverage frays by design under `.planes`**: an open data edge grows a ~gap-wide fringe along its tangent planes just past the samples (four knob "fixes" probed and refuted — coverage is the fix, so sweep around bodies; `.robust` suppresses >99% of the fringe, measured, but coverage stays the honest fix; the Docs page says both), and **the marching cell must stay under ~2·spacing** (a wide-bounds cloud starves the grid and junctions shred — printed note; crop to the region of interest); the **near-surface occupancy band** short-circuits far cells in one array read but **must not run under `.infinity`** (closing legitimately caps far from samples); `PointGrid3` ring searches **stop on a strict bound only** (in-box `ring·cell`, clamped `(ring−1)·cell`; a tie at the bound must expand another ring — the stipple stopping rule in 3D) and its queries are **flat scalar loops on purpose** (per-candidate closures + `Vector3` operators ran 8.7× slower in the unoptimized builds examples run under); counting-sort buckets ascend by index → byte-reproducible output. Example `3D/Geometry/SurfaceFromPoints`; snapshot `surface-from-points`; `SurfaceReconstructionTests` (grid matched against brute force, watertight sphere volume + outward wind, torus Euler 0, a hole stays open, thin-wall viewpoint orientation, byte-determinism both paths). `Docs/Generators/SurfaceReconstruction.md`. **Articulated & chaotic motion** — `IKChain` (`maxBend` enforced *inside* both FABRIK passes then re-placed at exact segment length, never post-clamped; keep the stall exit), `DoublePendulum` (fixed ≤1/480 s substeps — reproducible by default), `NBody` (Barnes-Hut; coincident bodies chain at the depth cap; θ=0 stays the exact all-pairs sum — don't "tighten" `NBodyTests`' error bound). `Docs/Simulation/Motion.md`. **`Color`** — CSS Color 4 named set + `Color(hex:)`, HSB (hue wraps), the OKLab family (`OKLab` / `OKLCH` / `OKHSL`, gamut-mapped, written from Ottosson), `Color.mix(_:_:t:in:)`, `Color(kelvin:)`, `withAlpha(_:)` / perceptual `luminance`, `Ramp` / `Palette` (ColorBrewer + harmonies), `CosinePalette` (iq), `Colormap`. **Palette import** — hex / CSV / TSV / JSON / ASE; the three text formats share **one parser** and `.auto` sniffs palette-per-line from the *file's* shape, an explicit format **overrides** the sniff (`palettePerLine: Bool?`, nil = decide); **no comment syntax is promised** (`#deface` is a color); loaders never trap on malformed bytes; **ship the loader, not the data** (bundled palette data stays license-gated). **Palette extraction** — weighted k-means++ → Lloyd in **OKLab**; identical pixels merged and **sorted before clustering**; output most-used first; deterministic given (image, count, seed). **Image dithering** — `Image.dithered`: 8 error-diffusion kernels + centered Bayer + generated `.blueNoise`; CPU-only by nature; **three color spaces, one per family — mixing them is the bug**: tone accumulates in **linear**, `.none` picks **OKLab**-nearest, error diffusion picks ***linear***-nearest (the false-contour rule, pinned by `DitherTests` on a 1×1 image); threshold maps run the **Yliluoma pair search** (variance-weighted penalty, linear mix fraction, dark→light phase); a fully transparent pixel diffuses no residual. **Print separations** — `Image.separated(into:paper:)` under the translucent-spot-ink overprint model (linear-light mixing, OKLab-judged coverage search); screening drops coverage under the **2% minimum dot**; the sketch declares **`printInks`** (NOT `inks`); `--export-separations` (registration band around the untouched artwork; recipes carry `ExportMetadata.inks`). `Docs/Output/PrintSeparations.md`. **Gradient paint** — `Gradient` (`.linear` / `.radial` / `.alongPath`) / `Paint` on every shape via a baked 256-texel LUT strip (SDF path) + per-vertex evaluation (tessellated path), with native SVG gradient export; shared CPU↔GPU structs live once in the C header (see *Shaders*). `Docs/Drawing/Geometry.md`, `Docs/Drawing/Color.md`, `Docs/Drawing/Epicycles.md`, `Docs/Drawing/Voronoi.md`, `Docs/Drawing/Truchet.md`, `Docs/Drawing/Attractors.md`, and the per-generator `Docs/Generators/*` pages.

- **3D (camera, depth, point clouds, meshes).** Opt-in: `camera(_:)` / `perspective(...)` / `ortho(...)` over `Camera3D` (per-frame state) allocates a camera-gated `depth32Float` buffer (a 2D-only frame allocates none and renders byte-identically). Right-handed, y-up; 3D rides the camera, not the 2D CTM. **Point clouds** (`PointCloud` / `drawPointCloud`, instanced camera-facing splats). **4×4 transform stack** (`translate(x,y,z)` / `rotateX/Y/Z` / `rotate(_:axis:)` / `scale` — a separate stack from the 2D CTM, bakes into geometry CPU-side). **Solid primitives** — the public `Mesh` value type + a broad generator catalog (`drawBox` / `drawSphere` / `drawIcosphere` / `drawCylinder` / `drawCone` / `drawCapsule` / `drawTorus` / `drawTorusKnot` / `drawPyramid` / `drawHelix` / the Platonic set / `drawMobius` / `drawKlein` / `drawSuperellipsoid` / `drawSupershape` / `drawExtrude` / `drawLathe` / `drawTube`) on a depth-tested mesh pipeline. **Per-vertex mesh colors** — `Mesh.colors` (aligned with `positions`, the `uvs` rule) **multiply** the resolved surface color at the `drawMesh` vertex bake (fill × material baseColor × vertex color, the texture contract — white fill shows a scan's colors untouched, fill stays a whole-mesh tint); an empty `colors` takes the constant-color path byte-identically, a wireframe ignores them (edges are the stroke), no shader/stride change (the mesh vertex already carried per-vertex color); painted via `Mesh.colored(by:)` (position+normal closure) / `colored(from: PointCloud)` (nearest sample over `PointGrid3`); `MeshColorTests` (transforms + Metal-gated multiply/variation probes). **Lights & materials** — `directionalLight` / `pointLight` / `spotLight` / `ambientLight` + `Light`, Blinn-Phong, **auto-lit by default** (`.auto` / `.custom` / `.off`, `lights()` / `noLights()`); curated `lightingPreset(_:)` / `LightingPreset`; **area lights** — `rectLight` / `diskLight` / `tubeLight` (`Light.rect/.disk/.tube`, kinds 3-5): linearly-transformed-cosine shading (rect = the exact horizon-clipped polygon integral, disk = the ellipse eigen + cubic + tabulated clipped-sphere path, tube = the analytic line integral × radius, capless) through the **bundled fitted tables** (`Resources/LTC/`, BSD-3 + notice, float32 from the reference `ltc.js`) at fragment **textures 8/9 on every `meshLitColor` carrier** (solid / textured / raymarch / half-res tier), gated by `OllinLighting.ltcEnabled` → **byte-identical with no area light**; `intensity` × `color` = the surface's **radiance** (physical falloff — the punctual kinds keep no-attenuation); **load-bearing:** the packed frame is right-handed (`tangent × bitangent = facing normal` — the shader's corner winding and disk front-test depend on it), the cubic/eigen **sqrt args clamp at 0** (float dip under exact-math-nonnegative → NaN black speckle where a surface grazes the disk's plane, a real fixed bug), non-PBR materials map `shininess` → perceptual roughness `(2/(s+2))^(1/4)` with specular = norm channel × `mat.specular` (inert at 0, the finish rule), the RT-reflection hit shade approximates a panel as a **centroid emitter with area falloff** (no LUT bind in a secondary bounce), and the SSS back term is the **flipped-normal integral** (carries the panel's falloff). **Area (rect/disk) cast shadows** — the caster search appends rect/disk **after** the punctual kinds (existing scenes keep their caster; tubes never cast — radial emission has no facing axis): the penumbra comes from the panel's **real extent** (RT device: rays to deterministic points on the actual panel surface — Vogel in a disk's plane, antithetic R2 pairs on a rect; else a spot-style map **aimed at the camera target**, PCSS radius = half-extent in texels capped at 40), `shadowSoftness` *scales* that extent (0 hard / 0.5 default = physical / 1 = 2×), the RT flip **overwrites `shadowDepthB`** with the softness scale (the map path needs the slot for the perspective linearization), and the shadow dims **both LTC integrals** inside the area branch (atten on `diffI`/`specI` → every model + SSS dim consistently; no caster multiplies by exactly 1.0, byte-identical). Examples `3D/Lighting/AreaLights` + `AreaShadows`; snapshots `area-lights` + `area-shadows` (RT-gated); `LightingTests` packing + `AreaLightRenderProbes` (one-sided / two-sided / falloff) + `AreaShadowRenderProbes` (on/off diff isolates the shadow; path-agnostic, so they cover the map fallback too). Mechanism: ARCHITECTURE.md § *Area lights* + § *Shadows*. the stylized **`material(_:)` / `Material`** library (toon / gooch + specular / Fresnel-rim / subsurface / iridescent / sparkle-flake finishes over a per-batch uniform; sparkle cells sized by `OllinLighting.sceneScale` so the default works at any scene scale); the **physically-based metallic-roughness finish** (`Material.physicallyBased` + `.metal(roughness:)` / `.dielectric(roughness:)` + the brushed/polished/plastic built-ins) — Cook-Torrance GGX (glTF 2.0 / Filament forms, reimplemented from the math), surface color stays `fill`; it's `shadingModel 3`, so every non-PBR material stays **byte-identical**. **Image-based lighting (IBL)** — `environment(_:)` / `Environment` lights the PBR materials from a surrounding HDRI via the **split-sum** bake (Karis; equirect→cube / irradiance / GGX-prefiltered mips / BRDF LUT, baked once per source and cached): **8 CC0 HDRIs bundled at 1K** (instant, offline, CI-safe) + **12 download-on-demand** (Poly Haven), user `.hdri(path:)` / `.hdri(downloadURL:)` / `.remote(url:fallbackResource:)`, `highRes(.twoK/.fourK/.eightK)` backdrop upgrades (the lighting never needs >1K), and the **procedural `.sky(turbidity:sunElevation:groundAlbedo:)`** — a zero-asset Hosek-Wilkie dome (vendored `External/CHosekWilkie`, BSD-3) generated with **no CPU read-back** (the read-back was the 3-fps bug), re-baked per frame at a reduced budget when animated (the HDRI path stays byte-identical), `rotated(_:)` spinning the sun for free; a neutral `.sky()` is the default placeholder while a download lands. Terse invariants (mechanism, caches, and downloads in [`ARCHITECTURE.md`](ARCHITECTURE.md) § *3D lighting and environments*): **mip the equirect *before* the equirect→cube pass** (else the cube reads empty → black IBL, a real bug); the bundled set is **PIZ half-float** (ImageIO won't decode dwab); **per-environment auto-exposure** (the set ranges ~800× in brightness) so any HDRI just works; non-PBR materials take the diffuse irradiance as their ambient (Gooch keeps its own ramp) so one environment lights every material — all gated on `iblEnabled` → **byte-identical with no environment**; export **blocks** on downloads (full-res art) while live shows a placeholder; the decoded equirect is blob-cached (`<hash>.equirectf16`) and read **off the render thread**; the in-memory bake cache is LRU-bounded (`IBLCacheTests`). The environment draws as a **skybox** backdrop by default (bicubic-reconstructed, resolution-aware `backgroundBlur`; `.lightingOnly()` opts out). (`Environment` is the bare public name, so Ollin's SwiftUI files qualify `@SwiftUI.Environment`, as they already do for `Image`.) **`matcap(_:)` / `Matcap`** (view-normal sphere-texture shading, 26 bundled CC0 looks + a CPU generator). **Meshes from file** — `loadMesh` / `Mesh(contentsOf:)`: Ollin's own OBJ + glTF readers beside Model I/O (`.usdz` / `.usdc` / `.usda` / `.stl` / `.ply` / `.abc`), carrying base-color material + texture + UVs; `bounds` / `center` / `normalized(scale:)` to recenter+fit. **Textured meshes** (`Mesh.uvs` / `MeshMaterial` / `mesh.textured(_:)`), **wireframe** (`wireframe(_:)`). **Cast shadows** — `castShadows()` / `noShadows()`: directional/spot via a 2D shadow map; point **ray-traced** on an RT GPU (inline `intersection_query` behind `OLLIN_RT_SHADOWS`), else a mid-point cube; rect/disk area panels ray-traced on an RT GPU, else a spot-style 2D map (the *Area cast shadows* rules above); `shadowQuality(_:)` maps hardware-relative ray counts. **Soft (PCSS) shadows** — `shadowSoftness(_:)` (0 hard → the legacy 3×3, byte-identical; 0.5 default contact-hardening; 1 very soft): blocker search → penumbra estimate → Vogel-disk PCF (`shadowFactorPCSS`, the Fernando/NVIDIA technique); **the penumbra ratio must be formed in light-linear depth** (a spot map linearizes via the projection's `[2][2]` term — details in ARCHITECTURE.md); the one knob also drives the RT point caster's area radius (the 0.5 default reproduces the old look, `point-shadows` byte-identical); 2D taps via `resolveShadowTaps2D` (export → `.detail`); soft-by-default measured sub-tolerance, so no snapshot re-records (the SSAO silhouette-AA precedent). **Ray-traced reflections** (`rayTracedReflections()`) — a physically-based metal reflects the **actual scene** (off-screen geometry included, no SSR contact-streaks), the hybrid-rendering approach written from the technique (Apple's WWDC21 session + Metal sample). It **integrates into the IBL specular, not a post-process** (the traced hit replaces the prefiltered env sample; a miss shows the sky), with **two-bounce, metalness-aware** hit shading — the second trace is load-bearing for mirror corners: an unoccluded env sample at the first hit pipes the HDRI's bright floor through as a razor-thin streak no AA can remove (a real fixed bug; dead ends recorded in ARCHITECTURE.md) — and grazing-widened env lobes (`ollin_rt_env_lobe`). It **reuses the shadow-caster accel** (per-geometry offsets buffer; `encodeShadowPass` decoupled so the accel builds when shadows *or* reflections are active), gated by `OllinLighting.rtReflections` → **byte-identical when off**; every solid mesh is reflectable; needs an environment + an RT GPU, off-by-default, non-RT → no-op. **Reflection AA is deferred + stochastic:** live temporally accumulates (an `SSRHistorySlot` twin, `statefulEncodeIsRepeat`-guarded); headless/export instead averages N deterministic in-frame rays (4/8/16 by tier — historyless, so no warmup and video exports can't flicker; snapshot `rt-reflections-3d` re-recorded once, supersampled). **Load-bearing:** render targets + the raymarched fields keep the inline single-ray trace (a thin wrapper over the shared hit shade, so the paths can't drift), and **no roughness-cone jitter without a spatial denoise stage** (a stochastic cone reads as sparkle on brushed metals). Mechanism: [`ARCHITECTURE.md`](ARCHITECTURE.md) § *3D lighting and environments* + § *Deferred ray-traced reflection AA*. **Depth-aware compositing** — `depth(at:)` / `noDepth()` / `project(_:)` / `withBillboard(at:){}` put 2D drawing inside the 3D depth buffer, or composite against a depth *feed* via `drawDepthScene(color:depth:…)` / the metric `drawDepthScene(_ frame: RGBDFrame)` + `Camera3D.fromIntrinsics` (a drawn cloud, a depth feed, and a placed object share one metric space). Wired depth-correct through live + headless `image(of:)`; accumulation / texture-handoff render 3D without depth sorting (documented limitation), and gradient paint isn't supported on the 3D path (solid color only). Building 3D drove the `Pipeline` enum → `Hashable PipelineKey` migration. `Docs/3D/3D.md`, `Docs/3D/DepthCompositing.md`; examples the `3D/*` set (PointCloud / DepthCloud / Transforms / Solids / Lighting / LoadedMesh / Shadows / RayTracedReflections / Materials / PhysicalMaterials / ImageBasedLighting / EnvironmentGallery / ProceduralSky / Matcap / DepthCompositing / MetricDepthScene / …); snapshots `sparkle-materials` / `pbr-materials` / `pbr-ibl` / `procedural-sky` / `rt-reflections-3d` (RT-gated via `Snapshot.hasRaytracing`, so it records/runs only on a ray-tracing GPU).

- **Camera control & cinematic moves.** Two opt-in halves over the shipped `Camera3D`, both driving the `Camera3D.orbiting` pose (object-on-a-turntable, not scene-flythrough) and set as the frame's camera — a sketch that calls neither keeps its own. **`cameraControl()`** hands the camera to the viewer (drag orbit, scroll dolly, right-/shift-/option-drag pan; exponential damping toward an input-driven goal + a capped momentum flick; the canonical orbit-control model, `deltaTime`-corrected, studied not ported). **`cameraMove(_:)`** plays a curated **`CameraMove`** — `.turntable` / `.sway` / `.pushIn` / `.pullOut` / `.tilt` / `.orbitAndRise` / `.reveal` / `.handheld` (cyclic ones phase-driven, finite ones on an internal `Timeline`, handheld a private Perlin field). Both seed the framing on the **first call only** and **compose over the current pose** (`CameraRig.lastMode` re-syncs the controller's goal after a move), so "frame by hand, then drift" is one call after another, and the rig clamps elevation off the poles. The shared **`Timeline<Value: Tweenable>`** sequencer (`Sources/Ollin/Math/Timeline.swift` — timed keyframes + per-segment easing + `loops`, `FrameAdvancing` like `@Eased`, `Tweenable` over `Double`/`Vector2`/`Vector3`) is the substrate, available to sketches (the future `@Param` keyframe client too). The rig (`Sources/Ollin/3D/CameraRig.swift` + `CameraMove.swift`) is a plain `let` advanced explicitly, **not** `FrameAdvancing` (the Mirror-collection cache only sees pre-first-frame stored properties); its platform-neutral input surface — `scrollDeltaY` (**double-buffered** in `advance()` so a wheel event between frames is never lost), `modifiers` (mapped behind the AppKit seam like `KeyCode`), `rightMouseIsPressed`, the `mouseWheel()` hook — is plumbed once through `OllinMTKView` so it reaches standalone + gallery + OllinLive. **`cameraShowcase(_:)`** is the fused default the ~42 auto-orbit `3D/*` examples adopt: it plays a `CameraMove` the viewer can interrupt and, after `idleReturn` (10s) of no input, eases back over `returnDuration` (4s, Hermite, shortest-arc azimuth) to the **opening** framing (the `CameraRig` anchor captured at `seed()`, *not* wherever the viewer left it), then resumes — a 3-phase `driving`/`manual`/`returning` state machine, **byte-identical to a plain move when uninteracted** (so headless/export/snapshot paths are unaffected and core `cameraMove`/`camera` are untouched). `.autoOrbit(period:)` is its curated default move; `activeCamera` exposes the resolved `Camera3D` (e.g. placing geometry at the camera `eye`, as `DepthCompositing` does). **Scene inspection views** — `cameraView(_:)` / `resetCamera()` snap the rig to canonical angles (`CameraView`: `.reset` restores the opening framing; the six axis views swing only the orbit angle, keeping target + radius; `.isometric` is true isometric, `.corner` the deprecated alias), gliding by default (`animated: false` cuts) and handing the pose back to the active driver via `CameraRig.driver` on completion, so motion resumes with no jump; **byte-identical when no snap is active** (`camera-move` not re-recorded); surfaced by the host **Camera menu** (`OllinCameraCommands`, ⌘0–⌘7 in all three hosts) → `OllinActiveSketch` → `SketchRunner.requestCameraView` (applied next frame); a 2D / hand-set-`camera()` sketch ignores it. **Live-only scene chrome:** `cameraAxis()` shows the interactive orientation widget (`AxisWidget`, a SwiftUI sibling — click an axis to snap, plus reset / isometric / an Ortho⇄Perspective toggle) and `groundGrid()` a faint reference floor at y=0 (a `.grid` `PipelineKey` pass, depth-tested-but-not-writing, *outside* the deferred render graph) — host chrome like the FPS overlay, **never exported, 2D no-op**, toggleable from the Camera menu. **Load-bearing grid invariant:** the infinite-grid LOD divides the **once-computed base world-uv derivative** by each decade scale — **never `dfdx()` of a pre-scaled uv** (the derivative spike at a LOD boundary shatters the lines into a crawling dotted band, a real fixed bug); major lines are distinguished **by width in the one line color**, not a brighter color or a separate per-class fade (the old form read as stacked planes at staggered depths — don't reintroduce it). Examples `3D/SceneViews`, `3D/CameraControl` + `3D/CameraMoves` (the lower-level demos); snapshot `camera-move` (a deterministic `.turntable` — the input-driven paths are covered by the `CameraRigTests` / `TimelineTests` CPU units, since a snapshot can't drive synthetic events). Full mechanism: [`ARCHITECTURE.md`](ARCHITECTURE.md) § *Camera rig*. `Docs/3D/Camera.md`, `Docs/Helpers/Animation.md` (the `Timeline` section).

- **RGBD recordings + live USB (Record3D)** (`OllinRecord3D` satellite). `Record3DRecording` opens a Record3D `.r3d` clip and `Record3DDevice` reads a tethered phone's live USB RGBD stream — both unproject color + depth with true intrinsics into a `PointCloud` (over the core `RGBDFrame` / `CameraIntrinsics`). All Apple-native decode (a minimal ZIP reader + LZFSE + JPEG) and a clean-room `USBMux` (usbmuxd) client to TCP 1337; the formats read clean-room (Record3D's library is LGPL — read its public structure, never translate; credit as a product reference, vendor nothing). **USB only, not WiFi.** **Gotchas:** `K` is column-major at capture resolution → scale to the depth grid; the depth grid isn't in the metadata (derive from the LZFSE float count + capture aspect); usbmuxd's `PortNumber` is network byte order; the trailing live-stream slot is a JSON misc blob, *not* confidence; unprojection lands in ARKit camera space (y-negate + z-negate). `RGBDFrame` gained `depth(atNormalizedX:y:)` / `unproject(normalized:)` (hole-robust median, y-flip). **Recorded poses:** `Record3DRecording.poses` / `pose(at:)` — per-frame camera-to-world from the metadata `poses` array (`[qx, qy, qz, qw, tx, ty, tz]`, quaternion order pinned by the identity's 1 in slot 3), the sweep-fusion path for a saved clip; **a clip captured without world tracking (TrueDepth) stores every pose as the identity** — real motion can't be recovered after the fact, so probe `poses` for variation before fusing (single frames reconstruct fine). `Docs/3D/Record3D.md`, `Docs/3D/RGBD.md`; examples `3D/Record3DCloud`, `Record3DLiveCloud`, `DepthLiftedPose`.

- **iPhone capture app (Ollin Capture + `OllinPhone`).** Ollin's own iOS app (`Apps/OllinPhoneApp`, xcodegen + Xcode) streams on-device ARKit perception over USB: a 3D body skeleton, a face (deforming mesh + 52 expression blendshapes, up to 3 faces), world-facing rear-LiDAR depth (a `PointCloud` + the 6DoF camera pose, via the core `RGBDFrame`), person segmentation (a matte + cutout, via the core-lifted `SegmentationImages`), and CoreMotion device motion. The Mac client `PhoneDevice` (`@MainActor`, self-retrying) exposes `latestBody` / `latestFaces` / `latestMotion` / `latestDepthFrame` / `latestSegmentationMatte` / `latestSegmentationCutout`; `PhoneBody` / `PhoneFace` / `PhoneMotion` mirror the Vision accessor shape. Transport: the shared `OllinUSBMux` tunnel + the `PhoneWire` codec (pure Foundation/simd, compiled verbatim into both ends; `streamPort` 1338). **World-pose fusion** — the core `WorldCloud` voxel accumulator + `PointCloud.transformed(by:)` place each depth frame by its pose into one fixed world cloud (sweep the phone, fuse the room). Body / Face / World / Segment are behind a phone-side camera toggle (mutually exclusive; motion always streams). **Gotchas:** the segmentation upright-rotation must be an accelerated `CGContext` draw, not a byte loop (debug-build frame rate); the 52 blendshapes ride the wire positionally so `PhoneBlendShape` raw values must stay contiguous 0…51; `PhoneWire.version` stays 1 pre-1.0 (both ends rebuild together). `Docs/3D/Phone.md`; examples `3D/PhoneBodyPose` / `PhoneFace` / `PhoneDepthCloud` / `PhoneWorldScan` / `PhoneSegmentation`.

- **Math & motion.** `map` / `dist` / `lerp`, the bare shaping scalars `clamp` / `fract` / `step` / `smoothstep` (spelled + argument-ordered to match the shader library so the vocabulary carries into per-pixel code; `Easing.smoothstep` is the same curve as an `Easing`, renamed from `smoothStep`), the loop-phase helpers `loopProgress(over:phase:)` / `pingPong(over:phase:)` (0…1 sawtooth / out-and-back fold of the sketch clock; `phase` is a fraction-of-loop head start), a resolution-relative `scale`, seedable `random` / `noise` (`randomGaussian` / `randomVector` / `ring` / `randomChoice` uniform+weighted / `shuffled` / `curlNoise` / `signedNoise`, a master `seed()`; gotcha: zero-width `random(a, a)` consumes no roll, so knob-scaled jitter rolls `random(-1, 1) * amount`), **looping noise** (`noise`/`signedNoise` `loop:`+`radius:` overloads — one lap tours a closed circle through the field and closes exactly, `loop` is `fract`-wrapped; the 2-coordinate form rides a 4D lattice, canonical 32-gradient `grad4`, its own contrast gain `gain4` calibrated to the 3D feel; circle centered at (0.5, 0.5) off the integer lattice on purpose), **`fbm`/`signedFbm`** (1D/2D/3D + 2D-loop forms, octaves/gain/lacunarity, normalized so `octaves: 1` == `noise`, name matches the shader lib), **the noise-variant family** (`Math/NoiseVariants.swift` + additions in `Noise.swift`; every name mirrored in the shader lib's noise module, all reseeded by the one `noiseSeed`): `simplexNoise`/`signedSimplexNoise` (1D/2D/3D, Gustavson's structure over the shared seeded permutation, contrast gains 81.3/87.7 calibrated to the Perlin field's ~2%-clip feel), `worley(x,y[,z], feature: .nearest/.second/.border, jitter:)` (seeded SplitMix-hashed cells; `.border` is zero on cell walls), `ridgedFbm` (Musgrave's crease fold with octave-weight feedback, reparameterized onto fbm's gain/lacunarity) / `turbulence` / `warpedFbm` (iq's two-level self-displacement; **`warp: 0` == `fbm` exactly**), each with the 2D `loop:` form, deterministic + pinned by `NoiseVariantTests`, `Generator` legs on the *Layered effects* bullet, `Docs/Generators/Noise.md`, examples `Patterns/RidgeLines` + `Shaders/DomainWarp` + `Effects/Cellular`, snapshots `ridge-lines` + `noise-toolkit`; `Double.tau`; easing (the `Easing` curve catalog + `@Eased`, plus the `Timeline<Value: Tweenable>` keyframe sequencer — worked example `Motion/Timeline`), input smoothing (`OneEuroFilter` + `@Smoothed`), and the damped spring (`Math/DampedSpring.swift`: `DampedSpring<Value: Smoothable>` + `@Sprung`, the third `FrameAdvancing` sibling — each step evaluates the exact closed-form damped-oscillator solution across the under/critical/over-damped branches, so any dt is stable and steps compose exactly (frame-rate-independent, pinned by `DampedSpringTests`); perceptual `duration`/`bounce` knobs (bounce 0 = critical, clamped (−1, 1]), velocity carries across retargets, `kick(_:)` throws; example `Motion/Springs`), all frame-rate-independent and auto-advanced per frame. `Docs/Helpers/Math.md`, `Docs/Generators/Random.md`, `Docs/Generators/Noise.md`, `Docs/Helpers/Animation.md`.

- **Canvas, capture & tooling.** `canvasSize` (the `CanvasSize` enum, 1080² default) previewed per `windowMode` (`.auto` / `.fixed` / `.resizable`); a maintained `Examples/` set incl. a `Recreations/` section; headless capture (`OllinApp.image(of:)`, `--export`) + deterministic frame-sequence export (`--export-sequence` + `--skip` warmup); **video & GIF export** (`OllinApp.exportVideo` / `exportGIF`, `--export-video` / `--export-gif` — AVAssetWriter h264/hevc/prores + ImageIO GIF over one fixed-timestep drive; codec / `--bitrate` / `--quality` control, the last gated `#if arch(arm64)`); **perfect-loop export** (`--export-loop` + the declared `Sketch.loopDuration` — renders exactly one period as a seamless GIF or video by extension; **the GIF lap is computed against the centisecond-quantized rate** `exportGIF` will actually play, or the frame count drifts off one period; example `Motion/PerfectLoop`, seam-verified byte-identical one period apart); **vector SVG export** (`--export-svg` — a CPU-only serializer, curved SDF shapes traced via marching squares; the opt-in `--hatch` / `Hatching` fill-to-line-work transform for pen plotters); **vector PDF export** (`--export-pdf` / `OllinApp.pdf` / `exportPDF` — replays the *same* recorded commands (`OllinApp.recordVectorFrame`, the shared front half) through a Core Graphics PDF context, so the SVG and PDF of a frame can't drift; hatching and `--frame` apply, both vector flags may ride one invocation; 1 canvas px = 1 PDF pt, so the **paper `CanvasSize` presets** (`.usLetter` / `.usLegal` / `.a3` / `.a4` / `.a5`, points-sized, portrait like the sheet) export true-to-size pages, and **`.dpi(300)`** scales the pixel grid for print-res raster while the `.page` case carries the point size so the PDF page stays the physical sheet; `PDFExportTests` pins structure + rasterized-pixel probes); **print-separation export** (`--export-separations` / `OllinApp.exportSeparations` — one grayscale master per declared `printInks` ink + a registration-marked overprint preview; the machinery and invariants live in the *Geometry & color* bullet); **reproducibility metadata** — every PNG / SVG / PDF / video export embeds a one-line JSON recipe (`ExportMetadata`: the recorded seed(s), `@Param` values, working-dir git hash + `-dirty`, frame/fps) — PNG `Description` text chunk, SVG comment, PDF Subject, video description item; GIF has no slot; **the video writer defers `startWriting` to the first rendered frame** so the recipe can carry the seed `setup()` applies (writer metadata is immutable once writing starts); seeds are recorded at init from `variation` and re-recorded by `randomSeed`/`noiseSeed` (`Sketch.recordedRandomSeed`/`recordedNoiseSeed`; `ExportMetadataTests`); the whole export/bench flag surface lives in `OllinApp.handleCommandLine(_:makeSketch:)` (`Sketch.main()` routes through it, and `OllinLive` runs it on a *loose watched sketch file* — `swift run OllinLive Sketch.swift --export-gif …` — compiling the file only when a flag matches, so any Guide-style sketch exports without needing a target); the `extend(...)` `SketchExtension` seam (setup / beforeDraw / afterDraw / afterFrame / frameRendered, opt-in `wantsRenderedFrame` / `wantsRenderedTexture`); **live reload** (`swift run OllinLive`, the typed `@Param` inspector controls, the shared `FrameStats`); the **live-coding performance host** (`swift run OllinLiveCoding` — evaluate-on-command over the same engine; see the *Live coding* block under § *Live reload* below, `Docs/Tools/LiveCoding.md`); the **examples gallery** (`swift run OllinExamples`) — a two-sidebar host on the OllinLive window recipe (fixed `HStack` + `.contentSize` + unified gradient toolbar + title-bar accessory toggles, the chrome shared via the core `WindowChrome.swift`): left, the example list as a collapsible tree mirroring the folder groups (`ExampleCatalog.tree`, disclosure state persisted, an Xcode-style bottom filter bar) — arrow keys navigate it because the canvas takes keyboard focus **only on click** (`SketchView(keyboardFocus: .onClick)`, a glass "click the sketch to use the keyboard" hint floated only for sketches whose *source* reads keys, `Example.usesKeyboard`); right, the hidable inspector (⌘/, the shared `MonitorCardView` + `@Param` knobs — so the gallery omits `OllinHUDCommands`/the detached panel). `Examples/3D` is grouped into topic subfolders (`Camera`/`Depth`/`Effects`/`Environments`/`Geometry`/`Lighting`/`Materials`/`Phone`/`Raymarching`), targets renamed to match (`Example-3D-<Group>-<Name>`); render-correctness snapshot tests (`Tests/OllinTests`, re-record via `OLLIN_RECORD_SNAPSHOTS=1`); CI. The AVFoundation link lives in the core for offline export only. `Docs/Output/Export.md`, `Docs/Core/Canvas.md`, `Docs/Helpers/Parameters.md`.

- **Variations & seed exploration.** A sketch is a *generator*, so its seed is first-class: `Sketch.init` rolls **`variation`** (a five-digit `Int`, 1…99_999) and seeds **both** `random` and `noise` from it, so an unseeded run still varies yet always names the number that brings it back (every export's recipe now carries a `seed`). **Load-bearing:** `seed(_:)` *sets* `variation` (it's the one host-navigable seed); `randomSeed`/`noiseSeed` are **partial reseeds that leave `variation` alone** (so the recipe still splits them). Two unseeded exports of a randomness-free sketch differ **only** in the recorded seed (`SVGExportTests` pins body-equality; snapshot pixels unaffected, none re-recorded). Live navigation is **`SketchRunner.restart(variation:)`**, an in-place restart on the *same instance* (reseed → `frameCount = 0` → `resetAccumulation()` → re-run `setup()` next frame), so `@Param` values survive; it must call **`sketch.loop()`**, not just unpause the view — a still `noLoop()` sketch's fresh `noLoop()` is only a *change* (and only re-pauses) if the running state was restored first. The shared `VariationCardView` (core `Live/`) drives it from all four inspector surfaces via `OllinActiveSketch.runner`, and `SketchSession.recordSeed` carries a navigated seed across hot reload the way tuned knobs do (applied in `syncParams` **before** `setup()`, so a sketch that pins its own seed still wins; pinned by `OllinLiveCoding --sessiontest`). Export side: **`--seed N` reseeds before `setup()` on every export path** (one `make()` wrapper in `handleCommandLine`), and **`--export-grid`** / `OllinApp.contactSheet(of:seeds:)` / `exportContactSheet` tile one frame per seed into a labeled proof sheet, with **`--export-sweep`** / `contactSheet(of:sweeping:values:seed:)` the same sheet along one named `@Param` instead (values apply via the hosts' `ParamStored` restore path so a tile matches the dragged knob; **every tile pins one seed** — given, or rolled once and recorded — so the knob is the only thing changing; unknown name → nil + the sketch's actual parameter list on stderr; both sheets share one private tiling core; **the framing is deliberate, don't re-blend it**: seeds = a piece's *identity* (the Variations story, matching the generative-art seed culture — @eaviles keeps `Docs/Core/Variations.md` seed-pure) while the sweep is a *tuning* tool, documented on the Parameters/Export pages) — a **fresh sketch per tile** (no state leak) over **one reused renderer** (`OllinApp.renderImage(of:frame:fps:renderer:)`, factored out of `image(of:)`), `resetAccumulation()` between tiles, tile width floored at 64px, the sheet's own recipe carrying the seed list (`ExportMetadata.sheetRecipe`). Example `Randomness/Variations` (a whole composition per seed — the decisions live in `setup()`); `VariationTests`. `Docs/Core/Variations.md`, `Docs/Output/Export.md`.

- **Next.** Everything shipped is inventoried above; the forward plan is the Quick map under *Roadmap & design notes* below, plus [`ROADMAP.md`](ROADMAP.md) / [`DESIGN-NOTES.md`](DESIGN-NOTES.md).

## Live reload — edit code, see it render (shipped via `OllinLive`)

The headline creative-coding feature, and it's built. `swift run OllinLive <path/to/Sketch.swift>` opens a window, watches the file, and on save recompiles *just that sketch* into a `.dylib` and hot-swaps it into the running loop — the window never closes. This is the Olive / canvas-sketch model (a persistent host owns the window; the sketch is the swappable unit), chosen over InjectionIII (needs an external app, finicky on the terminal `swift run` workflow Ollin lives in). Code in `Sources/OllinLive/` (`main`, `FileWatcher`, `LiveSession`, plus `--selftest`/`--watchtest` headless smoke tests); the reusable loader `SketchLoader` lives in the **`OllinRuntime`** library (shared with the examples gallery, kept *out* of the shipping `Ollin` framework); the runner-side swap is `SketchRunner.reload(to:)` in `SketchView.swift`.

The mechanism (module-path discovery, the factory shim, FSEvents flags, threading, the `@Param` plumbing) is in [`ARCHITECTURE.md`](ARCHITECTURE.md) § *Live reload and the live-coding hosts*; the rules that must survive:

- **`-undefined dynamic_lookup` against the host, not a dynamic Ollin product.** **A `.dynamic` product does not fix the two-copies problem** — SwiftPM still links the target statically into the executable, giving two `Sketch` copies and a failed cast (verified the hard way). The `-I` module dirs are **discovered, not assumed** (the classic and Xcode build layouts differ), vendored C targets' checked-in module maps under `External/` are collected too (else a loose sketch can't `import OllinPhysics`), and each load uses a unique `-module-name` so repeated reloads don't collide in the objc runtime.
- **FSEvents on the *directory*, never an fd watch** (editors save atomically — temp file + rename; the `UseCFTypes` flag is required or the path-array cast crashes), debounced ~150ms, dispatched by extension: `.swift` → recompile + swap; `.metal` → shader reload; image assets → re-run `setup()`.
- **A broken edit never kills the session:** recompiles run off the main thread (the window keeps drawing the old sketch), a compile error leaves the running sketch alone, and shader reloads build the new pipelines before committing.
- **State on reload: fresh restart by default; `--keep-clock` carries `time`/`frameCount` forward** so an animation's phase doesn't jump (instance state resets either way). `Sketch.onReload()` fires once after the post-reload setup, never on first launch. `reload(to:)` honors the *new* sketch's declared `canvasSize` for non-`.resizable` modes.
- **Keep `Sketch`/`draw()` `open`** (already true) — the dylib subclass must override them; never `final`.

Window title: `"Ollin - <SketchName>"` (plain hyphen, not a middot). @eaviles ruled out FPS-in-the-title (the oF idiom); live FPS lives in the hosts' inspector surfaces (the OllinLive and gallery sidebars; standalone, the detached ⌘/ panel), not the title bar.

**Single-file sketches — the `ollin` command (`Scripts/ollin`), shipped.** One loose `.swift` file is a whole sketch, run from any directory: `ollin <file.swift>` opens it in the live host (all flags pass through, so every export flag works on the loose file), `ollin new [name]` writes an executable starter file (class named from the file name), `ollin install [dir]` symlinks the script onto PATH. **Load-bearing:** the script resolves the repo from its own symlink-resolved location and execs `swift run --package-path`, so a PATH symlink is the whole install and `git pull` self-heals on the next run — capture `${0:A}` at **top level** (inside a zsh function `$0` is the *function name*, a real bug) — and sketch paths are absolutized before handoff (the host process runs with the repo as its package, not the caller's cwd). **Hashbang:** `#!/usr/bin/env ollin` + `chmod +x` makes a sketch directly executable; `SketchLoader` neutralizes a leading `#!` to `//` **in place** (same length, so diagnostics keep exact line/column) and compiles from the work-dir copy, since swiftc allows a hashbang only in a main file — applies to `.file` *and* `.source` inputs (the live-coding editor buffer carries the line too). Pinned by two `--selftest` phases (hashbang renders; a broken hashbang sketch reports `Sketch.swift:5:`). `Docs/Tools/SingleFile.md`; the Guide's Ch 1 points at it.

**GUI / parameter knobs — shipped, typed.** The generic `@Param` wrapper/registry in the core (`Param.swift`) drives the shared inspector (`Inspector.swift`) in all three hosts; **the control follows the property's type** — `Double` slider / `Int` stepper / `Bool` toggle / `ParamOption` enum menu / `Color` well / `Vector2` fields (+ `style: .pad`) / `Vector3` / `Rectangle` / `Insets` / `ClosedRange<Double>` two-thumb slider / `String` field / `style: .segmented` / the **`ParamChoices`** named-catalog menu — through the type-erased `AnyParam` / `ParamControl` surface, so hosts never touch `Param<Value>`; `ParamValue` is public, so a user type can conform by mapping onto an existing control kind (no custom rows). `icon:` / `group:` decorate rows into titled group cards; numeric value boxes scrub (Option fine, Shift coarse, click to type). Rules: `ParamChoices` needs `Equatable` to find the current selection (why `Easing`, a closure wrapper, and the parameterized `Material` finishes stay out; `LightingPreset` conforms); knob values persist across reloads as `ParamStored` payloads (`syncParams`; a property that changed *type* drops its stale value so the new default wins); OSC/MIDI bind to `Param<Double>`, and smoothing stays Double-only. The SwiftUI layout gotchas (the scrub-pill `.fixedSize()`, the segmented ViewThatFits wrap) are in ARCHITECTURE.md. Headless gates: `OllinLive --paramtest` + `ParamTests`.

**Performance stats — a shared `FrameStats` feeding every host surface.** The runner fills one `FrameStats` (`@Observable`: fps, CPU frame time, draw counts, clock, canvas size) via the built-in `StatsExtension` on the extend seam; the OllinLive sidebar, the gallery's right sidebar, and — standalone — a detached frosted `NSPanel` (⌘/ via `OllinHUDCommands`) read it. A host with its own inspector sidebar opts the panel off (`SketchView(showsInspectorPanel: false)`) and omits `OllinHUDCommands`. Stats are *debug chrome, not in-canvas drawing* — SwiftUI siblings of the Metal view, so they never land in exports. Gotcha: the CPU-ms must time `performDraw()` *alone* — timing across `renderer.render()` includes the triple-buffer semaphore (vsync) wait and pins to ~1/fps, telling you nothing. `SketchView` `ZStack`s host chrome over a private `MetalCanvas` — the `NSViewRepresentable` MTKView, still the AppKit/UIKit-portability seam.

**Live coding — the performance host (shipped via `OllinLiveCoding`).** `swift run OllinLiveCoding [Sketch.swift]` (release wrapper `Scripts/OllinLiveCoding`) is the on-stage instrument: one window, the sketch letterboxed on a black stage, the code over it as translucent text (per-glyph backdrop strips, not a panel), ⌘↩ compiles the editor buffer and hot-swaps mid-motion (`keepClock` **on by default**; ⌘⇧↩ evaluates fresh), ⌘S is the only thing that writes the `.swift` (evaluate never saves; a crash-recovery autosave rides every evaluation). Load-bearing invariants, cheap to forget:

- **The shared engine lives in `OllinRuntime.SketchSession`** (supersede-cancel compile scheduling, `syncParams` re-applied *before* the swap so knobs never snap, the two-channel Swift-vs-shader error model where neither clears the other); `LiveSession` and `PerformanceSession` are thin wrappers over it — orchestration changes go in `SketchSession`, never re-forked per host.
- **Buffer compiles** write the editor text into the per-compile work dir *under the sketch's own file name* (diagnostics carry exact buffer lines and match by `fileName` — never by path, it's the temp copy), while the factory shim's `Bundle.module` keeps pointing at the sketch's *real* folder so co-located assets resolve. An untitled buffer compiles as a stand-in path under Application Support; save first if the sketch needs assets.
- **Letterbox at the SwiftUI layer only** (`.aspectRatio(_, contentMode: .fit)` around `SketchView`): the canvas view stretches to its frame and maps the mouse by bounds, so the aspect-fit frame is what keeps rendering *and* mouse coordinates exact.
- **No `.windowResizability(.contentSize)` here** (unlike the sibling hosts): fullscreen is the projection mode (`.fullScreenPrimary` via `WindowCustomizer`), the stage flexes, and status/error UI are *stage overlays* (chip, evaluated toast, diagnostics strip) because fullscreen hides the title bar. The error strip never dims the stage — the last good sketch keeps playing through a typo.
- **Editor rules:** the `NSTextView` owns the buffer (never mirror it into SwiftUI state); hide-code (⌃⇧H) is `.opacity` + hit-testing, never a structural `if` (that destroys the buffer and undo stack); every automatic substitution is off (a smart quote is a silent compile break); `keyboardFocus: .onClick` so the editor keeps the keys, and a canvas click intentionally hands them to the sketch.
- **Recovery is scoped to its document** (`recovery-origin` beside `recovery.swift`): an untitled buffer's net must never offer itself over an opened file (a real early miss — restoring replaced an opened sketch with old untitled work).
- Headless gates: `--selftest` (buffer compile → render → edit → differ, plus line-accurate diagnostic parsing; asserts the buffer path never writes the file) and `--sessiontest` (param carry, edited defaults, supersede semantics — the extraction's regression net for both hosts).

## Roadmap & design notes

Planned, in-progress, and eventual work is documented outside this file so the always-loaded guidance stays small:

- [`ROADMAP.md`](ROADMAP.md) — the public, contributor-facing roadmap: what's planned and how to help.
- [`DESIGN-NOTES.md`](DESIGN-NOTES.md) — the engineering design intent behind each planned item (the approach, the APIs involved, the reasoning).

**When work ships, it leaves these two files.** Drop it from `ROADMAP.md` (the roadmap stays forward-looking — what's *planned*, not a changelog) and record it instead under *Current state* above. Remove it from `DESIGN-NOTES.md` too, unless its design rationale still guides future maintenance or evolution (e.g. the conventions for extending the examples set), in which case keep only that part, not the status narration. Anything fully done and self-contained belongs in its own `Docs/` page or this file — not the roadmap, not the design notes.

**No status words in `ROADMAP.md` / `DESIGN-NOTES.md`.** They describe only what's *ahead*: never write "X landed / shipped / done" or a changelog aside into either file — a reader shouldn't be able to tell from them what shipped yesterday versus a year ago. When the shipped thing is one entry in a still-open *umbrella* item (the SDF shape catalog, a family of helpers), **drop just that entry and keep the umbrella** — don't annotate it as landed. (The `drawTriangle` commit got this wrong: it rewrote the catalog bullet to announce the triangle "has landed" instead of silently dropping it — the exact narration this rule forbids.) The ship checklist, every time something lands: **(1)** add it to *Current state* in this file; **(2)** remove it from `ROADMAP.md` (drop the entry; for an umbrella, the sub-entry only) with no "landed" note — and the README's Roadmap section is a short pointer with a one-line "what's ahead" list, so drop the item there too if it's named; **(3)** strip it from `DESIGN-NOTES.md`, keeping only rationale that still guides future work; **(4)** if it adds user-facing API, give it a `Docs/` page *and* wire it into both indexes in the *same* commit — `Docs/README.md` (the flat annotated index, one grouped entry per page) and the README, which links **by capability** through its *What's in it* catalog table, so a new page joins the table row it belongs to (or the short "good places to start" list) rather than a flat enumeration. **The invariant is that every `Docs/` page stays reachable from the README** — it broke once when that flat list was slimmed and orphaned 21 pages, so check it (`comm` the README's `Docs/…md` links against `find Docs -name '*.md'`) rather than eyeballing. A commit that introduces a public concept but leaves `Docs/` untouched is missing its page; purely internal changes (a refactor, a renderer tweak) skip this and live only in *Current state*; **(5)** if it adds a user-facing capability, **teach it in the Guide, don't just list it** — give it a row in the feature-coverage matrix (`Guide/PLAN.md`) naming its chapter home, clear its parking-lot entry, and get the row to at least **`shown`** (a chapter mention plus a worked example) *in this commit*, with `taught` (a section plus a figure) the goal. A row left at `pointed` is Guide debt and needs an entry in the ledger in `Guide/PLAN.md` naming the chapter that owes it, called out in the commit message. **`Scripts/guide-coverage.sh` is the gate** (it fails on a `pointed` row with no debt entry, a Docs page with no row, and a page unreachable from a numbered chapter); the rationale and the authoring workflow live in `Guide/AUTHORING.md` § *Coverage*. A row marked `pointed` used to satisfy this step outright, which is how seven slices shipped un-taught while audits stayed green. **A renderer change moves Guide figures even when the diff never touches `Guide/`** — run `Scripts/guide-figures.sh` after any change to the drawing paths and commit the re-renders with it, or the next Guide-touching commit inherits unexplained churn (the stroke-join work moved 79 figures by a sub-perceptual ~2/255 at their joins, invisible to the snapshot suite's mean-diff tolerance but not to a byte comparison). Do all of this only once the work is actually verified (built and run) — don't pre-narrate unbuilt work as shipped.

Both live at the repo root, alongside `README.md`. The convention: the root holds project meta and contributor docs (`README.md`, `ROADMAP.md`, `DESIGN-NOTES.md`, [`ARCHITECTURE.md`](ARCHITECTURE.md), `AGENTS.md`, this file, `LICENSE`, `ATTRIBUTION.md`, `THIRD-PARTY-NOTICES.md`), while [`Docs/`](Docs/) is the user-facing API reference (`Sketch`, `Drawing`, `Color`, …) plus `Docs/Tools/` for the user-facing host-app guides (live coding) — and nothing else. The third doc tree is [`Guide/`](Guide/), the narrative book: a practical creative-coding guide taught through Ollin, written chapter-by-chapter across sessions. Its authoring charter is `Guide/AUTHORING.md`, its chapter queue + briefs + feature-coverage matrix `Guide/PLAN.md`, its figures rendered/verified by `Scripts/guide-figures.sh` — guide facts route there, not here. `ARCHITECTURE.md` is the contributor-facing engineering-internals doc (the *how* and *why* of shipped systems, the home for depth that would otherwise bloat this file); `DESIGN-NOTES.md`/`ROADMAP.md` stay forward-looking, `Docs/` stays user-facing, so `ARCHITECTURE.md` is the one place shipped internals belong. Keep these at the root (don't fold them into a `Notes/`-style folder): several are auto-discovered or front-page (`README`/`LICENSE`/`CLAUDE.md`/`AGENTS.md`), and `ARCHITECTURE.md` wants root-level discoverability by convention.

Quick map of what's *ahead*, rough priority (everything shipped is inventoried under *Current state* above — don't re-list it here): the capability tier — the broader iPhone-as-sensor-array stream (scene mesh, richer body data) plus drift-correcting a long world-pose sweep, building on the shipped `OllinRecord3D` / `OllinPhone` / `WorldCloud` slices. Bigger bets: a project generator (the 3D mode, PBR/IBL, softer shadows, the layered-effects tier, and the live-coding performance app are now in — 2D stays the default and untaxed). Eventual: Swift Playgrounds & iOS, visionOS, AR.
