#### <sup>[Ollin](../../README.md) → [Examples](../README.md) → Patterns</sup>

---

## Patterns

Generative patterns: grids, tessellations, and rule-based repetition. Shape *primitives* and
shape *composition* (booleans and SDF combinators) live in [Shapes](../Shapes/).

| [![AntColony](https://media.ollin.art/examples/Patterns/AntColony/still-640.jpg)](AntColony/) | [![Apollonian](https://media.ollin.art/examples/Patterns/Apollonian/still-640.jpg)](Apollonian/) | [![Buddhabrot](https://media.ollin.art/examples/Patterns/Buddhabrot/still-640.jpg)](Buddhabrot/) | [![Caustic](https://media.ollin.art/examples/Patterns/Caustic/still-640.jpg)](Caustic/) |
|---|---|---|---|
| [AntColony](AntColony/) | [Apollonian](Apollonian/) | [Buddhabrot](Buddhabrot/) | [Caustic](Caustic/) |
| [![ChaoticMaps](https://media.ollin.art/examples/Patterns/ChaoticMaps/still-640.jpg)](ChaoticMaps/) | [![Chladni](https://media.ollin.art/examples/Patterns/Chladni/still-640.jpg)](Chladni/) | [![CirclePacking](https://media.ollin.art/examples/Patterns/CirclePacking/still-640.jpg)](CirclePacking/) | [![ContourMap](https://media.ollin.art/examples/Patterns/ContourMap/still-640.jpg)](ContourMap/) |
| [ChaoticMaps](ChaoticMaps/) | [Chladni](Chladni/) | [CirclePacking](CirclePacking/) | [ContourMap](ContourMap/) |
| [![Cracks](https://media.ollin.art/examples/Patterns/Cracks/still-640.jpg)](Cracks/) | [![CreasePattern](https://media.ollin.art/examples/Patterns/CreasePattern/still-640.jpg)](CreasePattern/) | [![DeBruijn](https://media.ollin.art/examples/Patterns/DeBruijn/still-640.jpg)](DeBruijn/) | [![Delaunay](https://media.ollin.art/examples/Patterns/Delaunay/still-640.jpg)](Delaunay/) |
| [Cracks](Cracks/) | [CreasePattern](CreasePattern/) | [DeBruijn](DeBruijn/) | [Delaunay](Delaunay/) |
| [![Dendrite](https://media.ollin.art/examples/Patterns/Dendrite/still-640.jpg)](Dendrite/) | [![DifferentialGrowth](https://media.ollin.art/examples/Patterns/DifferentialGrowth/still-640.jpg)](DifferentialGrowth/) | [![ElementaryCA](https://media.ollin.art/examples/Patterns/ElementaryCA/still-640.jpg)](ElementaryCA/) | [![Flocking](https://media.ollin.art/examples/Patterns/Flocking/still-640.jpg)](Flocking/) |
| [Dendrite](Dendrite/) | [DifferentialGrowth](DifferentialGrowth/) | [ElementaryCA](ElementaryCA/) | [Flocking](Flocking/) |
| [![ForceGraph](https://media.ollin.art/examples/Patterns/ForceGraph/still-640.jpg)](ForceGraph/) | [![FordCircles](https://media.ollin.art/examples/Patterns/FordCircles/still-640.jpg)](FordCircles/) | [![FractalFlame](https://media.ollin.art/examples/Patterns/FractalFlame/still-640.jpg)](FractalFlame/) | [![Girih](https://media.ollin.art/examples/Patterns/Girih/still-640.jpg)](Girih/) |
| [ForceGraph](ForceGraph/) | [FordCircles](FordCircles/) | [FractalFlame](FractalFlame/) | [Girih](Girih/) |
| [![Grid](https://media.ollin.art/examples/Patterns/Grid/still-640.jpg)](Grid/) | [![Guilloche](https://media.ollin.art/examples/Patterns/Guilloche/still-640.jpg)](Guilloche/) | [![HexGrid](https://media.ollin.art/examples/Patterns/HexGrid/still-640.jpg)](HexGrid/) | [![Hitomezashi](https://media.ollin.art/examples/Patterns/Hitomezashi/still-640.jpg)](Hitomezashi/) |
| [Grid](Grid/) | [Guilloche](Guilloche/) | [HexGrid](HexGrid/) | [Hitomezashi](Hitomezashi/) |
| [![HyperbolicTiling](https://media.ollin.art/examples/Patterns/HyperbolicTiling/still-640.jpg)](HyperbolicTiling/) | [![IteratedFunctions](https://media.ollin.art/examples/Patterns/IteratedFunctions/still-640.jpg)](IteratedFunctions/) | [![Kaleidoscope](https://media.ollin.art/examples/Patterns/Kaleidoscope/still-640.jpg)](Kaleidoscope/) | [![Lichtenberg](https://media.ollin.art/examples/Patterns/Lichtenberg/still-640.jpg)](Lichtenberg/) |
| [HyperbolicTiling](HyperbolicTiling/) | [IteratedFunctions](IteratedFunctions/) | [Kaleidoscope](Kaleidoscope/) | [Lichtenberg](Lichtenberg/) |
| [![LowDiscrepancy](https://media.ollin.art/examples/Patterns/LowDiscrepancy/still-640.jpg)](LowDiscrepancy/) | [![Maze](https://media.ollin.art/examples/Patterns/Maze/still-640.jpg)](Maze/) | [![Meander](https://media.ollin.art/examples/Patterns/Meander/still-640.jpg)](Meander/) | [![ParquetDeformation](https://media.ollin.art/examples/Patterns/ParquetDeformation/still-640.jpg)](ParquetDeformation/) |
| [LowDiscrepancy](LowDiscrepancy/) | [Maze](Maze/) | [Meander](Meander/) | [ParquetDeformation](ParquetDeformation/) |
| [![Penrose](https://media.ollin.art/examples/Patterns/Penrose/still-640.jpg)](Penrose/) | [![Pentominoes](https://media.ollin.art/examples/Patterns/Pentominoes/still-640.jpg)](Pentominoes/) | [![Percolation](https://media.ollin.art/examples/Patterns/Percolation/still-640.jpg)](Percolation/) | [![Phyllotaxis](https://media.ollin.art/examples/Patterns/Phyllotaxis/still-640.jpg)](Phyllotaxis/) |
| [Penrose](Penrose/) | [Pentominoes](Pentominoes/) | [Percolation](Percolation/) | [Phyllotaxis](Phyllotaxis/) |
| [![PowerDiagram](https://media.ollin.art/examples/Patterns/PowerDiagram/still-640.jpg)](PowerDiagram/) | [![Pursuit](https://media.ollin.art/examples/Patterns/Pursuit/still-640.jpg)](Pursuit/) | [![RidgeLines](https://media.ollin.art/examples/Patterns/RidgeLines/still-640.jpg)](RidgeLines/) | [![Roses](https://media.ollin.art/examples/Patterns/Roses/still-640.jpg)](Roses/) |
| [PowerDiagram](PowerDiagram/) | [Pursuit](Pursuit/) | [RidgeLines](RidgeLines/) | [Roses](Roses/) |
| [![Schottky](https://media.ollin.art/examples/Patterns/Schottky/still-640.jpg)](Schottky/) | [![ShapeGrammar](https://media.ollin.art/examples/Patterns/ShapeGrammar/still-640.jpg)](ShapeGrammar/) | [![ShapePacking](https://media.ollin.art/examples/Patterns/ShapePacking/still-640.jpg)](ShapePacking/) | [![Spectre](https://media.ollin.art/examples/Patterns/Spectre/still-640.jpg)](Spectre/) |
| [Schottky](Schottky/) | [ShapeGrammar](ShapeGrammar/) | [ShapePacking](ShapePacking/) | [Spectre](Spectre/) |
| [![Spirograph](https://media.ollin.art/examples/Patterns/Spirograph/still-640.jpg)](Spirograph/) | [![Spirolateral](https://media.ollin.art/examples/Patterns/Spirolateral/still-640.jpg)](Spirolateral/) | [![Stippling](https://media.ollin.art/examples/Patterns/Stippling/still-640.jpg)](Stippling/) | [![Streamlines](https://media.ollin.art/examples/Patterns/Streamlines/still-640.jpg)](Streamlines/) |
| [Spirograph](Spirograph/) | [Spirolateral](Spirolateral/) | [Stippling](Stippling/) | [Streamlines](Streamlines/) |
| [![Subdivision](https://media.ollin.art/examples/Patterns/Subdivision/still-640.jpg)](Subdivision/) | [![TenPrint](https://media.ollin.art/examples/Patterns/TenPrint/still-640.jpg)](TenPrint/) | [![Topography](https://media.ollin.art/examples/Patterns/Topography/still-640.jpg)](Topography/) | [![TriangleGrid](https://media.ollin.art/examples/Patterns/TriangleGrid/still-640.jpg)](TriangleGrid/) |
| [Subdivision](Subdivision/) | [TenPrint](TenPrint/) | [Topography](Topography/) | [TriangleGrid](TriangleGrid/) |
| [![Truchet](https://media.ollin.art/examples/Patterns/Truchet/still-640.jpg)](Truchet/) | [![Turmites](https://media.ollin.art/examples/Patterns/Turmites/still-640.jpg)](Turmites/) | [![UlamSpiral](https://media.ollin.art/examples/Patterns/UlamSpiral/still-640.jpg)](UlamSpiral/) | [![Venation](https://media.ollin.art/examples/Patterns/Venation/still-640.jpg)](Venation/) |
| [Truchet](Truchet/) | [Turmites](Turmites/) | [UlamSpiral](UlamSpiral/) | [Venation](Venation/) |
| [![Voronoi](https://media.ollin.art/examples/Patterns/Voronoi/still-640.jpg)](Voronoi/) | [![WangTiles](https://media.ollin.art/examples/Patterns/WangTiles/still-640.jpg)](WangTiles/) | [![WaveFunctionCollapse](https://media.ollin.art/examples/Patterns/WaveFunctionCollapse/still-640.jpg)](WaveFunctionCollapse/) | [![Wavefront](https://media.ollin.art/examples/Patterns/Wavefront/still-640.jpg)](Wavefront/) |
| [Voronoi](Voronoi/) | [WangTiles](WangTiles/) | [WaveFunctionCollapse](WaveFunctionCollapse/) | [Wavefront](Wavefront/) |

| Example | What it shows |
|---|---|
| [Anamorphosis](Anamorphosis/Sketch.swift) | a plate that reads as nothing until a mirrored cylinder reflects it: a word is wrapped around the cylinder, and each point of it is traced from the eye, off the mirror, and down to the page; a panel shows what that eye receives, read back off the finished plate (`Anamorphosis`) |
| [Apollonian](Apollonian/Sketch.swift) | an Apollonian gasket: every gap between three touching circles is filled by the one circle that touches all three, and the color changes with each generation, so the filling order shows (`apollonianGasket`) |
| [Bifurcation](Bifurcation/Sketch.swift) | the logistic map's bifurcation diagram drawn as an ink print, with the Lyapunov exponent traced beneath it: as the growth rate sweeps, the settled orbit forks, doubles again and again into chaos, and opens its periodic windows; parameters zoom the range in view (`IteratedMap`, `drawBifurcation`, `makeBatch`) |
| [Billiards](Billiards/Sketch.swift) | one bouncing rule in four rooms: in a circle the path wraps around a hole it can never enter, in an ellipse the foci sort the paths, and a stadium and a square with a post in it both fill with scribble; a fan of balls shows which rooms keep the fan together (`Billiard`) |
| [BlueNoise](BlueNoise/Sketch.swift) | blue noise fed to the tessellator: `poissonDisk` scatters points so that no two are closer than a radius (Bridson's dart-throwing), `voronoi` cuts one cell per site, and the even cells show the even but organic spacing (`poissonDisk`, `voronoi`, `drawShape`) |
| [Chladni](Chladni/Sketch.swift) | a vibrating square plate stepping through its Chladni figures: sand gathers along the nodal lines, which do not move, while the modes blend from one to the next and the whole cycle loops (`chladni`) |
| [CirclePacking](CirclePacking/Sketch.swift) | circle packing three ways, switched with one key: a pack that seeds itself and fills the gaps, a closed-form foam grown at blue-noise sites, and an overlapping crowd pushed apart until nothing overlaps, with every radius kept (`packCircles`, `packCircles(around:)`, `relaxCircles`) |
| [ChaoticMaps](ChaoticMaps/Sketch.swift) | every `ChaoticMap` preset on a parameter, iterated into a density field that accumulates additively, so the filaments are bright where the orbit returns again and again: the dissipative maps run one continuous orbit, and the nearly area-preserving Gumowski-Mira blossom starts a fresh orbit every frame and stacks them, the way the classic plates do (`ChaoticMap`, `noClear`, `blendMode(.add)`) |
| [ContourMap](ContourMap/Sketch.swift) | a topographic map of a drifting fbm terrain, traced by marching squares, with every fifth level drawn heavier, like a cartographer's index contour (`isolines`, `fbm`) |
| [Cracks](Cracks/Sketch.swift) | crack growth: cracks that branch at right angles divide the plane into city-block cells, and each crack drags a translucent grain wash across the open space on one side of it (`CrackGrowth`, `noClear`) |
| [Dendrite](Dendrite/Sketch.swift) | diffusion-limited aggregation: random walkers freeze where they first touch the cluster, so frost-like dendrites grow from a seed at the center, tinted by arrival time (`DiffusionLimitedAggregation`) |
| [Lichtenberg](Lichtenberg/Sketch.swift) | a Lichtenberg figure growing live: each step, the dielectric-breakdown model solves the field around the discharge and grows where the field is strongest, so the branches spread outward and arc to the rim (`DielectricBreakdown`) |
| [DifferentialGrowth](DifferentialGrowth/Sketch.swift) | a closed ring relaxed under attraction, alignment, and repulsion, with long edges splitting, so it folds live into meanders like brain coral (`DifferentialGrowth`) |
| [Meander](Meander/Sketch.swift) | a river migrating across a parchment plain, driven by its own curvature: bends slide downstream, oxbow lakes pinch off, and the recorded scars are drawn as ribbons beneath the water (`Meander`) |
| [Kolam](Kolam/Sketch.swift) | one line launched at 45 degrees between a field of dots, bouncing off the edges until it closes: `rows` by `columns` dots make exactly gcd(rows, columns) loops, so 7 by 5 is one unbroken loop and 6 by 4 is two |
| [ElementaryCA](ElementaryCA/Sketch.swift) | 1D cellular automata stacked as rows, both the elementary kind and the 3-color totalistic kind, with the rule and the code on parameters, so one number switches the picture between order, fractals, and chaos (`elementaryCA`, `totalisticCA`) |
| [Flocking](Flocking/Sketch.swift) | a few hundred boids, each steering only by its neighbors (separate, align, cohere), so the flock swirls and regroups with no leader (`Boids`) |
| [ForceGraph](ForceGraph/Sketch.swift) | a growing network that lays itself out live: repulsion spreads the nodes, edges pull them together, hubs emerge from rich-get-richer growth, and you can drag any node while the web reflows around it (`ForceLayout`) |
| [FractalFlame](FractalFlame/Sketch.swift) | a random fractal flame refining live: the chaos game with nonlinear variations, structural coloring, and the log-density display, accumulated slice by slice; click to reroll (`FractalFlame`, `FractalFlame.Renderer`) |
| [Buddhabrot](Buddhabrot/Sketch.swift) | the escaping orbits of the Mandelbrot set exposed as a density plate that rises out of the noise live; click to switch between the false-color and grayscale plates (`Buddhabrot`, `Buddhabrot.Renderer`) |
| [Percolation](Percolation/Sketch.swift) | site percolation sweeping back and forth through the critical threshold: clusters are tinted by size, and the spanning cluster lights up with its outline traced (`Percolation`) |
| [Girih](Girih/Sketch.swift) | Islamic star patterns by the polygons-in-contact method: a honeycomb's strapwork changes shape as the contact angle sweeps, under a girih-tile medallion of a decagon and pentagons at the classic 54 degrees (`girihPattern`, `Girih.Tile`) |
| [Knotwork](Knotwork/Sketch.swift) | the kolam line given width and a rule for which cord passes over which: the choice alternates along every cord, and that alternation is what makes it read as a weave instead of a tangle |
| [Grid](Grid/Sketch.swift) | both point layouts of `Grid` side by side: under the default `.center` distribution, `cells` outlines the frames and `points` pulses a dot at each center, and beside it a `.spanning` lattice reaches its dots to the edges; a modulo rule on `row` and `column` sorts the dots into black and white bands |
| [HexGrid](HexGrid/Sketch.swift) | a pulse radiating across a honeycomb in exact rings of hex distance, re-centered on the hexagon under the mouse (`hexGrid`, `distance(from:to:)`, `cell(at:)`) |
| [InversionFractal](InversionFractal/Sketch.swift) | the lace-like limit set of a ring of tangent circles, resampled every frame so that it shimmers; click to change the ring (`inversionLimitSet`, `inverted`) |
| [IteratedFunctions](IteratedFunctions/Sketch.swift) | the chaos game building up the fern, the triangle, and the carpet on the accumulation surface, a few thousand visits per frame (`IFS`, `ifsPoints`, `fitted`) |
| [Kaleidoscope](Kaleidoscope/Sketch.swift) | one drawn arm copied into a sixteen-fold mandala by `symmetry(8, mirrored: true)`, so every draw call is repeated through the mirrors |
| [Kleinian](Kleinian/Sketch.swift) | Kleinian limit sets drawn as one ordered closed curve, with a bright head that runs the whole lap on each loop; click for the next preset (`kleinianLimitSet`, `KleinianPreset`) |
| [LevyFlight](LevyFlight/Sketch.swift) | a Lévy flight: step lengths follow a power law, so tight scribbled clusters are joined by sudden long jumps, and a comet retraces the journey out and back (`levyFlight`) |
| [LowDiscrepancy](LowDiscrepancy/Sketch.swift) | random, Poisson-disk, Halton, and Sobol scatters side by side while the count rises and falls: the sequences stay even at every count, and they only ever add points, never reshuffle (`haltonPoints`, `sobolPoints`) |
| [LSystem](LSystem/Sketch.swift) | the built-in L-system presets, one per cell: small rewriting grammars expanded into fractal curves and branching plants (`LSystem`, `drawLSystem`) |
| [ParametricLSystem](ParametricLSystem/Sketch.swift) | the same rewriting grammar with numbers on every symbol and arithmetic in every rule, so a branch can carry its own length and a rule can taper it (`ParametricLSystem`) |
| [Marbling](Marbling/Sketch.swift) | paper marbling in closed form: bull's-eye drops feathered by combs, a stylus pull, and a stirred vortex, with every ink kept as a vector outline; a click drops ink, and a drag pulls the stylus (`Marbling`, `drawMarbling`) |
| [Maze](Maze/Sketch.swift) | the walls of a perfect maze drawn as clean merged line work, with its longest path traced through it; each re-roll moves to the next carving algorithm, because the algorithm decides the texture (`maze`, `longestPath`) |
| [Penrose](Penrose/Sketch.swift) | a Penrose tiling with the matching-rule arcs stroked on top, where every arc meets a neighbor's across the shared edge, and light pulses outward from the five-fold sun; a click swaps the P3 rhombs for the P2 kites and darts (`penroseTiling`) |
| [Phyllotaxis](Phyllotaxis/Sketch.swift) | a sunflower seed head: `phyllotaxis(count:spacing:)` places each seed at the golden angle and moves outward by `sqrt(i)`; the seeds are sized by age and drawn with one `drawCircles` batch, and the head spins slowly while the count rises and falls |
| [RidgeLines](RidgeLines/Sketch.swift) | stacked mountain ridgelines from ridged fractal noise, where each row's opaque panel hides the range behind it; the noise field loops, so the terrain returns to its start every lap (`ridgedFbm`) |
| [Clothoid](Clothoid/Sketch.swift) | a closed route where every corner is an easement in, an arc, and an easement back out, driven at a steady speed; the bend is drawn as a comb off the route, and a steering wheel reads it too, so if you pull `easement` to zero the comb becomes a square step (`Clothoid`, `clothoidCorners`) |
| [Pursuit](Pursuit/Sketch.swift) | runners on the corners of a polygon, each running toward the next: the ring shrinks and turns at the same time, the paths are logarithmic spirals, and the caption compares the distance run with the law that predicts it (`Pursuit`) |
| [Rivers](Rivers/Sketch.swift) | the drainage of a landscape, computed rather than drawn: water runs to the steepest neighbor, the flow through a cell is the sum of every cell upstream of it, and the network above a threshold is stroked by Strahler order over the contour map it came from (`Drainage`) |
| [Roses](Roses/Sketch.swift) | the rose-curve chart laid out over a `Grid`: columns raise `n` and rows raise `d` in `k = n/d`; an odd `n` gives `n` petals and an even `n` gives `2n`, a fractional `k` weaves stars, and duplicates that reduce to an earlier fraction are inked in the accent color; each rose spins one petal per lap (`rose`) |
| [Schottky](Schottky/Sketch.swift) | the circle orbit of a Schottky group toured around the Apollonian gasket: circles nest inside circles without end, and the whole figure leans with the trace (`schottkyCircles`) |
| [SelfAvoidingWalk](SelfAvoidingWalk/Sketch.swift) | one unbroken lattice path that never crosses itself; it backtracks out of dead ends, so it winds long and dense, and it is colored along its length (`selfAvoidingWalk`) |
| [ShapePacking](ShapePacking/Sketch.swift) | packing that respects each shape's outline: a few shapes are added per frame, each grown against the outlines already placed, so small shapes settle into notches, and the picture accumulates under `noClear`; a click runs the same packing to completion in one call, with a fresh variation on each press (`ContinuousPacking`, `packShapes`) |
| [Spectre](Spectre/Sketch.swift) | the einstein tile: a spectre patch grown by substitution, where every tile is the same shape with the same handedness, and the rare odd mystics glow as a tide drifts across (`spectreTiling`) |
| [HyperbolicTiling](HyperbolicTiling/Sketch.swift) | regular tilings of the hyperbolic plane seen through the Poincare disk, where identical tiles meet the same way at every vertex and repeat toward a circular horizon, with a slow camera pan (`hyperbolicTiling`) |
| [Spirograph](Spirograph/Sketch.swift) | the toy gear set: one gear pair drawn at several pen distances and stacked into a glowing rosette, with the pen sliding in and out over the loop and the rosette spinning exactly one lobe per lap (`hypotrochoid`) |
| [Guilloche](Guilloche/Sketch.swift) | the engine-turned ornament of watch faces and banknotes: concentric wavy rings, each turned slightly against its neighbor, weave into a braided moire that drifts with time (`guilloche`) |
| [Subdivision](Subdivision/Sketch.swift) | recursive subdivision in the grid-painting look: repeated splits make unequal panels, a few take primary colors under heavy rules, and the picture re-rolls every few seconds (`subdivide`) |
| [ShapeGrammar](ShapeGrammar/Sketch.swift) | a window frame, one rule, and a lattice that divides itself: a cell becomes two cells, split by one straight line between two of its edges, and the rule is applied over and over (`ShapeGrammar`) |
| [Stippling](Stippling/Sketch.swift) | weighted-Voronoi stippling: a painted, lit sphere rebuilt from 5,200 dots, packed more densely where the picture is darker (`stipple`) |
| [Streamlines](Streamlines/Sketch.swift) | evenly spaced streamlines through a noise flow field; each line stops when it comes close to one already drawn, so they fan into smooth currents that never cross (`FlowField.streamlines`) |
| [Topography](Topography/Sketch.swift) | a pulsing blob inset inward over and over until the region pinches out, with each surviving ring stroked, so the contour lines crowd where the form narrows; it is pure line work, so it plots straight to SVG (`Shape.offset(by:join:)`) |
| [TriangleGrid](TriangleGrid/Sketch.swift) | a shimmer over alternating up and down triangles, where the two orientations read one noise field through two palettes (`triangleGrid`) |
| [TenPrint](TenPrint/Sketch.swift) | a maze of diagonals from one coin toss per cell, drawn as joined runs with a color each; hold the mouse to see the plain per-cell version (`tenPrint`, `runs`, `lines`) |
| [Wavefront](Wavefront/Sketch.swift) | a wave leaving a shoreline by Huygens' construction, breaking into pieces where it travels into a bend; hold the mouse to see the wavelets themselves (`huygensFront`) |
| [Caustic](Caustic/Sketch.swift) | the bright curve at the bottom of a cup, built in one call from the wall and the light source, with the raw reflected rays glowing under it and the whole curve traced faintly past the rim (`caustic`, `reflectedRays`, `drawEnvelope`) |
| [DeBruijn](DeBruijn/Sketch.swift) | a ring of sixty-four beads in which every run of three colors appears exactly once, and a reader that finds its position on the ring from three beads alone (`DeBruijnCode`, `deBruijnSequence`) |
| [Pentominoes](Pentominoes/Sketch.swift) | all twelve pentominoes fitted into a board, laid one at a time in the order the search found them, while the tray of unused pieces empties beside it (`Polyomino`, `tilePolyominoes`, `outlines`) |
| [PowerDiagram](PowerDiagram/Sketch.swift) | territories for circles of different sizes, where each cell is weighted so that no boundary cuts its own circle; hold the mouse to see the plain Voronoi diagram of the same centers (`powerDiagram`, `WeightedSite`) |
| [FordCircles](FordCircles/Sketch.swift) | a circle for every fraction, arriving one denominator at a time and never overlapping; hold the mouse to join the pairs that touch (`fordCircles`, `fareySequence`, `touches`) |
| [Spirolateral](Spirolateral/Sketch.swift) | a wall of walks that step 1, 2, 3 … and turn, each repeated until it returns to its start; the orders that never return are drawn pale and walking away, and holding the mouse changes the turn to a fifth of a turn (`spirolateral`, `closes`, `run`) |
| [UlamSpiral](UlamSpiral/Sketch.swift) | the primes written in a square spiral, where the diagonals they fall on drift as the starting number changes; hold the mouse to see the walk itself (`ulamSpiral`, `primePoints`, `path`) |
| [Truchet](Truchet/Sketch.swift) | one tile per grid cell, turned by a seeded choice, with both built-in tiles sharing the canvas: quarter arcs weave meandering loops on the left, corner-to-corner diagonals cut maze corridors on the right, and a flow field sweeps hue along both (`truchet`) |
| [Hitomezashi](Hitomezashi/Sketch.swift) | one bit per grid line decides whether its row of dashes starts on the edge or one cell in, and the shifted lines join at the crossings into steps, staircases, and loops (`hitomezashi`) |
| [ParquetDeformation](ParquetDeformation/Sketch.swift) | one tiling whose tile changes shape as you read across it, a soft pinwheel on one side and an interlocking key on the other, with the run between them sliding back and forth as a moving front (`ParquetDeformation`) |
| [Turmites](Turmites/Sketch.swift) | small Turing machines painting a grid that wraps at its edges: Langton's ant and its highway, spiral builders, and chaotic weavers, all chosen from a preset parameter (`Turmite`) |
| [AntColony](AntColony/Sketch.swift) | an ant colony solving a tour, seen from above: the whole colony walks once per frame and lays its pheromone again, so a haze over every pair of cities condenses onto a short route (`AntColony`) |
| [CreasePattern](CreasePattern/Sketch.swift) | the flat sheet and the folded result side by side: every line the paper is creased along, marked with the two directions a fold can go, beside the shape it folds into (`CreasePattern`) |
| [Venation](Venation/Sketch.swift) | space colonization: veins grow live from a root at the bottom toward a blue-noise scatter of attractors, with branch thickness from the pipe model (`SpaceColonization`, `poissonDisk`) |
| [Voronoi](Voronoi/Sketch.swift) | a field of sites drifting on noise, divided into Voronoi cells, evened out with Lloyd relaxation, and colored by a sweeping focal point; the cursor is a live site, so the cells form around the mouse (`voronoi`/`lloyd`) |
| [Delaunay](Delaunay/Sketch.swift) | the triangle half of the tessellation pair: a drifting point set is meshed live into well-shaped triangles, and the star of the site under the pointer glows with its Delaunay neighbors lit; those are the sites whose Voronoi cells would border it (`delaunay`, `neighbors(of:)`) |
| [TextureSynthesis](TextureSynthesis/Sketch.swift) | the overlapping model of Wave Function Collapse: a small picture written in the source supplies its own square patches, and a much larger texture is built so that every overlap agrees (`OverlappingWFC`, `wfc(from:)`) |
| [WangTiles](WangTiles/Sketch.swift) | Wang tiles: because edge colors must agree, independent seeded picks become one connected quilt, re-laid every few seconds and drawn as the classic edge triangles (`wangTiling`, `drawWangTiling`) |
| [WarpGrid](WarpGrid/Sketch.swift) | a checkerboard of rects warped under the mouse (`drawRect`) |
| [WaveFunctionCollapse](WaveFunctionCollapse/Sketch.swift) | a pipe network solved by Wave Function Collapse: min-entropy observation, weighted collapse, and constraint propagation over edge sockets, repeated until every neighbor connects legally (`wfc`, `drawWFC`) |

Run one with `swift run Example-Patterns-<Name>`, for example `swift run Example-Patterns-Grid`.
