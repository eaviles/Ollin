/// The Ollin framework version. Pre-1.0, so the public API still churns (the
/// README disclaims stability) — bump this as releases are cut. Surfaced in the
/// standalone stats panel; the single source of truth for "what build is this".
public enum OllinVersion {
    /// Semantic version string.
    public static let current = "0.2.0"
}
