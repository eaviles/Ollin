// SwiftPM needs at least one compiled source for the target; the module's
// contents are entirely in COllinSourceKit.h. Including it here also checks
// that it compiles as C.
#include "COllinSourceKit.h"
